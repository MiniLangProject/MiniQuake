diff --git a/WinQuake/net_wins.c b/WinQuake/net_wins.c
index 5094e1d..0000000 100644
--- a/WinQuake/net_wins.c
+++ b/WinQuake/net_wins.c
@@ -19,8 +19,12 @@ See the GNU General Public License for more details.
 */
 // net_wins.c
 
+#ifdef MINIQUAKE_PINNED_ORACLE
+#include "net_wins_oracle_stubs.h"
+#else
 #include "quakedef.h"
 #include "winquake.h"
+#endif
 
 extern cvar_t hostname;
 
@@ -52,7 +56,9 @@ struct hostent FAR * (PASCAL FAR *pgethostbyaddr)(const char FAR * addr,
 int (PASCAL FAR *pgetsockname)(SOCKET s, struct sockaddr FAR *name,
 							   int FAR * namelen);
 
+#ifndef MINIQUAKE_PINNED_ORACLE
 #include "net_wins.h"
+#endif
 
 int winsock_initialized = 0;
 WSADATA		winsockdata;
@@ -575,3 +581,7 @@ int WINS_SetSocketPort (struct qsockaddr *addr, int port)
 }
 
 //=============================================================================
+
+#ifdef MINIQUAKE_PINNED_ORACLE
+#include "net_wins_oracle_bridge.inc"
+#endif
