diff --git a/WinQuake/gl_screen.c b/WinQuake/gl_screen.c
index 8a0c096..0000000 100644
--- a/WinQuake/gl_screen.c
+++ b/WinQuake/gl_screen.c
@@ -20,7 +20,11 @@ You should have received a copy of the GNU General Public License
 
 // screen.c -- master for refresh, status bar, console, chat, notify, etc
 
+#ifdef MINIQUAKE_PINNED_ORACLE
+#include "gl_screen_oracle_stubs.h"
+#else
 #include "quakedef.h"
+#endif
 
 /*
 
@@ -249,7 +253,11 @@ float CalcFov (float fov_x, float width, float height)
 Must be called whenever vid changes
 Internal use only
 =================
 */
+#ifdef MINIQUAKE_PINNED_ORACLE
+void SCR_CalcRefdef (void)
+#else
 static void SCR_CalcRefdef (void)
+#endif
 {
 	vrect_t		vrect;
 	float		size;
