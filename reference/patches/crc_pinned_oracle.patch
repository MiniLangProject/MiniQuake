diff --git a/WinQuake/crc.c b/WinQuake/crc.c
index ca2d9cb..0000000 100644
--- a/WinQuake/crc.c
+++ b/WinQuake/crc.c
@@ -19,8 +19,12 @@ See the GNU General Public License for more details.
 */
 /* crc.c */
 
+#ifdef MINIQUAKE_PINNED_ORACLE
+#include "crc_oracle_stubs.h"
+#else
 #include "quakedef.h"
 #include "crc.h"
+#endif
 
 // this is a 16 bit, non-reflected CRC using the polynomial 0x1021
 // and the initial and final xor values shown below...  in other words, the
