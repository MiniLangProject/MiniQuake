diff --git a/WinQuake/net_dgrm.c b/WinQuake/net_dgrm.c
index 2267a26..0000000 100644
--- a/WinQuake/net_dgrm.c
+++ b/WinQuake/net_dgrm.c
@@ -19,11 +19,17 @@ See the GNU General Public License for more details.
 */
 // net_dgrm.c
 
+#ifdef MINIQUAKE_PINNED_ORACLE
+#include "net_dgrm_oracle_stubs.h"
+#endif
+
 // This is enables a simple IP banning mechanism
 #define BAN_TEST
 
 #ifdef BAN_TEST
-#if defined(_WIN32)
+#if defined(MINIQUAKE_PINNED_ORACLE)
+/* Network address declarations are supplied by the oracle stub header. */
+#elif defined(_WIN32)
 #include <windows.h>
 #elif defined (NeXT)
 #include <sys/socket.h>
@@ -49,8 +55,12 @@ unsigned long inet_addr(const char *cp);
 #endif
 #endif	// BAN_TEST
 
+#ifdef MINIQUAKE_PINNED_ORACLE
+#include "net_dgrm_oracle_stubs.h"
+#else
 #include "quakedef.h"
 #include "net_dgrm.h"
+#endif
 
 // these two macros are to make the code more readable
 #define sfunc	net_landrivers[sock->landriver]
@@ -1390,3 +1394,7 @@ qsocket_t *Datagram_Connect (char *host)
 				break;
 	return ret;
 }
+
+#ifdef MINIQUAKE_PINNED_ORACLE
+#include "net_dgrm_oracle_bridge.inc"
+#endif
