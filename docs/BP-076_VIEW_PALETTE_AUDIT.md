# BP-076 view/palette audit

Reference unit: `view.c`.

The block covers roll, bob, pitch drift, damage kick, contents and powerup cshifts, gamma tables, palette blend, stair smoothing, gun angles, intermission refdef and the historical 0.03125 view nudge.
