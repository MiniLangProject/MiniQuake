# BP-064 – Network and Windows-platform closure

The candidate contract `network_platform_109_frozen_v1` binds public NET
lifecycle, connectionless Protocol 3 control messages, datagram framing,
WinSock address behavior, the QHOST protocol and dedicated WinMain system
behavior.  The Windows acceptance script additionally launches two independent
server/client process pairs and requires byte-identical normalized client
reports.
