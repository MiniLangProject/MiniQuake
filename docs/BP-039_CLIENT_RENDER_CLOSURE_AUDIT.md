# BP-039 audit — client/render closure

The final step joins active client entities, frame-local temporary beam models,
efrag visibility and the renderer's opaque/sprite passes. The candidate
contract is `client_render_109_frozen_v1`, fingerprint `0x95e2b295`.
