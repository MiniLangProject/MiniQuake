# BP-076 — view, palette and refdef parity

- Binds gamma-table, cshift, bob, damage-kick and refdef behavior from `view.c`.
- Preserves Binary32 boundaries and the 0.03125 BSP view nudge.
- Adds view/palette runtime fixtures.
