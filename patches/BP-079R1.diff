diff --git a/BLOCK_LEDGER.json b/BLOCK_LEDGER.json
index 8ffaa96..e48f9a0 100644
--- a/BLOCK_LEDGER.json
+++ b/BLOCK_LEDGER.json
@@ -4,7 +4,7 @@
   "block_id": "BP-075-079",
   "parent_package_id": "BP-070-074R6",
   "final_package_id": "BP-079",
-  "delivery_revision": "BP-075-079",
+  "delivery_revision": "BP-075-079R1",
   "core_assets_memory_status": "core_assets_memory_109_frozen_v1",
   "core_assets_memory_fingerprint": "0x6c8d974d",
   "steps": [
@@ -75,7 +75,7 @@
     "game_data": "external; never bundled",
     "pixel_similarity": "inherited deterministic evidence; original-image corpus remains external"
   },
-  "delivery_parent": "BP-070-074R6",
+  "delivery_parent": "BP-075-079",
   "r1_delivery_hotfix": {
     "revision": "BP-070-074R1",
     "parent": "BP-070-074",
@@ -187,5 +187,25 @@
     "contract_fingerprint": "0x6c8d974d",
     "trace_sha256": "80695b98a91b7d52f91b35dcd220c3cc0962edb6851b0411832746f6f94d3486",
     "trace_rolling_hash": "d905b042"
+  },
+  "bp075079r1_delivery_hotfix": {
+    "revision": "BP-075-079R1",
+    "parent": "BP-075-079",
+    "status": "Windows acceptance pending",
+    "predecessor_result_archive": "MiniQuake_BP-075-079_RESULTS_20260730-143415.zip",
+    "predecessor_result_sha256": "78963a812a05fe9d4951eef1d1cb425757c3a63513c9a956481b36f5033a0384",
+    "failure": "inherited BP-029 checker compared the complete downstream server.ml hash and rejected unrelated host-command changes",
+    "root_cause": "world/physics routines remained byte-identical; the historical whole-file golden was too broad for downstream packages",
+    "fix": "preserve strict historical whole-file mode and add downstream mode with eight exact files, a protected server.ml hash and three accepted server world/physics sections",
+    "server_protected_sha256": "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781",
+    "server_sections": {
+      "runWorldPhysicsWithRetouch": "5b082bf6147659c213fe46e84679d44a73f11dcd2aac070ef51eaf66dd0ecc8b",
+      "runNonClientPhysicsWithRetouch": "24a05fed078529237192653250631835a9d22d6d6c9054abb0ea5121f13095a6",
+      "frameMode": "09549e467d9a73e0802fda2c71552348fe7470dd6a84766098d02c90cbf87921"
+    },
+    "production_source_changed": false,
+    "native_source_changed": false,
+    "contract_fingerprint_changed": false,
+    "fixture_count_changed": false
   }
 }
diff --git a/CHANGELOG_BP-075-079R1.md b/CHANGELOG_BP-075-079R1.md
new file mode 100644
index 0000000..950cd95
--- /dev/null
+++ b/CHANGELOG_BP-075-079R1.md
@@ -0,0 +1,16 @@
+# MiniQuake BP-075–BP-079R1
+
+## Fixed
+
+- The inherited BP-029 world/physics checker no longer rejects unrelated downstream edits in `src/miniquake/server.ml`.
+- Historical strict mode still binds the exact accepted BP-029 whole-file source hashes.
+- New downstream mode binds all eight unchanged authoritative files by their accepted hashes, the three world/physics-relevant `server.ml` functions independently, and the complete `server.ml` source after masking only the documented BP-079 host-command regions.
+- `build.ps1` invokes the inherited world/physics checker with `--allow-downstream-package`.
+- Added a revision-specific Windows runner, collector metadata, result analysis and block ledger.
+
+## Unchanged
+
+- No file under `src/` or `native/` changed from BP-075–BP-079.
+- All 112 BP-075..BP-079 runtime fixtures are unchanged.
+- `world_physics_109_frozen_v1` remains `0x2235d77c`.
+- `gameplay_presentation_109_frozen_v1` remains `0xad91624c`.
diff --git a/COLLECT_RESULTS.ps1 b/COLLECT_RESULTS.ps1
index 0ea0779..9f53a3c 100755
--- a/COLLECT_RESULTS.ps1
+++ b/COLLECT_RESULTS.ps1
@@ -7,11 +7,11 @@ $Root = $PSScriptRoot
 $Build = Join-Path $Root "build"
 $PackageId = "BP-079"
 $BlockId = "BP-075-079"
-$DeliveryRevision = "BP-075-079"
-$DeliveryParent = "BP-070-074R6"
+$DeliveryRevision = "BP-075-079R1"
+$DeliveryParent = "BP-075-079"
 $Timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
-$Staging = Join-Path $Build (".bp075-079-results-{0}" -f $Timestamp)
-$Archive = Join-Path $Build ("MiniQuake_BP-075-079_RESULTS_{0}.zip" -f $Timestamp)
+$Staging = Join-Path $Build (".bp075-079r1-results-{0}" -f $Timestamp)
+$Archive = Join-Path $Build ("MiniQuake_BP-075-079R1_RESULTS_{0}.zip" -f $Timestamp)
 
 if (-not (Test-Path -LiteralPath $Build -PathType Container)) { throw "build directory does not exist: $Build" }
 if (Test-Path -LiteralPath $Staging) { Remove-Item -Recurse -Force -LiteralPath $Staging }
@@ -76,6 +76,13 @@ $RootFiles = @(
   "COLLECT_RESULTS.ps1",
   "test.ps1",
   "TEST_BP-075-079.ps1",
+  "TEST_BP-075-079R1.ps1",
+  "CHANGELOG_BP-075-079R1.md",
+  "docs\BP-075-079R1_TESTING.md",
+  "docs\BP-075-079R1_RESULT_ANALYSIS.md",
+  "docs\BP-075-079R1_HOTFIX_REPORT.json",
+  "docs\BP-075-079R1_BLOCK_LEDGER.json",
+  "patches\BP-079R1.diff",
   "CHANGELOG_BP-075.md",
   "CHANGELOG_BP-076.md",
   "CHANGELOG_BP-077.md",
diff --git a/PORT_LEDGER.json b/PORT_LEDGER.json
index fbce61a..b411895 100644
--- a/PORT_LEDGER.json
+++ b/PORT_LEDGER.json
@@ -2,8 +2,8 @@
   "schema_version": 1,
   "project": "MiniQuake",
   "current_package": "BP-079",
-  "current_delivery_revision": "BP-075-079",
-  "delivery_parent": "BP-070-074R6",
+  "current_delivery_revision": "BP-075-079R1",
+  "delivery_parent": "BP-075-079",
   "parent_package": "BP-078",
   "compatibility_target": {
     "reference": "WinQuake/GLQuake 1.09",
@@ -1816,5 +1816,25 @@
     "contract_status": "gameplay_presentation_109_frozen_v1",
     "contract_fingerprint": "0xad91624c",
     "accepted_parent": "BP-070-074R6"
+  },
+  "bp075079r1_hotfix": {
+    "revision": "BP-075-079R1",
+    "parent": "BP-075-079",
+    "status": "Windows acceptance pending",
+    "predecessor_result_archive": "MiniQuake_BP-075-079_RESULTS_20260730-143415.zip",
+    "predecessor_result_sha256": "78963a812a05fe9d4951eef1d1cb425757c3a63513c9a956481b36f5033a0384",
+    "failure": "inherited BP-029 checker compared the complete downstream server.ml hash and rejected unrelated host-command changes",
+    "root_cause": "world/physics routines remained byte-identical; the historical whole-file golden was too broad for downstream packages",
+    "fix": "preserve strict historical whole-file mode and add downstream mode with eight exact files, a protected server.ml hash and three accepted server world/physics sections",
+    "server_protected_sha256": "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781",
+    "server_sections": {
+      "runWorldPhysicsWithRetouch": "5b082bf6147659c213fe46e84679d44a73f11dcd2aac070ef51eaf66dd0ecc8b",
+      "runNonClientPhysicsWithRetouch": "24a05fed078529237192653250631835a9d22d6d6c9054abb0ea5121f13095a6",
+      "frameMode": "09549e467d9a73e0802fda2c71552348fe7470dd6a84766098d02c90cbf87921"
+    },
+    "production_source_changed": false,
+    "native_source_changed": false,
+    "contract_fingerprint_changed": false,
+    "fixture_count_changed": false
   }
 }
diff --git a/PORT_STATUS.md b/PORT_STATUS.md
index 3892384..7e263cc 100644
--- a/PORT_STATUS.md
+++ b/PORT_STATUS.md
@@ -1,8 +1,8 @@
 # MiniQuake port status
 
-## Current delivery: BP-075–BP-079
+## Current delivery: BP-075–BP-079R1
 
-The accepted Windows parent is BP-070–BP-074R6. It completed all build, runtime, retail core-asset, audio, render, Protocol-3 and UDP gates and froze:
+The accepted Windows engine parent is BP-070–BP-074R6. R1 fixes the inherited BP-029 world/physics checker so unrelated downstream host-command edits no longer invalidate the frozen physics contract. It completed all build, runtime, retail core-asset, audio, render, Protocol-3 and UDP gates and froze:
 
 ```text
 core_assets_memory_109_frozen_v1
diff --git a/README.md b/README.md
index 53116c7..27c819a 100644
--- a/README.md
+++ b/README.md
@@ -1,13 +1,13 @@
-# MiniQuake – BP-075–BP-079
+# MiniQuake – BP-075–BP-079R1
 
-> Current delivery: WinQuake 1.09 math/chase, view/palette, screen/loading, statusbar/scoreboard and host-command presentation closure on top of the accepted BP-070–BP-074R6 core-assets/memory baseline.
+> Current delivery: BP-075–BP-079R1. The gameplay/presentation code is unchanged; R1 repairs the inherited BP-029 world/physics contract checker for valid downstream `server.ml` changes.
 
 ```text
 gameplay_presentation_109_frozen_v1
 fingerprint=0xad91624c
 ```
 
-Run the Windows acceptance with `TEST_BP-075-079.ps1`; see `docs/BP-075-079_TESTING.md`. Output is streamed live and the package contains no Quake game data.
+Run the Windows acceptance with `TEST_BP-075-079R1.ps1`; see `docs/BP-075-079R1_TESTING.md`. Output is streamed live and the package contains no Quake game data.
 
 ---
 
@@ -24,7 +24,7 @@ MiniQuake verwendet die legal vorhandenen Daten einer installierten Quake-
 Version.
 
 
-## Paketstand: BP-075–BP-079 (Engine-Endstand BP-079)
+## Paketstand: BP-075–BP-079R1 (Engine-Endstand BP-079)
 
 BP-070–BP-074R6 is the accepted Windows parent. The current block closes `mathlib.c`, `chase.c`, `view.c`, `gl_screen.c`, `sbar.c` and the remaining numeric host-command conversion paths.
 
diff --git a/TEST_BP-075-079R1.ps1 b/TEST_BP-075-079R1.ps1
new file mode 100644
index 0000000..3fa419d
--- /dev/null
+++ b/TEST_BP-075-079R1.ps1
@@ -0,0 +1,429 @@
+﻿[CmdletBinding()]
+param(
+  [string]$Compiler = "",
+  [Alias("StdLibPath", "ImportRoot")]
+  [string]$StdLib = "",
+  [string]$Python = "",
+  [string]$QuakeBase = "",
+  [string]$Game = "id1",
+  [string]$Map = "start",
+  [ValidateRange(1, 1000000)]
+  [int]$Frames = 300,
+  [ValidateRange(1, 1000000)]
+  [int]$TraceFrames = 128,
+  [ValidateRange(1, 1000000)]
+  [int]$RenderEvidenceFrame = 128,
+  [ValidateSet("Release", "Debug")]
+  [string]$Configuration = "Release",
+  [switch]$SkipBuild,
+  [switch]$SkipGameValidation,
+  [switch]$SkipTraceValidation,
+  [switch]$SkipRenderEvidence,
+  [switch]$SkipAudioEvidence,
+  [switch]$SkipNetworkEvidence,
+  [switch]$NetworkTests,
+  [switch]$RebuildNative,
+  [switch]$Listings,
+  [switch]$ContinueIndependentTests = $true,
+  [switch]$BisectOnFailure
+)
+
+$ErrorActionPreference = "Stop"
+Set-StrictMode -Version Latest
+
+# Force Python tools and the Python MiniLang compiler to flush every line.
+# The PowerShell wrappers below also forward every received line immediately.
+$env:PYTHONUNBUFFERED = "1"
+
+$PackageId = "BP-079"
+$ParentPackageId = "BP-078"
+$BlockId = "BP-075-079"
+$DeliveryRevision = "BP-075-079R1"
+$DeliveryParent = "BP-075-079"
+$BlockParentPackageId = "BP-070-074R6"
+$Root = $PSScriptRoot
+$Build = Join-Path $Root "build"
+$BuildScript = Join-Path $Root "build.ps1"
+$GameExe = Join-Path $Build "MiniQuake.exe"
+$EvidenceExe = Join-Path $Build "MiniQuakeNetworkPlatformEvidence.exe"
+$CoreAssetEvidenceExe = Join-Path $Build "MiniQuakeCoreAssetRetailEvidence.exe"
+$TraceComparator = Join-Path $Root "tools\compare_traces.py"
+$RenderComparator = Join-Path $Root "tools\compare_render_evidence.py"
+$Timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
+$TranscriptPath = Join-Path $Build ("bp075-079r1-test-{0}.log" -f $Timestamp)
+$SummaryPath = Join-Path $Build "bp075-079r1-test-summary.json"
+$Steps = New-Object System.Collections.Generic.List[object]
+
+function Resolve-Tool([string]$Value, [string]$Label) {
+  if (-not [string]::IsNullOrWhiteSpace($Value) -and (Test-Path -LiteralPath $Value -PathType Leaf)) {
+    return [IO.Path]::GetFullPath($Value)
+  }
+  if (-not [string]::IsNullOrWhiteSpace($Value)) {
+    $Command = Get-Command $Value -ErrorAction SilentlyContinue | Select-Object -First 1
+    if ($null -ne $Command) { return $Command.Path }
+  }
+  throw "$Label not found: $Value"
+}
+
+function Add-Step([string]$Name, [string]$Status, [int]$ExitCode, [string]$Detail) {
+  $Steps.Add([ordered]@{ name = $Name; status = $Status; exit_code = $ExitCode; detail = $Detail })
+  $Color = if ($Status -eq "PASS") { "Green" } elseif ($Status -eq "SKIPPED") { "Yellow" } else { "Red" }
+  Write-Host ("[{0}] {1} - {2}" -f $Status, $Name, $Detail) -ForegroundColor $Color
+}
+
+function Failure-Marker([string]$Text) {
+  if ([string]::IsNullOrWhiteSpace($Text)) { return "" }
+  foreach ($Line in ($Text -split "`r?`n")) {
+    if ($Line -match '^\s*FAIL:\s*' -or $Line -match 'tests failed:\s*[1-9]' -or $Line -match 'evidence:\s*FAIL') { return $Line.Trim() }
+  }
+  return ""
+}
+
+function Invoke-LiveProcess(
+  [string]$Executable,
+  [string[]]$Arguments,
+  [string]$LogPath
+) {
+  $Lines = [System.Collections.Generic.List[string]]::new()
+  $Encoding = [System.Text.UTF8Encoding]::new($false)
+  $Writer = [System.IO.StreamWriter]::new($LogPath, $false, $Encoding)
+  $Code = 1
+  try {
+    & $Executable @Arguments 2>&1 |
+      ForEach-Object {
+        $Line = [string]$_
+        $Lines.Add($Line)
+        $Writer.WriteLine($Line)
+        $Writer.Flush()
+        Write-Host $Line
+      }
+    $Code = [int]$LASTEXITCODE
+  } finally {
+    $Writer.Flush()
+    $Writer.Dispose()
+  }
+  $Text = [string]::Join("`n", $Lines.ToArray())
+  return [pscustomobject]@{
+    exit_code = $Code
+    text = $Text
+  }
+}
+
+function Run-Logged([string]$Name, [string]$Executable, [string[]]$Arguments, [string]$LogName, [string]$RequiredMarker = "") {
+  $LogPath = Join-Path $Build $LogName
+  Write-Host ("[MiniQuake] starting {0}" -f $Name) -ForegroundColor Cyan
+  $Result = Invoke-LiveProcess -Executable $Executable -Arguments $Arguments -LogPath $LogPath
+  $Code = [int]$Result.exit_code
+  $Text = [string]$Result.text
+  $Marker = Failure-Marker $Text
+  if ($Code -eq 0 -and -not [string]::IsNullOrWhiteSpace($Marker)) { $Code = 1 }
+  if ($Code -eq 0 -and -not [string]::IsNullOrWhiteSpace($RequiredMarker) -and $Text -notmatch [regex]::Escape($RequiredMarker)) { $Code = 1; $Marker = "missing marker: $RequiredMarker" }
+  if ($Code -ne 0) { Add-Step $Name "FAIL" $Code ("log=" + $LogName + " " + $Marker); throw "$Name failed with exit code $Code" }
+  Add-Step $Name "PASS" 0 ("log=" + $LogName)
+  return $Text
+}
+
+function Start-BackgroundCapturedProcess(
+  [string]$Executable,
+  [string]$ArgumentsText,
+  [string]$Label
+) {
+  $StartInfo = New-Object System.Diagnostics.ProcessStartInfo
+  $StartInfo.FileName = $Executable
+  $StartInfo.Arguments = $ArgumentsText
+  $StartInfo.WorkingDirectory = $Root
+  $StartInfo.UseShellExecute = $false
+  $StartInfo.CreateNoWindow = $true
+  $StartInfo.RedirectStandardOutput = $true
+  $StartInfo.RedirectStandardError = $true
+
+  $Process = New-Object System.Diagnostics.Process
+  $Process.StartInfo = $StartInfo
+  try {
+    if (-not $Process.Start()) {
+      throw "process start returned false"
+    }
+  } catch {
+    $Process.Dispose()
+    throw ("INFRA_FAILURE: could not start {0}: {1}" -f $Label, $_.Exception.Message)
+  }
+
+  # Drain both redirected streams asynchronously while the server runs. This
+  # avoids a pipe deadlock without buffering any foreground compiler/test job.
+  $StdoutTask = $Process.StandardOutput.ReadToEndAsync()
+  $StderrTask = $Process.StandardError.ReadToEndAsync()
+  return [pscustomobject]@{
+    process = $Process
+    stdout_task = $StdoutTask
+    stderr_task = $StderrTask
+    label = $Label
+  }
+}
+
+function Stop-BackgroundCapturedProcess([pscustomobject]$Handle) {
+  if ($null -eq $Handle -or $null -eq $Handle.process) { return }
+  $Process = [System.Diagnostics.Process]$Handle.process
+  try {
+    if (-not $Process.HasExited) { $Process.Kill() }
+  } catch { }
+  try { $Process.WaitForExit() } catch { }
+}
+
+function Complete-BackgroundCapturedProcess(
+  [pscustomobject]$Handle,
+  [int]$TimeoutMilliseconds,
+  [string]$StdoutPath,
+  [string]$StderrPath
+) {
+  $Process = [System.Diagnostics.Process]$Handle.process
+  $Label = [string]$Handle.label
+  $Exited = $false
+  try {
+    $Exited = $Process.WaitForExit($TimeoutMilliseconds)
+  } catch {
+    Stop-BackgroundCapturedProcess $Handle
+    throw ("INFRA_FAILURE: waiting for {0} failed: {1}" -f $Label, $_.Exception.Message)
+  }
+  if (-not $Exited) {
+    Stop-BackgroundCapturedProcess $Handle
+    throw ("INFRA_FAILURE: {0} timed out after {1} ms" -f $Label, $TimeoutMilliseconds)
+  }
+
+  # The parameterless wait synchronizes redirected-stream completion on
+  # Windows PowerShell 5.1. Refresh before reading ExitCode so the process
+  # object cannot retain the blank/null status observed in R5.
+  $Process.WaitForExit()
+  $Process.Refresh()
+
+  try {
+    $Stdout = [string]$Handle.stdout_task.Result
+    $Stderr = [string]$Handle.stderr_task.Result
+  } catch {
+    $Process.Dispose()
+    throw ("INFRA_FAILURE: collecting {0} output failed: {1}" -f $Label, $_.Exception.Message)
+  }
+
+  $Encoding = New-Object System.Text.UTF8Encoding($false)
+  [System.IO.File]::WriteAllText($StdoutPath, $Stdout, $Encoding)
+  [System.IO.File]::WriteAllText($StderrPath, $Stderr, $Encoding)
+
+  try {
+    $Code = [int]$Process.ExitCode
+  } catch {
+    $Process.Dispose()
+    throw ("INFRA_FAILURE: {0} exit code unavailable after process completion: {1}" -f $Label, $_.Exception.Message)
+  }
+  $Process.Dispose()
+
+  return [pscustomobject]@{
+    exit_code = $Code
+    stdout = $Stdout
+    stderr = $Stderr
+    timed_out = $false
+  }
+}
+
+function Write-NetworkEvidenceOutput([string]$Suffix, [string]$Stdout, [string]$Stderr) {
+  if (-not [string]::IsNullOrWhiteSpace($Stdout)) {
+    Write-Host ("[MiniQuake] network evidence server {0} stdout" -f $Suffix) -ForegroundColor Cyan
+    foreach ($Line in ($Stdout -split "`r?`n")) {
+      if (-not [string]::IsNullOrEmpty($Line)) { Write-Host $Line }
+    }
+  }
+  if (-not [string]::IsNullOrWhiteSpace($Stderr)) {
+    Write-Host ("[MiniQuake] network evidence server {0} stderr" -f $Suffix) -ForegroundColor Yellow
+    foreach ($Line in ($Stderr -split "`r?`n")) {
+      if (-not [string]::IsNullOrEmpty($Line)) { Write-Host $Line -ForegroundColor Yellow }
+    }
+  }
+}
+
+function Run-NetworkEvidencePair([string]$Suffix, [int]$Port) {
+  $ServerOut = Join-Path $Build ("bp075-079r1-network-server-{0}.log" -f $Suffix)
+  $ServerErr = Join-Path $Build ("bp075-079r1-network-server-{0}.err.log" -f $Suffix)
+  $ClientLog = Join-Path $Build ("bp075-079r1-network-client-{0}.log" -f $Suffix)
+  $PairJson = Join-Path $Build ("bp075-079r1-network-pair-{0}.json" -f $Suffix)
+  Remove-Item -Force -ErrorAction SilentlyContinue $ServerOut, $ServerErr, $ClientLog, $PairJson
+
+  Write-Host ("[MiniQuake] starting network evidence server {0} on UDP port {1}" -f $Suffix, $Port) -ForegroundColor Cyan
+  $ServerHandle = Start-BackgroundCapturedProcess -Executable $EvidenceExe -ArgumentsText ("server {0}" -f $Port) -Label ("network evidence server {0}" -f $Suffix)
+  $ClientResult = $null
+  $ServerResult = $null
+  try {
+    Start-Sleep -Milliseconds 300
+    Write-Host ("[MiniQuake] starting network evidence client {0}" -f $Suffix) -ForegroundColor Cyan
+    $ClientResult = Invoke-LiveProcess -Executable $EvidenceExe -Arguments @("client", [string]$Port) -LogPath $ClientLog
+    $ServerResult = Complete-BackgroundCapturedProcess -Handle $ServerHandle -TimeoutMilliseconds 7000 -StdoutPath $ServerOut -StderrPath $ServerErr
+  } catch {
+    Stop-BackgroundCapturedProcess $ServerHandle
+    throw
+  }
+
+  $ClientCode = [int]$ClientResult.exit_code
+  $ClientText = [string]$ClientResult.text
+  $ServerCode = [int]$ServerResult.exit_code
+  $ServerText = [string]$ServerResult.stdout
+  $ServerErrorText = [string]$ServerResult.stderr
+  Write-NetworkEvidenceOutput -Suffix $Suffix -Stdout $ServerText -Stderr $ServerErrorText
+
+  $ServerPassed = $ServerText -match 'network platform evidence server: PASS'
+  $ClientPassed = $ClientText -match 'network platform evidence: PASS'
+  $PairSummary = [ordered]@{
+    schema_version = 1
+    suffix = $Suffix
+    port = $Port
+    server_exit_code = $ServerCode
+    client_exit_code = $ClientCode
+    server_pass_marker = [bool]$ServerPassed
+    client_pass_marker = [bool]$ClientPassed
+    server_stderr_empty = [string]::IsNullOrWhiteSpace($ServerErrorText)
+    status = if ($ServerCode -eq 0 -and $ClientCode -eq 0 -and $ServerPassed -and $ClientPassed) { "PASS" } else { "FAIL" }
+  }
+  $PairSummary | ConvertTo-Json -Depth 4 | Set-Content -LiteralPath $PairJson -Encoding UTF8
+
+  if ($ServerCode -ne 0 -or $ClientCode -ne 0) {
+    throw "network evidence pair $Suffix failed: server=$ServerCode client=$ClientCode"
+  }
+  if (-not $ServerPassed) { throw "network evidence server $Suffix did not report PASS" }
+  if (-not $ClientPassed) { throw "network evidence client $Suffix did not report PASS" }
+  return $ClientLog
+}
+
+New-Item -ItemType Directory -Force -Path $Build | Out-Null
+Start-Transcript -Path $TranscriptPath -Force | Out-Null
+$Failure = ""
+try {
+  Write-Host "MiniQuake BP-075-079R1 acceptance test"
+  Write-Host "  parent: $BlockParentPackageId"
+  Write-Host "  root:   $Root"
+  if ($BisectOnFailure) { Write-Host "  logical bisect markers enabled" }
+
+  if ([string]::IsNullOrWhiteSpace($Python)) { $Python = "python" }
+  $PythonExe = Resolve-Tool $Python "Python"
+
+  if (-not $SkipBuild) {
+    $PowerShellExe = (Get-Process -Id $PID).Path
+    $Arguments = @("-NoProfile", "-ExecutionPolicy", "Bypass", "-File", $BuildScript)
+    if (-not [string]::IsNullOrWhiteSpace($Compiler)) { $Arguments += @("-Compiler", $Compiler) }
+    if (-not [string]::IsNullOrWhiteSpace($StdLib)) { $Arguments += @("-StdLib", $StdLib) }
+    $Arguments += @("-Python", $PythonExe, "-Configuration", $Configuration)
+    if ($NetworkTests) { $Arguments += "-NetworkTests" }
+    if ($RebuildNative) { $Arguments += "-RebuildNative" }
+    if ($Listings) { $Arguments += "-Listings" }
+    [void](Run-Logged "single cumulative build and unit-test suite" $PowerShellExe $Arguments "bp075-079r1-build-child.log" "MiniQuake BP-079 gameplay/presentation closure tests passed: 24")
+  } else {
+    Add-Step "single cumulative build and unit-test suite" "SKIPPED" 0 "-SkipBuild"
+  }
+
+  foreach ($Path in @($GameExe, $EvidenceExe, $CoreAssetEvidenceExe)) { if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) { throw "required executable missing: $Path" } }
+
+  $Version = Run-Logged "package identity" $GameExe @("--version") "bp075-079r1-version.txt" "Gameplay/presentation status: gameplay_presentation_109_frozen_v1"
+  if ($Version -notmatch 'Package: BP-079' -or $Version -notmatch 'Block: BP-075-079') { throw "compiled package identity mismatch" }
+  if ($Version -notmatch 'Core assets/memory status: core_assets_memory_109_frozen_v1' -or $Version -notmatch 'Core assets/memory fingerprint: 0x6c8d974d') { throw "compiled core assets/memory identity mismatch" }
+  if ($Version -notmatch 'Gameplay/presentation status: gameplay_presentation_109_frozen_v1' -or $Version -notmatch 'Gameplay/presentation fingerprint: 0xad91624c') { throw "compiled gameplay/presentation identity mismatch" }
+
+  $CanUseGame = -not $SkipGameValidation -and -not [string]::IsNullOrWhiteSpace($QuakeBase)
+  if ($CanUseGame) {
+    if (-not (Test-Path -LiteralPath (Join-Path $QuakeBase "$Game\pak0.pak") -PathType Leaf)) { throw "Quake data not found: $QuakeBase\$Game\pak0.pak" }
+    [void](Run-Logged "installed Quake data validation" $GameExe @("--validate-game", $QuakeBase, $Map, "-game", $Game) "bp075-079r1-game-validation.log" "Validation result: PASS")
+    [void](Run-Logged "headless runtime validation" $GameExe @("--validate-runtime", $QuakeBase, $Map, [string]$Frames, "-game", $Game) "bp075-079r1-runtime-validation.log")
+  } else {
+    Add-Step "installed Quake data validation" "SKIPPED" 0 "no usable -QuakeBase or skipped"
+    Add-Step "headless runtime validation" "SKIPPED" 0 "no usable -QuakeBase or skipped"
+  }
+
+  if ($CanUseGame) {
+    $CoreA = Join-Path $Build "bp075-079r1-core-assets-a.log"
+    $CoreB = Join-Path $Build "bp075-079r1-core-assets-b.log"
+    [void](Run-Logged "retail core asset evidence A" $CoreAssetEvidenceExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($CoreA)) "result=PASS")
+    [void](Run-Logged "retail core asset evidence B" $CoreAssetEvidenceExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($CoreB)) "result=PASS")
+    $CoreHashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $CoreA).Hash.ToLowerInvariant()
+    $CoreHashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $CoreB).Hash.ToLowerInvariant()
+    if ($CoreHashA -ne $CoreHashB) { throw "retail core asset evidence differs: A=$CoreHashA B=$CoreHashB" }
+    Add-Step "byte-identical retail core asset evidence" "PASS" 0 ("sha256=" + $CoreHashA)
+  } else {
+    Add-Step "retail core asset evidence" "SKIPPED" 0 "no usable -QuakeBase"
+  }
+
+  if (-not $SkipAudioEvidence -and $CanUseGame) {
+    $AudioExe = Join-Path $Build "MiniQuakeAudioRetailEvidence.exe"
+    $A = Join-Path $Build "bp075-079r1-audio-a.log"; $B = Join-Path $Build "bp075-079r1-audio-b.log"
+    $TextA = Run-Logged "retail audio evidence A" $AudioExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($A)) "MiniQuake BP-059 retail audio evidence: PASS"
+    $TextB = Run-Logged "retail audio evidence B" $AudioExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($B)) "MiniQuake BP-059 retail audio evidence: PASS"
+    $HashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $A).Hash.ToLowerInvariant(); $HashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $B).Hash.ToLowerInvariant()
+    if ($HashA -ne $HashB) { throw "retail audio evidence differs" }
+    Add-Step "byte-identical retail audio evidence" "PASS" 0 ("sha256=" + $HashA)
+  } else { Add-Step "retail audio evidence" "SKIPPED" 0 "skipped or no Quake data" }
+
+  if (-not $SkipTraceValidation -and $CanUseGame) {
+    $TraceDir = Join-Path $Build "bp075-079r1-traces"; Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $TraceDir; New-Item -ItemType Directory -Force -Path $TraceDir | Out-Null
+    $PrefixA = Join-Path $TraceDir "run-a"; $PrefixB = Join-Path $TraceDir "run-b"
+    [void](Run-Logged "compatibility trace A" $GameExe @("--compat-trace", $QuakeBase, $Map, [string]$TraceFrames, $PrefixA, "-game", $Game) "bp075-079r1-trace-a.log")
+    [void](Run-Logged "compatibility trace B" $GameExe @("--compat-trace", $QuakeBase, $Map, [string]$TraceFrames, $PrefixB, "-game", $Game) "bp075-079r1-trace-b.log")
+    [void](Run-Logged "byte-identical trace comparison" $PythonExe @($TraceComparator, ("{0}.mqtrace" -f $PrefixA), ("{0}.mqtrace" -f $PrefixB), "--json-output", (Join-Path $Build "bp075-079r1-trace-comparison.json")) "bp075-079r1-trace-comparison.log")
+  } else { Add-Step "deterministic compatibility traces" "SKIPPED" 0 "skipped or no Quake data" }
+
+  if (-not $SkipRenderEvidence -and $CanUseGame) {
+    $EvidenceDir = Join-Path $Build "bp075-079r1-render"; Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $EvidenceDir; New-Item -ItemType Directory -Force -Path $EvidenceDir | Out-Null
+    $EA = Join-Path $EvidenceDir "a"; $EB = Join-Path $EvidenceDir "b"
+    [void](Run-Logged "render evidence A" $GameExe @("--render-evidence", $QuakeBase, $Map, [string]$RenderEvidenceFrame, $EA, "-game", $Game) "bp075-079r1-render-a.log")
+    [void](Run-Logged "render evidence B" $GameExe @("--render-evidence", $QuakeBase, $Map, [string]$RenderEvidenceFrame, $EB, "-game", $Game) "bp075-079r1-render-b.log")
+    [void](Run-Logged "byte-identical render evidence" $PythonExe @($RenderComparator, ("{0}.tga" -f $EA), ("{0}.tga" -f $EB), "--require-exact", "--json-out", (Join-Path $Build "bp075-079r1-render-comparison.json")) "bp075-079r1-render-comparison.log")
+  } else { Add-Step "deterministic framebuffer evidence" "SKIPPED" 0 "skipped or no Quake data" }
+
+  if (-not $SkipNetworkEvidence) {
+    $PortA = Get-Random -Minimum 30000 -Maximum 45000
+    $PortB = Get-Random -Minimum 45001 -Maximum 60000
+    $ClientA = Run-NetworkEvidencePair "a" $PortA
+    $ClientB = Run-NetworkEvidencePair "b" $PortB
+    $HashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $ClientA).Hash.ToLowerInvariant(); $HashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $ClientB).Hash.ToLowerInvariant()
+    if ($HashA -ne $HashB) { throw "network evidence client reports differ: A=$HashA B=$HashB" }
+    Add-Step "two independent UDP control handshakes" "PASS" 0 ("sha256=" + $HashA)
+  } else { Add-Step "two independent UDP control handshakes" "SKIPPED" 0 "-SkipNetworkEvidence" }
+
+  if ($NetworkTests) { [void](Run-Logged "Winsock UDP loopback smoke" $GameExe @("--udp-smoke", "2000") "bp075-079r1-udp-smoke.log" "result=PASS") }
+  else { Add-Step "Winsock UDP loopback smoke" "SKIPPED" 0 "-NetworkTests not supplied" }
+
+  $Failed = @($Steps | Where-Object { $_.status -eq "FAIL" })
+  if ($Failed.Count -gt 0) { throw "independent test groups failed: $($Failed.Count)" }
+  Add-Step "MiniQuake BP-075-079 acceptance" "PASS" 0 "all required gates passed"
+} catch {
+  $Failure = $_.Exception.Message
+  Write-Host ("ERROR: " + $Failure) -ForegroundColor Red
+} finally {
+  $Status = if ([string]::IsNullOrWhiteSpace($Failure)) { "PASS" } else { "FAIL" }
+  $Summary = [ordered]@{
+    schema_version = 1
+    package_id = $PackageId
+    parent_package_id = $ParentPackageId
+    block_id = $BlockId
+    block_parent_package_id = $BlockParentPackageId
+    delivery_revision = $DeliveryRevision
+    delivery_parent = $DeliveryParent
+    network_platform_status = "network_platform_109_frozen_v1"
+    network_platform_fingerprint = "0xb3ec7589"
+    frontend_status = "frontend_109_frozen_v1"
+    frontend_fingerprint = "0x924251fa"
+    core_assets_memory_status = "core_assets_memory_109_frozen_v1"
+    core_assets_memory_fingerprint = "0x6c8d974d"
+    gameplay_presentation_status = "gameplay_presentation_109_frozen_v1"
+    gameplay_presentation_fingerprint = "0xad91624c"
+    status = $Status
+    failure = $Failure
+    created_utc = (Get-Date).ToUniversalTime().ToString("o")
+    quake_base_supplied = -not [string]::IsNullOrWhiteSpace($QuakeBase)
+    steps = $Steps
+  }
+  $Summary | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $SummaryPath -Encoding UTF8
+  Stop-Transcript | Out-Null
+}
+
+if (-not [string]::IsNullOrWhiteSpace($Failure)) {
+  Write-Host "MiniQuake BP-075-079R1 acceptance test: FAIL" -ForegroundColor Red
+  Write-Host "Run .\COLLECT_RESULTS.ps1 and upload the generated ZIP."
+  exit 1
+}
+Write-Host "MiniQuake BP-075-079R1 acceptance test: PASS" -ForegroundColor Green
+Write-Host "Result summary: $SummaryPath"
+Write-Host "For feedback, run .\COLLECT_RESULTS.ps1 and upload the generated ZIP."
+exit 0
diff --git a/build.ps1 b/build.ps1
index 75e3c49..a05c4a3 100755
--- a/build.ps1
+++ b/build.ps1
@@ -637,7 +637,11 @@ if (-not $SkipPreflight -and $null -ne $PythonExe) {
     if (-not (Test-Path -LiteralPath $CheckerPath -PathType Leaf)) {
       throw ($Checker.Name + " checker is missing: " + $CheckerPath)
     }
-    & $PythonExe @PythonPrefixArgs $CheckerPath $Root
+    $CheckerArguments = @($Root)
+    if ($Checker.Path -eq "tools\check_world_physics_closure.py") {
+      $CheckerArguments += "--allow-downstream-package"
+    }
+    & $PythonExe @PythonPrefixArgs $CheckerPath @CheckerArguments
     if ($LASTEXITCODE -ne 0) { throw ($PackageId + " " + $Checker.Name + " preflight failed.") }
   }
 
diff --git a/docs/BP-075-079R1_BLOCK_LEDGER.json b/docs/BP-075-079R1_BLOCK_LEDGER.json
new file mode 100644
index 0000000..e48f9a0
--- /dev/null
+++ b/docs/BP-075-079R1_BLOCK_LEDGER.json
@@ -0,0 +1,211 @@
+{
+  "schema_version": 1,
+  "project": "MiniQuake",
+  "block_id": "BP-075-079",
+  "parent_package_id": "BP-070-074R6",
+  "final_package_id": "BP-079",
+  "delivery_revision": "BP-075-079R1",
+  "core_assets_memory_status": "core_assets_memory_109_frozen_v1",
+  "core_assets_memory_fingerprint": "0x6c8d974d",
+  "steps": [
+    {
+      "package_id": "BP-075",
+      "title": "mathlib and chase-camera parity",
+      "patch": "patches/BP-075.diff",
+      "runtime_fixtures": 22,
+      "status": "candidate"
+    },
+    {
+      "package_id": "BP-076",
+      "title": "view, palette and refdef parity",
+      "patch": "patches/BP-076.diff",
+      "runtime_fixtures": 22,
+      "status": "candidate"
+    },
+    {
+      "package_id": "BP-077",
+      "title": "screen, loading and screenshot parity",
+      "patch": "patches/BP-077.diff",
+      "runtime_fixtures": 22,
+      "status": "candidate"
+    },
+    {
+      "package_id": "BP-078",
+      "title": "statusbar and scoreboard parity",
+      "patch": "patches/BP-078.diff",
+      "runtime_fixtures": 22,
+      "status": "candidate"
+    },
+    {
+      "package_id": "BP-079",
+      "title": "host-command and gameplay/presentation closure",
+      "patch": "patches/BP-079.diff",
+      "runtime_fixtures": 24,
+      "status": "candidate"
+    }
+  ],
+  "block_runtime_fixtures": 112,
+  "retail_evidence_files": 4,
+  "accepted_parent": {
+    "delivery_revision": "BP-070-074R6",
+    "status": "PASS",
+    "result_archive": "MiniQuake_BP-070-074R6_RESULTS_20260730-132228.zip",
+    "result_archive_sha256": "f3f3a15f52932699d744ea6691b6dcb6a4fdca5fe6b77cbc5d38cf114fd283a2",
+    "trace_sha256": "80695b98a91b7d52f91b35dcd220c3cc0962edb6851b0411832746f6f94d3486",
+    "trace_rolling_hash": "d905b042",
+    "retail_core_asset_evidence_sha256": "06eac1012b97d36116a8f605cd428b09bf81f2881c474eda049c5e50183ec20e",
+    "retail_audio_evidence_sha256": "896049a03f26b1db4ddc1550d35d1cacb5f7e0f0c900b0ee36393c8ab8bd0501",
+    "render_tga_sha256": "734e965e17c9a20af7c42184d3d8a3d7f2997e1321342976799ec7f4670af945",
+    "network_client_report_sha256": "50534266c750fa20058c82ce48baca953b0107106dc24fd69c5854fc3364ba96",
+    "udp_loopback": "PASS"
+  },
+  "acceptance": {
+    "single_cumulative_build": true,
+    "installed_game_validation": true,
+    "headless_frames": 300,
+    "trace_frames": 128,
+    "retail_core_asset_evidence": true,
+    "retail_audio_evidence": true,
+    "render_evidence": true,
+    "network_evidence_process_pairs": 2,
+    "udp_loopback": true
+  },
+  "scope_boundary": {
+    "reference": "WinQuake/GLQuake 1.09 gameplay and presentation surface",
+    "game_data": "external; never bundled",
+    "pixel_similarity": "inherited deterministic evidence; original-image corpus remains external"
+  },
+  "delivery_parent": "BP-075-079",
+  "r1_delivery_hotfix": {
+    "revision": "BP-070-074R1",
+    "parent": "BP-070-074",
+    "predecessor_result_sha256": "c1ef8340e2f58904e583f2c85a81a7b3f1d37a7b316f8905297a4f2bbd6bef8c",
+    "failure": "inherited BP-069 checker rejected downstream BP-074 identity",
+    "production_source_changed": false,
+    "native_source_changed": false,
+    "engine_contracts_changed": false,
+    "fixture_count_changed": false,
+    "strict_historical_mode_preserved": true,
+    "downstream_flag": "--allow-downstream-package",
+    "status": "FAIL at BP-070 compile gate; superseded by R2",
+    "result_archive": "MiniQuake_BP-070-074R1_RESULTS_20260729-192646.zip",
+    "result_archive_sha256": "02cb2d978b35076623432cf46a8c56e9907e1430875b07b6ee401eb43387a1c9"
+  },
+  "r2_delivery_hotfix": {
+    "revision": "BP-070-074R2",
+    "parent": "BP-070-074R1",
+    "predecessor_result_sha256": "02cb2d978b35076623432cf46a8c56e9907e1430875b07b6ee401eb43387a1c9",
+    "failure": "BP-070 fixture imported miniquake.byteio as bio but called nonexistent uppercase C-style members",
+    "fixture_calls": "miniquake.common.ShortSwap/LongSwap/ShortNoSwap/LongNoSwap/FloatNoSwap",
+    "production_source_changed": true,
+    "native_source_changed": false,
+    "engine_contracts_changed": false,
+    "fixture_count_changed": false,
+    "status": "FAIL at BP-071 fixture 10; superseded by R3",
+    "result_archive": "MiniQuake_BP-070-074R2_RESULTS_20260729-210721.zip",
+    "result_archive_sha256": "e0206af9dd6775f6e302d697ea3db05ea55e8e902e96f47b3f1e83cf6d0c703b",
+    "windows_build": "76/76 targets compiled",
+    "bp070_runtime": "24/24 PASS"
+  },
+  "r3_delivery_hotfix": {
+    "revision": "BP-070-074R3",
+    "parent": "BP-070-074R2",
+    "predecessor_result_archive": "MiniQuake_BP-070-074R2_RESULTS_20260729-210721.zip",
+    "predecessor_result_sha256": "e0206af9dd6775f6e302d697ea3db05ea55e8e902e96f47b3f1e83cf6d0c703b",
+    "failure": "BP-071 shareware loose-subdirectory fixture left FileSystem.staticRegistered at its startup value true",
+    "fix": "fixture explicitly enters the post-COM_CheckRegistered shareware state before resolving a loose subdirectory path",
+    "production_source_changed": false,
+    "native_source_changed": false,
+    "engine_contracts_changed": false,
+    "fixture_count_changed": false,
+    "status": "Windows test reached result collection; collector failed on synthetic build fixture; superseded by R4"
+  },
+  "r4_delivery_hotfix": {
+    "revision": "BP-070-074R4",
+    "parent": "BP-070-074R3",
+    "failure": "COLLECT_RESULTS.ps1 rejected build\\bp071_fs\\id1\\latin.txt as id1 content",
+    "root_cause": "the BP-071/BP-072 runtime suites intentionally create synthetic id1 trees under build; the collector applied its strict source-data rule to ephemeral test artifacts",
+    "fix": "skip and record synthetic/game-data-shaped build artifacts while preserving strict rejection for source/docs inputs",
+    "production_source_changed": false,
+    "native_source_changed": false,
+    "engine_contracts_changed": false,
+    "fixture_count_changed": false,
+    "result_collection_rerun_required": true,
+    "runtime_retest_required": false,
+    "status": "Windows run reached BP-073; model fixture and collector failed; superseded by R5"
+  },
+  "r5_delivery_hotfix": {
+    "revision": "BP-070-074R5",
+    "parent": "BP-070-074R4",
+    "failure": "BP-073 expected a texture name in the numeric animation metadata table; R4 collector raised a Windows PowerShell 5.1 ArgumentException",
+    "root_cause": "fixture confused BspTexture.name with sequenceTextureAnimations metadata; collector converted a generic List[object] through @($SkippedBuildArtifacts)",
+    "fix": "assert the base texture name separately, bind anim_total=4, and serialize skipped artifacts from a native object array",
+    "production_source_changed": false,
+    "native_source_changed": false,
+    "engine_contracts_changed": false,
+    "fixture_count_changed": false,
+    "runtime_retest_required": true,
+    "collector_retest_required": true,
+    "status": "Windows build 76/76; BP-070/BP-071/BP-072 PASS; failed at BP-073 fixture 6; superseded by R6",
+    "result_archive": "MiniQuake_BP-070-074R5_RESULTS_20260730-121742.zip",
+    "result_archive_sha256": "7cf42d4a9a8aed1ff532d944920c8d30bed9906a8f431366e26a3b08b20d655f",
+    "windows_build": "76/76 targets compiled",
+    "bp070_runtime": "24/24 PASS",
+    "bp071_runtime": "24/24 PASS",
+    "bp072_runtime": "20/20 PASS"
+  },
+  "r6_delivery_hotfix": {
+    "revision": "BP-070-074R6",
+    "parent": "BP-070-074R5",
+    "predecessor_result_sha256": "7cf42d4a9a8aed1ff532d944920c8d30bed9906a8f431366e26a3b08b20d655f",
+    "failure": "BP-073 expected raw BSP submodel bounds after Mod_LoadSubmodels",
+    "fix": "bind original one-unit minimum/maximum spread and corrected registry bounds",
+    "production_source_changed": false,
+    "native_source_changed": false,
+    "engine_contracts_changed": false,
+    "fixture_count_changed": false,
+    "status": "PASS; accepted Windows baseline",
+    "result_archive": "MiniQuake_BP-070-074R6_RESULTS_20260730-132228.zip",
+    "result_archive_sha256": "f3f3a15f52932699d744ea6691b6dcb6a4fdca5fe6b77cbc5d38cf114fd283a2",
+    "windows_targets": "76/76 PASS",
+    "runtime_fixtures": "116/116 PASS",
+    "retail_core_asset_evidence_sha256": "06eac1012b97d36116a8f605cd428b09bf81f2881c474eda049c5e50183ec20e",
+    "trace_sha256": "80695b98a91b7d52f91b35dcd220c3cc0962edb6851b0411832746f6f94d3486",
+    "trace_rolling_hash": "d905b042",
+    "render_ssim": 1.0,
+    "udp_loopback": "PASS"
+  },
+  "gameplay_presentation_status": "gameplay_presentation_109_frozen_v1",
+  "gameplay_presentation_fingerprint": "0xad91624c",
+  "accepted_core_assets_memory_block": {
+    "delivery_revision": "BP-070-074R6",
+    "status": "PASS",
+    "result_archive_sha256": "f3f3a15f52932699d744ea6691b6dcb6a4fdca5fe6b77cbc5d38cf114fd283a2",
+    "runtime_fixtures": 116,
+    "retail_evidence_files": 4,
+    "contract_status": "core_assets_memory_109_frozen_v1",
+    "contract_fingerprint": "0x6c8d974d",
+    "trace_sha256": "80695b98a91b7d52f91b35dcd220c3cc0962edb6851b0411832746f6f94d3486",
+    "trace_rolling_hash": "d905b042"
+  },
+  "bp075079r1_delivery_hotfix": {
+    "revision": "BP-075-079R1",
+    "parent": "BP-075-079",
+    "status": "Windows acceptance pending",
+    "predecessor_result_archive": "MiniQuake_BP-075-079_RESULTS_20260730-143415.zip",
+    "predecessor_result_sha256": "78963a812a05fe9d4951eef1d1cb425757c3a63513c9a956481b36f5033a0384",
+    "failure": "inherited BP-029 checker compared the complete downstream server.ml hash and rejected unrelated host-command changes",
+    "root_cause": "world/physics routines remained byte-identical; the historical whole-file golden was too broad for downstream packages",
+    "fix": "preserve strict historical whole-file mode and add downstream mode with eight exact files, a protected server.ml hash and three accepted server world/physics sections",
+    "server_protected_sha256": "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781",
+    "server_sections": {
+      "runWorldPhysicsWithRetouch": "5b082bf6147659c213fe46e84679d44a73f11dcd2aac070ef51eaf66dd0ecc8b",
+      "runNonClientPhysicsWithRetouch": "24a05fed078529237192653250631835a9d22d6d6c9054abb0ea5121f13095a6",
+      "frameMode": "09549e467d9a73e0802fda2c71552348fe7470dd6a84766098d02c90cbf87921"
+    },
+    "production_source_changed": false,
+    "native_source_changed": false,
+    "contract_fingerprint_changed": false,
+    "fixture_count_changed": false
+  }
+}
diff --git a/docs/BP-075-079R1_HOTFIX_REPORT.json b/docs/BP-075-079R1_HOTFIX_REPORT.json
new file mode 100644
index 0000000..8cdad75
--- /dev/null
+++ b/docs/BP-075-079R1_HOTFIX_REPORT.json
@@ -0,0 +1,24 @@
+{
+  "schema_version": 1,
+  "delivery_revision": "BP-075-079R1",
+  "delivery_parent": "BP-075-079",
+  "engine_package": "BP-079",
+  "classification": "inherited contract checker false positive",
+  "predecessor_result_archive": "MiniQuake_BP-075-079_RESULTS_20260730-143415.zip",
+  "predecessor_result_sha256": "78963a812a05fe9d4951eef1d1cb425757c3a63513c9a956481b36f5033a0384",
+  "failure": "BP-029 whole-file server.ml hash differed after unrelated BP-079 host-command changes",
+  "strict_historical_mode_preserved": true,
+  "downstream_mode": "historical_files_plus_server_sections",
+  "server_section_hashes": {
+    "runWorldPhysicsWithRetouch": "5b082bf6147659c213fe46e84679d44a73f11dcd2aac070ef51eaf66dd0ecc8b",
+    "runNonClientPhysicsWithRetouch": "24a05fed078529237192653250631835a9d22d6d6c9054abb0ea5121f13095a6",
+    "frameMode": "09549e467d9a73e0802fda2c71552348fe7470dd6a84766098d02c90cbf87921"
+  },
+  "production_source_changed": false,
+  "native_source_changed": false,
+  "world_physics_fingerprint_changed": false,
+  "gameplay_presentation_fingerprint_changed": false,
+  "runtime_fixture_count_changed": false,
+  "windows_acceptance": "pending",
+  "server_protected_sha256": "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781"
+}
diff --git a/docs/BP-075-079R1_RESULT_ANALYSIS.md b/docs/BP-075-079R1_RESULT_ANALYSIS.md
new file mode 100644
index 0000000..8b92bf2
--- /dev/null
+++ b/docs/BP-075-079R1_RESULT_ANALYSIS.md
@@ -0,0 +1,45 @@
+# BP-075–BP-079 result analysis
+
+The Windows acceptance stopped in the inherited BP-029 preflight before any MiniLang target was compiled.
+
+```text
+MiniQuake BP-029 world/physics closure verification: FAIL
+ERROR: closure golden differs from authoritative sources
+```
+
+The complete BP-079 package verifier and all BP-075..BP-079 source-derived component checkers had already passed. The mismatch was limited to the historical `authoritative_files` hash for `src/miniquake/server.ml`.
+
+BP-075..BP-079 legitimately changed only host-command parsing in that shared module:
+
+- imported `miniquake.host_command_numbers`;
+- routed `color` through the C-`atoi` compatible adapter;
+- routed numeric kick targets through the Quake-compatible player-index adapter;
+- routed `give` values through the correct integer adapter.
+
+The frozen world/physics functions remained byte-identical to the Windows-accepted BP-025..BP-029R3 baseline:
+
+| Function | Accepted SHA-256 |
+|---|---|
+| `runWorldPhysicsWithRetouch` | `5b082bf6147659c213fe46e84679d44a73f11dcd2aac070ef51eaf66dd0ecc8b` |
+| `runNonClientPhysicsWithRetouch` | `24a05fed078529237192653250631835a9d22d6d6c9054abb0ea5121f13095a6` |
+| `frameMode` | `09549e467d9a73e0802fda2c71552348fe7470dd6a84766098d02c90cbf87921` |
+
+## Classification
+
+Infrastructure/lineage false positive. No gameplay, physics, native bridge or runtime failure was observed.
+
+## R1 correction
+
+The checker now has two modes. The masked downstream server hash is `181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781`:
+
+- **strict historical mode:** unchanged exact BP-029 whole-file hash comparison;
+- **downstream mode:** eight unchanged authoritative files remain fully hashed. `server.ml` is checked twice: by the three accepted world/physics function slices listed above and by the complete source after masking only the added host-command import and the three documented host-command bodies.
+
+`build.ps1` uses downstream mode in BP-079. Package identity is still checked independently by `tools/verify.py`.
+
+Result archive analysed:
+
+```text
+MiniQuake_BP-075-079_RESULTS_20260730-143415.zip
+SHA-256: 78963a812a05fe9d4951eef1d1cb425757c3a63513c9a956481b36f5033a0384
+```
diff --git a/docs/BP-075-079R1_TESTING.md b/docs/BP-075-079R1_TESTING.md
new file mode 100644
index 0000000..b543fd0
--- /dev/null
+++ b/docs/BP-075-079R1_TESTING.md
@@ -0,0 +1,55 @@
+# BP-075–BP-079R1 Windows acceptance
+
+Extract the complete ZIP into a new, empty directory. Do not overlay an older MiniQuake tree.
+
+```powershell
+$QuakeBase = "C:\Users\nilsk\Dropbox\Quake"
+Test-Path "$QuakeBase\id1\pak0.pak"
+```
+
+The path check must return `True`.
+
+```powershell
+.\TEST_BP-075-079R1.ps1 `
+  -Compiler C:\Users\nilsk\Desktop\MiniLang\MiniLangCompilerPy\mlc_win64.py `
+  -StdLib C:\Users\nilsk\Desktop\MiniLang\MiniLangCompilerPy `
+  -QuakeBase $QuakeBase `
+  -Game id1 `
+  -Map start `
+  -Frames 300 `
+  -TraceFrames 128 `
+  -NetworkTests `
+  -ContinueIndependentTests `
+  -BisectOnFailure
+```
+
+The repaired preflight must report:
+
+```text
+MiniQuake BP-029 world/physics closure verification: PASS
+  downstream=true
+  authoritative_mode=historical_files_plus_server_sections
+  package=BP-079 block=BP-075-079
+```
+
+The five new runtime groups must finish with:
+
+```text
+MiniQuake BP-075 math/chase tests passed: 22
+MiniQuake BP-076 view/palette tests passed: 22
+MiniQuake BP-077 screen/loading tests passed: 22
+MiniQuake BP-078 statusbar/scoreboard tests passed: 22
+MiniQuake BP-079 gameplay/presentation closure tests passed: 24
+```
+
+Expected final line:
+
+```text
+MiniQuake BP-075-079R1 acceptance test: PASS
+```
+
+Collect the result afterwards:
+
+```powershell
+.\COLLECT_RESULTS.ps1
+```
diff --git a/test.ps1 b/test.ps1
index d535e0e..7e5ca6e 100755
--- a/test.ps1
+++ b/test.ps1
@@ -4,5 +4,5 @@ param(
   [object[]]$RemainingArguments
 )
 
-& (Join-Path $PSScriptRoot "TEST_BP-075-079.ps1") @RemainingArguments
+& (Join-Path $PSScriptRoot "TEST_BP-075-079R1.ps1") @RemainingArguments
 exit $LASTEXITCODE
diff --git a/tools/check_world_physics_closure.py b/tools/check_world_physics_closure.py
index 1bce0ee..c238495 100755
--- a/tools/check_world_physics_closure.py
+++ b/tools/check_world_physics_closure.py
@@ -1,53 +1,372 @@
 #!/usr/bin/env python3
-"""Verify the cumulative BP-025..BP-029 WinQuake world/physics freeze."""
+"""Verify the frozen BP-025..BP-029 WinQuake world/physics contract.
+
+The historical checker keeps an exact-source mode for the original BP-029
+package. Later black-port packages may explicitly request downstream mode.
+Downstream mode still binds every immutable world/physics source byte-for-byte
+and masks only the narrowly documented host-command changes in server.ml.
+"""
 from __future__ import annotations
-import argparse,hashlib,json,subprocess,sys
+
+import argparse
+import hashlib
+import json
+import re
+import subprocess
+import sys
 from pathlib import Path
-PACKAGE='BP-029';PARENT='BP-028';STATUS='world_physics_109_frozen_v1';FINGERPRINT=0x2235d77c;GOLDEN='audit/world_physics_closure_golden.json'
-CHECKERS=['tools/check_world_hull.py','tools/check_world_link.py','tools/check_world_trace.py','tools/check_server_move.py','tools/check_server_physics.py','tools/check_sv_user_movement.py','tools/check_server_user.py']
-CANONICAL='world_physics_109_frozen_v1|hull_nodes=6|link_expand=1|item_expand=15|move_step=18|clip_planes=5|fly_bumps=4|stop_epsilon=0.1|client_maxspeed=320|air_cap=30|idealpitch_forward=6|production_dispatch=shared_nonclient|force_retouch=ordered|protocol=protocol15_frozen_v1|quakec=quakec_109_frozen_v1'
-def fnv(text):
- h=2166136261
- for b in text.encode('utf-8'):h=((h^b)*16777619)&0xffffffff
- return h
-def sha(p):return hashlib.sha256(Path(p).read_bytes()).hexdigest()
-def doc(root):
- files=['src/miniquake/world_hull.ml','src/miniquake/world.ml','src/miniquake/world_bsp.ml','src/miniquake/server_collision.ml','src/miniquake/server_move.ml','src/miniquake/physics.ml','src/miniquake/sv_user.ml','src/miniquake/server.ml','src/miniquake/world_physics_contract.ml']
- return {'schema':'MiniQuakeWorldPhysicsClosureGolden/1','package_id':PACKAGE,'parent_package_id':PARENT,'status':STATUS,'fingerprint':FINGERPRINT,'canonical':CANONICAL,'components':['BP-025','BP-026','BP-027','BP-028','BP-029'],'authoritative_files':{f:sha(root/f) for f in files},'runtime_fixtures':20}
-def contract(root):
- e=[];module=(root/'src/miniquake/world_physics_contract.ml').read_text(encoding='utf-8-sig');test=(root/'tests/world_physics_closure_tests.ml').read_text(encoding='utf-8-sig')
- if fnv(CANONICAL)!=FINGERPRINT:e.append('Python contract fingerprint changed')
- for m in (f'const STATUS = "{STATUS}"',f'const CONTRACT_FINGERPRINT = 0x{FINGERPRINT:08x}',CANONICAL):
-  if m not in module:e.append('missing closure marker: '+m[:80])
- if test.count('if run(')!=20 or 'world/physics closure tests passed: 20' not in test:e.append('expected 20 closure fixtures')
- physics=(root/'src/miniquake/physics.ml').read_text(encoding='utf-8-sig');server=(root/'src/miniquake/server.ml').read_text(encoding='utf-8-sig')
- for marker in ('function SV_Physics_NonClientEntity','function SV_ForceRetouchValue','function SV_ForceRetouchEntity','function SV_FinishForceRetouch'):
-  if marker not in physics:e.append('missing production physics marker: '+marker)
- frame=server[server.index('function frameMode'):server.index('function frame(',server.index('function frameMode'))]
- world=server[server.index('function runWorldPhysicsWithRetouch'):server.index('function runNonClientPhysicsWithRetouch',server.index('function runWorldPhysicsWithRetouch'))]
- nonclients=server[server.index('function runNonClientPhysicsWithRetouch'):server.index('function runNonClientPhysics(',server.index('function runNonClientPhysicsWithRetouch'))]
- if 'physics.SV_Physics_NonClientEntity' not in world:e.append('production world loop does not use shared physics dispatch')
- if 'physics.SV_Physics_NonClientEntity' not in nonclients:e.append('production non-client loop does not use shared physics dispatch')
- if 'moveQcEntity(server' in nonclients:e.append('production non-client loop still uses simplified moveQcEntity')
- if frame.index('runWorldPhysicsWithRetouch') > frame.index('physics.SV_ForceRetouchEntity(server, clientValue.edictIndex, forceRetouch)'):e.append('world edict is not processed before client physics slots')
- if 'physics.SV_ForceRetouchEntity(server, clientValue.edictIndex, forceRetouch)' not in frame:e.append('client force_retouch ordering missing')
- if 'physics.SV_FinishForceRetouch(server, forceRetouch)' not in frame:e.append('force_retouch decrement missing from integrated frame')
- return e
-def main():
- ap=argparse.ArgumentParser();ap.add_argument('root',nargs='?',default='.');ap.add_argument('--root',dest='rf');ap.add_argument('--write-golden',action='store_true');ap.add_argument('--json-output');a=ap.parse_args();root=Path(a.rf or a.root).resolve();d=doc(root);g=root/GOLDEN
- if a.write_golden:g.parent.mkdir(parents=True,exist_ok=True);g.write_text(json.dumps(d,indent=2)+'\n',encoding='utf-8')
- errors=[]
- if not g.is_file():errors.append('missing closure golden')
- elif json.loads(g.read_text(encoding='utf-8-sig'))!=d:errors.append('closure golden differs from authoritative sources')
- component=[]
- for rel in CHECKERS:
-  r=subprocess.run([sys.executable,str(root/rel),str(root)],capture_output=True,text=True)
-  component.append({'checker':rel,'exit_code':r.returncode})
-  if r.returncode:errors.append(rel+' failed: '+(r.stdout+r.stderr).strip())
- errors+=contract(root)
- report={'schema':'MiniQuakeBP029WorldPhysicsClosureVerification/1','package_id':PACKAGE,'parent_package_id':PARENT,'status':STATUS,'fingerprint':f'0x{FINGERPRINT:08x}','ok':not errors,'components':component,'runtime_fixtures':20,'errors':errors}
- if a.json_output:Path(a.json_output).write_text(json.dumps(report,indent=2)+'\n',encoding='utf-8')
- print('MiniQuake BP-029 world/physics closure verification: '+('PASS' if not errors else 'FAIL'));print(f'  components={len(CHECKERS)} runtime_fixtures=20 fingerprint=0x{FINGERPRINT:08x}')
- for x in errors:print('  ERROR: '+x)
- return 0 if not errors else 1
-if __name__=='__main__':raise SystemExit(main())
+
+PACKAGE = "BP-029"
+PARENT = "BP-028"
+STATUS = "world_physics_109_frozen_v1"
+FINGERPRINT = 0x2235D77C
+GOLDEN = "audit/world_physics_closure_golden.json"
+CHECKERS = [
+    "tools/check_world_hull.py",
+    "tools/check_world_link.py",
+    "tools/check_world_trace.py",
+    "tools/check_server_move.py",
+    "tools/check_server_physics.py",
+    "tools/check_sv_user_movement.py",
+    "tools/check_server_user.py",
+]
+AUTHORITATIVE_FILES = [
+    "src/miniquake/world_hull.ml",
+    "src/miniquake/world.ml",
+    "src/miniquake/world_bsp.ml",
+    "src/miniquake/server_collision.ml",
+    "src/miniquake/server_move.ml",
+    "src/miniquake/physics.ml",
+    "src/miniquake/sv_user.ml",
+    "src/miniquake/server.ml",
+    "src/miniquake/world_physics_contract.ml",
+]
+MUTABLE_DOWNSTREAM_FILE = "src/miniquake/server.ml"
+DOWNSTREAM_SERVER_ALLOWED_IMPORT = "import miniquake.host_command_numbers as hostNumbers"
+DOWNSTREAM_SERVER_ALLOWED_FUNCTIONS = (
+    "Host_Color_f",
+    "Host_Kick_f",
+    "Host_Give_f",
+)
+# SHA-256 of the accepted BP-070..074R6 server.ml after masking only the
+# BP-079 host-command import and the three host-command functions above.
+DOWNSTREAM_SERVER_PROTECTED_SHA256 = (
+    "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781"
+)
+SERVER_SECTION_HASHES = {
+    "runWorldPhysicsWithRetouch": "5b082bf6147659c213fe46e84679d44a73f11dcd2aac070ef51eaf66dd0ecc8b",
+    "runNonClientPhysicsWithRetouch": "24a05fed078529237192653250631835a9d22d6d6c9054abb0ea5121f13095a6",
+    "frameMode": "09549e467d9a73e0802fda2c71552348fe7470dd6a84766098d02c90cbf87921",
+}
+DOWNSTREAM_SERVER_SECTION_SHA256 = SERVER_SECTION_HASHES
+DOWNSTREAM_MODE = "historical_files_plus_server_sections"
+CANONICAL = (
+    "world_physics_109_frozen_v1|hull_nodes=6|link_expand=1|item_expand=15|"
+    "move_step=18|clip_planes=5|fly_bumps=4|stop_epsilon=0.1|"
+    "client_maxspeed=320|air_cap=30|idealpitch_forward=6|"
+    "production_dispatch=shared_nonclient|force_retouch=ordered|"
+    "protocol=protocol15_frozen_v1|quakec=quakec_109_frozen_v1"
+)
+
+
+def fnv(text: str) -> int:
+    value = 2166136261
+    for byte in text.encode("utf-8"):
+        value = ((value ^ byte) * 16777619) & 0xFFFFFFFF
+    return value
+
+
+def sha(path: Path) -> str:
+    return hashlib.sha256(path.read_bytes()).hexdigest()
+
+
+def _read(path: Path) -> str:
+    return path.read_text(encoding="utf-8-sig").replace("\r\n", "\n")
+
+
+def _const_string(source: str, name: str) -> str:
+    match = re.search(rf'^const\s+{re.escape(name)}\s*=\s*"([^"]*)"\s*$', source, re.M)
+    return match.group(1) if match else ""
+
+
+def _mask_downstream_server(source: str) -> tuple[str, list[str]]:
+    """Remove only the documented BP-079 host-command deltas.
+
+    Everything else in server.ml remains part of the frozen world/physics
+    evidence. Missing or duplicate masks are treated as verification errors.
+    """
+    errors: list[str] = []
+    normalized = source.replace("\r\n", "\n")
+    import_pattern = re.compile(
+        rf"^{re.escape(DOWNSTREAM_SERVER_ALLOWED_IMPORT)}\n", re.M
+    )
+    import_matches = len(import_pattern.findall(normalized))
+    if import_matches > 1:
+        errors.append("downstream server contains duplicate host-command import")
+    normalized = import_pattern.sub("", normalized)
+
+    for name in DOWNSTREAM_SERVER_ALLOWED_FUNCTIONS:
+        pattern = re.compile(
+            rf"(?ms)^function\s+{re.escape(name)}\([^\n]*\)\n.*?^end function\n?"
+        )
+        matches = list(pattern.finditer(normalized))
+        if len(matches) != 1:
+            errors.append(
+                f"downstream server expected exactly one {name} function, got {len(matches)}"
+            )
+            continue
+        normalized = pattern.sub(
+            f"function {name}(<downstream-host-command-body>)\nend function\n",
+            normalized,
+            count=1,
+        )
+    return normalized, errors
+
+
+def _protected_server_hash(root: Path) -> tuple[str, list[str]]:
+    normalized, errors = _mask_downstream_server(_read(root / MUTABLE_DOWNSTREAM_FILE))
+    return hashlib.sha256(normalized.encode("utf-8")).hexdigest(), errors
+
+
+def _function_hashes(source: str) -> tuple[dict[str, str], list[str]]:
+    values: dict[str, str] = {}
+    errors: list[str] = []
+    for name in SERVER_SECTION_HASHES:
+        pattern = re.compile(
+            rf"(?ms)^function\s+{re.escape(name)}\([^\n]*\)\n.*?^end function\n?"
+        )
+        matches = list(pattern.finditer(source))
+        if len(matches) != 1:
+            errors.append(f"expected exactly one protected server function {name}, got {len(matches)}")
+            continue
+        values[name] = hashlib.sha256(matches[0].group(0).encode("utf-8")).hexdigest()
+    return values, errors
+
+
+def doc(root: Path) -> dict[str, object]:
+    return {
+        "schema": "MiniQuakeWorldPhysicsClosureGolden/1",
+        "package_id": PACKAGE,
+        "parent_package_id": PARENT,
+        "status": STATUS,
+        "fingerprint": FINGERPRINT,
+        "canonical": CANONICAL,
+        "components": ["BP-025", "BP-026", "BP-027", "BP-028", "BP-029"],
+        "authoritative_files": {name: sha(root / name) for name in AUTHORITATIVE_FILES},
+        "runtime_fixtures": 20,
+    }
+
+
+def _verify_downstream_golden(root: Path, golden: dict[str, object]) -> tuple[list[str], dict[str, object]]:
+    errors: list[str] = []
+    expected_metadata = {
+        "schema": "MiniQuakeWorldPhysicsClosureGolden/1",
+        "package_id": PACKAGE,
+        "parent_package_id": PARENT,
+        "status": STATUS,
+        "fingerprint": FINGERPRINT,
+        "canonical": CANONICAL,
+        "components": ["BP-025", "BP-026", "BP-027", "BP-028", "BP-029"],
+        "runtime_fixtures": 20,
+    }
+    for key, expected in expected_metadata.items():
+        if golden.get(key) != expected:
+            errors.append(f"downstream world/physics golden metadata differs: {key}")
+
+    recorded = golden.get("authoritative_files")
+    if not isinstance(recorded, dict):
+        errors.append("downstream world/physics golden has no authoritative_files object")
+        recorded = {}
+
+    exact_files = 0
+    for name in AUTHORITATIVE_FILES:
+        if name == MUTABLE_DOWNSTREAM_FILE:
+            continue
+        expected_hash = recorded.get(name)
+        actual_hash = sha(root / name)
+        if expected_hash != actual_hash:
+            errors.append(f"frozen world/physics source changed: {name}")
+        else:
+            exact_files += 1
+
+    server_source = _read(root / MUTABLE_DOWNSTREAM_FILE)
+    server_hash, server_errors = _protected_server_hash(root)
+    errors.extend(server_errors)
+    section_hashes, section_errors = _function_hashes(server_source)
+    errors.extend(section_errors)
+    for name, expected_hash in SERVER_SECTION_HASHES.items():
+        if section_hashes.get(name) != expected_hash:
+            errors.append(
+                f"frozen world/physics server function changed: {name}: "
+                f"expected {expected_hash}, got {section_hashes.get(name, '')}"
+            )
+    if server_hash != DOWNSTREAM_SERVER_PROTECTED_SHA256:
+        errors.append(
+            "server.ml changed outside the documented BP-079 host-command regions: "
+            f"expected {DOWNSTREAM_SERVER_PROTECTED_SHA256}, got {server_hash}"
+        )
+
+    build = _read(root / "src/miniquake/build_info.ml")
+    if f'const WORLD_PHYSICS_STATUS = "{STATUS}"' not in build:
+        errors.append("downstream package no longer binds frozen world/physics status")
+    package_id = _const_string(build, "PACKAGE_ID")
+    parent_id = _const_string(build, "PARENT_PACKAGE_ID")
+    block_id = _const_string(build, "BLOCK_ID")
+    if not package_id or not parent_id or not block_id:
+        errors.append("downstream build identity is incomplete")
+
+    details = {
+        "exact_authoritative_files": exact_files,
+        "mutable_authoritative_file": MUTABLE_DOWNSTREAM_FILE,
+        "allowed_server_functions": list(DOWNSTREAM_SERVER_ALLOWED_FUNCTIONS),
+        "server_protected_sha256": server_hash,
+        "server_section_sha256": section_hashes,
+        "build_package_id": package_id,
+        "build_parent_package_id": parent_id,
+        "build_block_id": block_id,
+    }
+    return errors, details
+
+
+def contract(root: Path) -> list[str]:
+    errors: list[str] = []
+    module = _read(root / "src/miniquake/world_physics_contract.ml")
+    test = _read(root / "tests/world_physics_closure_tests.ml")
+    if fnv(CANONICAL) != FINGERPRINT:
+        errors.append("Python contract fingerprint changed")
+    for marker in (
+        f'const STATUS = "{STATUS}"',
+        f"const CONTRACT_FINGERPRINT = 0x{FINGERPRINT:08x}",
+        CANONICAL,
+    ):
+        if marker not in module:
+            errors.append("missing closure marker: " + marker[:80])
+    if test.count("if run(") != 20 or "world/physics closure tests passed: 20" not in test:
+        errors.append("expected 20 closure fixtures")
+
+    physics = _read(root / "src/miniquake/physics.ml")
+    server = _read(root / "src/miniquake/server.ml")
+    for marker in (
+        "function SV_Physics_NonClientEntity",
+        "function SV_ForceRetouchValue",
+        "function SV_ForceRetouchEntity",
+        "function SV_FinishForceRetouch",
+    ):
+        if marker not in physics:
+            errors.append("missing production physics marker: " + marker)
+
+    frame_start = server.index("function frameMode")
+    frame_end = server.index("function frame(", frame_start)
+    frame = server[frame_start:frame_end]
+    world_start = server.index("function runWorldPhysicsWithRetouch")
+    world_end = server.index("function runNonClientPhysicsWithRetouch", world_start)
+    world = server[world_start:world_end]
+    nonclient_start = world_end
+    nonclient_end = server.index("function runNonClientPhysics(", nonclient_start)
+    nonclients = server[nonclient_start:nonclient_end]
+
+    if "physics.SV_Physics_NonClientEntity" not in world:
+        errors.append("production world loop does not use shared physics dispatch")
+    if "physics.SV_Physics_NonClientEntity" not in nonclients:
+        errors.append("production non-client loop does not use shared physics dispatch")
+    if "moveQcEntity(server" in nonclients:
+        errors.append("production non-client loop still uses simplified moveQcEntity")
+    client_marker = "physics.SV_ForceRetouchEntity(server, clientValue.edictIndex, forceRetouch)"
+    if frame.index("runWorldPhysicsWithRetouch") > frame.index(client_marker):
+        errors.append("world edict is not processed before client physics slots")
+    if client_marker not in frame:
+        errors.append("client force_retouch ordering missing")
+    if "physics.SV_FinishForceRetouch(server, forceRetouch)" not in frame:
+        errors.append("force_retouch decrement missing from integrated frame")
+    return errors
+
+
+def main() -> int:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("root", nargs="?", default=".")
+    parser.add_argument("--root", dest="root_flag")
+    parser.add_argument("--write-golden", action="store_true")
+    parser.add_argument("--json-output")
+    parser.add_argument(
+        "--allow-downstream-package",
+        action="store_true",
+        help="verify the frozen BP-029 contract inside a later package identity",
+    )
+    args = parser.parse_args()
+    root = Path(args.root_flag or args.root).resolve()
+    current = doc(root)
+    golden_path = root / GOLDEN
+    if args.write_golden:
+        golden_path.parent.mkdir(parents=True, exist_ok=True)
+        golden_path.write_text(json.dumps(current, indent=2) + "\n", encoding="utf-8")
+
+    errors: list[str] = []
+    downstream_details: dict[str, object] = {}
+    if not golden_path.is_file():
+        errors.append("missing closure golden")
+    else:
+        golden = json.loads(golden_path.read_text(encoding="utf-8-sig"))
+        if args.allow_downstream_package:
+            downstream_errors, downstream_details = _verify_downstream_golden(root, golden)
+            errors.extend(downstream_errors)
+        elif golden != current:
+            errors.append("closure golden differs from authoritative sources")
+
+    component: list[dict[str, object]] = []
+    for relative in CHECKERS:
+        completed = subprocess.run(
+            [sys.executable, str(root / relative), str(root)],
+            capture_output=True,
+            text=True,
+            check=False,
+        )
+        component.append({"checker": relative, "exit_code": completed.returncode})
+        if completed.returncode:
+            errors.append(relative + " failed: " + (completed.stdout + completed.stderr).strip())
+    errors.extend(contract(root))
+
+    report = {
+        "schema": "MiniQuakeBP029WorldPhysicsClosureVerification/2",
+        "package_id": PACKAGE,
+        "parent_package_id": PARENT,
+        "status": STATUS,
+        "fingerprint": f"0x{FINGERPRINT:08x}",
+        "ok": not errors,
+        "components": component,
+        "runtime_fixtures": 20,
+        "downstream_package": bool(args.allow_downstream_package),
+        "downstream_mode": DOWNSTREAM_MODE if args.allow_downstream_package else "strict_historical_whole_file",
+        "downstream": downstream_details,
+        "details": {
+            "server_section_hashes": downstream_details.get("server_section_sha256", {}),
+            "server_protected_sha256": downstream_details.get("server_protected_sha256", ""),
+            "authoritative_mode": DOWNSTREAM_MODE if args.allow_downstream_package else "strict_historical_whole_file",
+        },
+        "errors": errors,
+    }
+    if args.json_output:
+        Path(args.json_output).write_text(json.dumps(report, indent=2) + "\n", encoding="utf-8")
+
+    print("MiniQuake BP-029 world/physics closure verification: " + ("PASS" if not errors else "FAIL"))
+    print(
+        f"  components={len(CHECKERS)} runtime_fixtures=20 "
+        f"fingerprint=0x{FINGERPRINT:08x} "
+        f"downstream={str(bool(args.allow_downstream_package)).lower()}"
+    )
+    if args.allow_downstream_package and downstream_details:
+        print(
+            "  authoritative_mode=" + DOWNSTREAM_MODE
+            + " protected_server_sha256="
+            + str(downstream_details.get("server_protected_sha256", ""))
+            + " package="
+            + str(downstream_details.get("build_package_id", ""))
+            + " block="
+            + str(downstream_details.get("build_block_id", ""))
+        )
+    for error in errors:
+        print("  ERROR: " + error)
+    return 0 if not errors else 1
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/tools/verify.py b/tools/verify.py
index 0f51ccc..480034f 100755
--- a/tools/verify.py
+++ b/tools/verify.py
@@ -1,5 +1,5 @@
 #!/usr/bin/env python3
-"""Self-contained MiniQuake BP-079 / BP-075-079 gameplay-presentation verifier.
+"""Self-contained MiniQuake BP-079 / BP-075-079R1 gameplay-presentation verifier.
 
 The verifier uses only the Python standard library. It preserves every accepted
 parent contract and validates math/chase, view/palette, screen/loading,
@@ -24,8 +24,8 @@ PACKAGE_ID = "BP-079"
 PARENT_PACKAGE_ID = "BP-078"
 BLOCK_ID = "BP-075-079"
 BLOCK_PARENT_PACKAGE_ID = "BP-070-074R6"
-DELIVERY_REVISION = "BP-075-079"
-DELIVERY_PARENT = "BP-070-074R6"
+DELIVERY_REVISION = "BP-075-079R1"
+DELIVERY_PARENT = "BP-075-079"
 PROTOCOL_STATUS = "protocol15_frozen_v1"
 QUAKEC_STATUS = "quakec_109_frozen_v1"
 WORLD_PHYSICS_STATUS = "world_physics_109_frozen_v1"
@@ -680,6 +680,10 @@ REQUIRED_PATHS.update({
     "audit/gameplay_presentation_golden.json",
     "patches/BP-075.diff", "patches/BP-076.diff", "patches/BP-077.diff",
     "patches/BP-078.diff", "patches/BP-079.diff",
+    "TEST_BP-075-079R1.ps1", "CHANGELOG_BP-075-079R1.md",
+    "docs/BP-075-079R1_TESTING.md", "docs/BP-075-079R1_RESULT_ANALYSIS.md",
+    "docs/BP-075-079R1_HOTFIX_REPORT.json", "docs/BP-075-079R1_BLOCK_LEDGER.json",
+    "patches/BP-079R1.diff",
 })
 
 @dataclass
@@ -5111,28 +5115,31 @@ def check_current_delivery_scripts(root: Path) -> CheckResult:
             "MiniQuakeGameplayMathChaseTests.exe", "MiniQuakeGameplayViewTests.exe",
             "MiniQuakeGameplayScreenTests.exe", "MiniQuakeGameplayStatusbarTests.exe",
             "MiniQuakeGameplayPresentationClosureTests.exe",
-            "check_gameplay_075.py", "check_gameplay_079.py", "--allow-downstream-package",
+            "check_gameplay_075.py", "check_gameplay_079.py",
+            'if ($Checker.Path -eq "tools\\check_world_physics_closure.py")',
+            '$CheckerArguments += "--allow-downstream-package"',
         ],
-        "TEST_BP-075-079.ps1": [
+        "TEST_BP-075-079R1.ps1": [
             '$PackageId = "BP-079"', '$ParentPackageId = "BP-078"',
-            '$BlockId = "BP-075-079"', '$DeliveryRevision = "BP-075-079"',
-            '$BlockParentPackageId = "BP-070-074R6"',
+            '$BlockId = "BP-075-079"', '$DeliveryRevision = "BP-075-079R1"',
+            '$DeliveryParent = "BP-075-079"', '$BlockParentPackageId = "BP-070-074R6"',
             '$env:PYTHONUNBUFFERED = "1"', "Invoke-LiveProcess", "$Writer.Flush()",
             "MiniQuakeCoreAssetRetailEvidence.exe", "retail core asset evidence A",
             "Gameplay/presentation status: gameplay_presentation_109_frozen_v1",
-            "MiniQuake BP-075-079 acceptance test: PASS",
+            "MiniQuake BP-075-079R1 acceptance test: PASS",
         ],
         "COLLECT_RESULTS.ps1": [
-            'MiniQuake_BP-075-079_RESULTS_', '$PackageId = "BP-079"',
-            '$BlockId = "BP-075-079"', '$DeliveryRevision = "BP-075-079"',
-            '$DeliveryParent = "BP-070-074R6"', '$SkippedBuildArtifacts = @()',
+            'MiniQuake_BP-075-079R1_RESULTS_', '$PackageId = "BP-079"',
+            '$BlockId = "BP-075-079"', '$DeliveryRevision = "BP-075-079R1"',
+            '$DeliveryParent = "BP-075-079"', '$SkippedBuildArtifacts = @()',
             'skipped_build_artifacts = $SkippedBuildArtifacts',
             "MiniQuakeProtocol15ServerDataTests.exe", "BP-012_PROTOCOL15_SERVERDATA_AUDIT.md",
             "protocol15_serverdata_golden.json", "MiniQuakeGameplayPresentationClosureTests.exe",
-            "patches\\BP-075.diff", "patches\\BP-079.diff",
+            "patches\\BP-075.diff", "patches\\BP-079.diff", "patches\\BP-079R1.diff",
             "gameplay_presentation_golden.json", "BP-070-074R6_ACCEPTANCE_ANALYSIS.md",
+            "BP-075-079R1_RESULT_ANALYSIS.md",
         ],
-        "test.ps1": ["TEST_BP-075-079.ps1"],
+        "test.ps1": ["TEST_BP-075-079R1.ps1"],
     }
     for relative, markers in expected.items():
         try:
@@ -5143,9 +5150,14 @@ def check_current_delivery_scripts(root: Path) -> CheckResult:
         for item in markers:
             if item not in source:
                 errors.append(f"{relative} missing marker: {item}")
-    details.update({"scripts_checked": len(expected), "delivery_revision": DELIVERY_REVISION,
-                    "delivery_parent": DELIVERY_PARENT, "live_output": True,
-                    "gameplay_presentation_test_binaries": 5})
+    details.update({
+        "scripts_checked": len(expected),
+        "delivery_revision": DELIVERY_REVISION,
+        "delivery_parent": DELIVERY_PARENT,
+        "live_output": True,
+        "gameplay_presentation_test_binaries": 5,
+        "world_physics_downstream_mode": True,
+    })
     return CheckResult("delivery_scripts", not errors, details, errors)
 
 def check_live_output_contract(root: Path) -> CheckResult:
@@ -5153,13 +5165,13 @@ def check_live_output_contract(root: Path) -> CheckResult:
     details: dict[str, object] = {}
     try:
         build = (root / "build.ps1").read_text(encoding="utf-8-sig")
-        test = (root / "TEST_BP-075-079.ps1").read_text(encoding="utf-8-sig")
+        test = (root / "TEST_BP-075-079R1.ps1").read_text(encoding="utf-8-sig")
         for source_name, source, markers in (
             ("build.ps1", build, (
                 '$env:PYTHONUNBUFFERED = "1"', "function Invoke-LiveCapturedProcess",
                 "$Writer.Flush()", "Write-Host $Line",
             )),
-            ("TEST_BP-075-079.ps1", test, (
+            ("TEST_BP-075-079R1.ps1", test, (
                 '$env:PYTHONUNBUFFERED = "1"', "function Invoke-LiveProcess",
                 "$Writer.Flush()", "Write-Host $Line", "[MiniQuake] starting",
             )),
@@ -5170,8 +5182,12 @@ def check_live_output_contract(root: Path) -> CheckResult:
         forbidden = re.compile(r"\$Lines\s*=\s*@\(\s*&\s*\$Executable.*?2>&1\s*\)", re.S)
         if forbidden.search(test) or forbidden.search(build):
             errors.append("delivery scripts reintroduce whole-process output buffering")
-        details.update({"build_live_capture": True, "acceptance_live_capture": True,
-                        "python_unbuffered": True, "line_flush": True})
+        details.update({
+            "build_live_capture": True,
+            "acceptance_live_capture": True,
+            "python_unbuffered": True,
+            "line_flush": True,
+        })
     except OSError as exc:
         errors.append(f"cannot validate live-output contract: {exc}")
     return CheckResult("live_output_contract", not errors, details, errors)
@@ -5890,7 +5906,7 @@ def check_bp070074r2_common_endian_contract(root: Path) -> CheckResult:
         port_record = port.get("bp070074r2_hotfix", {})
         if port_record.get("revision") != "BP-070-074R2" or port_record.get("parent") != "BP-070-074R1":
             errors.append("PORT_LEDGER R2 lineage differs")
-        if port.get("current_delivery_revision") not in ("BP-070-074R2", "BP-070-074R3", "BP-070-074R4", "BP-070-074R5", "BP-070-074R6", "BP-075-079"):
+        if port.get("current_delivery_revision") not in ("BP-070-074R2", "BP-070-074R3", "BP-070-074R4", "BP-070-074R5", "BP-070-074R6", "BP-075-079", "BP-075-079R1"):
             errors.append("PORT_LEDGER current delivery does not preserve the R2 lineage")
 
         for marker in (result_hash, "Undefined variable 'bio'", "LongNoSwap", "FloatNoSwap"):
@@ -5969,7 +5985,7 @@ def check_bp070074r3_shareware_fixture_contract(root: Path) -> CheckResult:
         port_record = port.get("bp070074r3_hotfix", {})
         if port_record.get("revision") != "BP-070-074R3" or port_record.get("parent") != "BP-070-074R2":
             errors.append("PORT_LEDGER R3 lineage differs")
-        if port.get("current_delivery_revision") not in ("BP-070-074R3", "BP-070-074R4", "BP-070-074R5", "BP-070-074R6", "BP-075-079"):
+        if port.get("current_delivery_revision") not in ("BP-070-074R3", "BP-070-074R4", "BP-070-074R5", "BP-070-074R6", "BP-075-079", "BP-075-079R1"):
             errors.append("PORT_LEDGER current delivery does not preserve the R3 lineage")
         for marker in (result_hash, "shareware loose restriction", "staticRegistered", "COM_CheckRegistered"):
             if marker not in analysis:
@@ -6010,7 +6026,7 @@ def check_bp070074r4_collector_synthetic_workspace_contract(root: Path) -> Check
             errors.append("BLOCK_LEDGER R4 source-change classification differs")
         if port.get("bp070074r4_hotfix", {}).get("parent") != "BP-070-074R3":
             errors.append("PORT_LEDGER R4 lineage differs")
-        if port.get("current_delivery_revision") not in ("BP-070-074R4", "BP-070-074R5", "BP-070-074R6", "BP-075-079"):
+        if port.get("current_delivery_revision") not in ("BP-070-074R4", "BP-070-074R5", "BP-070-074R6", "BP-075-079", "BP-075-079R1"):
             errors.append("PORT_LEDGER no longer preserves R4 lineage")
         if hotfix.get("delivery_revision") != "BP-070-074R4" or hotfix.get("delivery_parent") != "BP-070-074R3":
             errors.append("R4 hotfix report lineage differs")
@@ -6085,7 +6101,7 @@ def check_bp070074r5_model_fixture_collector_contract(root: Path) -> CheckResult
         record = ledger.get("r5_delivery_hotfix", {})
         if record.get("production_source_changed") is not False or record.get("native_source_changed") is not False:
             errors.append("BLOCK_LEDGER R5 source-change classification differs")
-        if port.get("current_delivery_revision") not in ("BP-070-074R5", "BP-070-074R6", "BP-075-079"):
+        if port.get("current_delivery_revision") not in ("BP-070-074R5", "BP-070-074R6", "BP-075-079", "BP-075-079R1"):
             errors.append("PORT_LEDGER current delivery is not R5/R6")
         if port.get("bp070074r5_hotfix", {}).get("parent") != "BP-070-074R4":
             errors.append("PORT_LEDGER R5 lineage differs")
@@ -6188,6 +6204,9 @@ def check_bp070074r6_submodel_bounds_fixture_contract(root: Path) -> CheckResult
         elif current_revision == "BP-075-079":
             if current_parent != "BP-070-074R6":
                 errors.append("PORT_LEDGER downstream parent does not preserve accepted R6 lineage")
+        elif current_revision == "BP-075-079R1":
+            if current_parent != "BP-075-079":
+                errors.append("PORT_LEDGER R1 parent does not preserve the BP-075-079 delivery lineage")
         else:
             errors.append("PORT_LEDGER current delivery does not preserve accepted R6 lineage")
         if port.get("bp070074r6_hotfix", {}).get("parent") != "BP-070-074R5":
@@ -6281,7 +6300,7 @@ def check_gameplay_presentation_block_contract(root: Path) -> CheckResult:
         ledger = json.loads((root / "BLOCK_LEDGER.json").read_text(encoding="utf-8"))
         expected = {
             "block_id": "BP-075-079", "parent_package_id": "BP-070-074R6",
-            "final_package_id": "BP-079", "delivery_revision": "BP-075-079",
+            "final_package_id": "BP-079", "delivery_revision": "BP-075-079R1",
             "gameplay_presentation_status": GAMEPLAY_PRESENTATION_STATUS,
             "gameplay_presentation_fingerprint": "0xad91624c",
             "block_runtime_fixtures": 112,
@@ -6333,6 +6352,135 @@ def check_gameplay_presentation_block_contract(root: Path) -> CheckResult:
         errors.append(f"cannot validate gameplay/presentation block contract: {exc}")
     return CheckResult("gameplay_presentation_block_contract", not errors, details, errors)
 
+def check_bp075079r1_world_physics_lineage_contract(root: Path) -> CheckResult:
+    errors: list[str] = []
+    details: dict[str, object] = {}
+    result_hash = "78963a812a05fe9d4951eef1d1cb425757c3a63513c9a956481b36f5033a0384"
+    expected_sections = {
+        "runWorldPhysicsWithRetouch": "5b082bf6147659c213fe46e84679d44a73f11dcd2aac070ef51eaf66dd0ecc8b",
+        "runNonClientPhysicsWithRetouch": "24a05fed078529237192653250631835a9d22d6d6c9054abb0ea5121f13095a6",
+        "frameMode": "09549e467d9a73e0802fda2c71552348fe7470dd6a84766098d02c90cbf87921",
+    }
+    try:
+        checker_path = root / "tools/check_world_physics_closure.py"
+        checker = checker_path.read_text(encoding="utf-8-sig")
+        build = (root / "build.ps1").read_text(encoding="utf-8-sig")
+        analysis = (root / "docs/BP-075-079R1_RESULT_ANALYSIS.md").read_text(encoding="utf-8")
+        testing = (root / "docs/BP-075-079R1_TESTING.md").read_text(encoding="utf-8")
+        changelog = (root / "CHANGELOG_BP-075-079R1.md").read_text(encoding="utf-8")
+        hotfix = json.loads((root / "docs/BP-075-079R1_HOTFIX_REPORT.json").read_text(encoding="utf-8"))
+        ledger = json.loads((root / "BLOCK_LEDGER.json").read_text(encoding="utf-8"))
+        port = json.loads((root / "PORT_LEDGER.json").read_text(encoding="utf-8"))
+
+        for marker in (
+            "--allow-downstream-package",
+            "SERVER_SECTION_HASHES",
+            "DOWNSTREAM_SERVER_PROTECTED_SHA256",
+            "DOWNSTREAM_MODE",
+            "historical_files_plus_server_sections",
+            "runWorldPhysicsWithRetouch",
+            "runNonClientPhysicsWithRetouch",
+            "frameMode",
+        ):
+            if marker not in checker:
+                errors.append(f"world/physics checker missing R1 marker: {marker}")
+        for marker in (
+            'if ($Checker.Path -eq "tools\\check_world_physics_closure.py")',
+            '$CheckerArguments += "--allow-downstream-package"',
+        ):
+            if marker not in build:
+                errors.append(f"build.ps1 missing downstream world/physics marker: {marker}")
+
+        downstream_report = root / "build" / "bp075079r1-world-physics-downstream.json"
+        downstream_report.parent.mkdir(parents=True, exist_ok=True)
+        downstream = subprocess.run(
+            [sys.executable, str(checker_path), "--root", str(root),
+             "--allow-downstream-package", "--json-output", str(downstream_report)],
+            capture_output=True,
+            text=True,
+        )
+        if downstream.returncode != 0:
+            errors.append("downstream BP-029 checker failed: " + (downstream.stdout + downstream.stderr).strip())
+        else:
+            report = json.loads(downstream_report.read_text(encoding="utf-8"))
+            if report.get("downstream_package") is not True:
+                errors.append("downstream BP-029 report does not identify downstream mode")
+            if report.get("downstream_mode") != "historical_files_plus_server_sections":
+                errors.append("downstream BP-029 report mode differs")
+            if report.get("downstream", {}).get("exact_authoritative_files") != 8:
+                errors.append("downstream BP-029 report does not retain eight exact authoritative files")
+            if report.get("downstream", {}).get("build_package_id") != "BP-079":
+                errors.append("downstream BP-029 package identity differs")
+            if report.get("downstream", {}).get("build_block_id") != "BP-075-079":
+                errors.append("downstream BP-029 block identity differs")
+            section_hashes = report.get("details", {}).get("server_section_hashes", {})
+            if section_hashes != expected_sections:
+                errors.append(f"downstream server section hashes differ: {section_hashes!r}")
+            protected_hash = report.get("details", {}).get("server_protected_sha256", "")
+            if protected_hash != "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781":
+                errors.append(f"downstream protected server hash differs: {protected_hash!r}")
+
+        strict = subprocess.run(
+            [sys.executable, str(checker_path), "--root", str(root)],
+            capture_output=True,
+            text=True,
+        )
+        if strict.returncode == 0:
+            errors.append("strict historical BP-029 mode unexpectedly accepts downstream server.ml")
+        if "closure golden differs from authoritative sources" not in (strict.stdout + strict.stderr):
+            errors.append("strict historical BP-029 rejection reason changed")
+
+        if hotfix.get("delivery_revision") != "BP-075-079R1" or hotfix.get("delivery_parent") != "BP-075-079":
+            errors.append("R1 hotfix report lineage differs")
+        if hotfix.get("predecessor_result_sha256") != result_hash:
+            errors.append("R1 hotfix predecessor result hash differs")
+        if hotfix.get("server_section_hashes") != expected_sections:
+            errors.append("R1 hotfix server section hashes differ")
+        if hotfix.get("server_protected_sha256") != "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781":
+            errors.append("R1 hotfix protected server hash differs")
+        for key in ("production_source_changed", "native_source_changed",
+                    "world_physics_fingerprint_changed", "runtime_fixture_count_changed"):
+            if hotfix.get(key) is not False:
+                errors.append(f"R1 hotfix classification {key} differs")
+
+        if ledger.get("delivery_revision") != "BP-075-079R1" or ledger.get("delivery_parent") != "BP-075-079":
+            errors.append("BLOCK_LEDGER R1 lineage differs")
+        record = ledger.get("bp075079r1_delivery_hotfix", {})
+        if record.get("server_sections") != expected_sections:
+            errors.append("BLOCK_LEDGER R1 server sections differ")
+        if record.get("server_protected_sha256") != "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781":
+            errors.append("BLOCK_LEDGER R1 protected server hash differs")
+        if port.get("current_delivery_revision") != "BP-075-079R1" or port.get("delivery_parent") != "BP-075-079":
+            errors.append("PORT_LEDGER R1 lineage differs")
+        if port.get("bp075079r1_hotfix", {}).get("predecessor_result_sha256") != result_hash:
+            errors.append("PORT_LEDGER R1 predecessor hash differs")
+
+        for marker in (result_hash, "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781", "whole-file", "runWorldPhysicsWithRetouch",
+                       "runNonClientPhysicsWithRetouch", "frameMode"):
+            if marker not in analysis:
+                errors.append(f"R1 result analysis missing marker: {marker}")
+        for marker in ("downstream=true", "historical_files_plus_server_sections"):
+            if marker not in testing:
+                errors.append(f"R1 testing guide missing marker: {marker}")
+        if "No file under `src/` or `native/` changed" not in changelog:
+            errors.append("R1 changelog no longer records unchanged engine/native trees")
+
+        details.update({
+            "delivery_revision": "BP-075-079R1",
+            "delivery_parent": "BP-075-079",
+            "predecessor_result_sha256": result_hash,
+            "strict_historical_mode_rejects_downstream": strict.returncode != 0,
+            "downstream_mode_accepts_current": downstream.returncode == 0,
+            "exact_authoritative_files": 8,
+            "server_sections": len(expected_sections),
+            "server_protected_sha256": "181db4de0fc6db19d9f067aea9d8ebd654c8e14a37c477be1640958c0cff3781",
+            "production_source_changed": False,
+            "native_source_changed": False,
+        })
+    except (OSError, json.JSONDecodeError, TypeError, ValueError) as exc:
+        errors.append(f"cannot validate BP-075-079R1 world/physics lineage hotfix: {exc}")
+    return CheckResult("bp075079r1_world_physics_lineage_contract", not errors, details, errors)
+
 def check_package_identity(root: Path) -> CheckResult:
     errors: list[str] = []
     details: dict[str, object] = {}
@@ -6429,6 +6577,7 @@ def run_checks(root: Path, manifest_path: Path) -> VerificationReport:
         check_current_component(root, "check_gameplay_078.py", "bp078_statusbar_scoreboard_contract"),
         check_current_component(root, "check_gameplay_079.py", "bp079_gameplay_presentation_contract"),
         check_gameplay_presentation_block_contract(root),
+        check_bp075079r1_world_physics_lineage_contract(root),
         check_current_delivery_scripts(root),
         check_live_output_contract(root),
         check_package_identity(root),
