diff --git a/CHANGELOG_OPT-001CR3R1.md b/CHANGELOG_OPT-001CR3R1.md
new file mode 100644
index 0000000..f51bfd7
--- /dev/null
+++ b/CHANGELOG_OPT-001CR3R1.md
@@ -0,0 +1,10 @@
+# MiniQuake OPT-001CR3R1 – Changelog
+
+- Dauerhafte Live-Ausgabe über `tools/run_process_live.py` ohne PowerShell-`Tee-Object`-Pipeline.
+- Sofortiges Flushen jeder Kindprozesszeile in Konsole und UTF-8-Log.
+- Die letzten 80 Logzeilen werden bei einem Kindprozessfehler nochmals ausgegeben.
+- EXE-abhängige Tests werden nach einem Buildfehler als `SKIPPED` markiert.
+- Neun riskante Inline-Wrapper mit weiteren Package-/Native-Aufrufen wurden zurückgestellt.
+- Drei reine Skalareinzelfunktionen bleiben selektiv inline.
+- Alle CR3-Array-, Builder- und Diagnoseallokationsoptimierungen bleiben erhalten.
+- Der Hotpath-Compiletest läuft vor dem vollständigen Spielbuild.
diff --git a/COLLECT_RESULTS.ps1 b/COLLECT_RESULTS.ps1
index 907a542..a18af8f 100644
--- a/COLLECT_RESULTS.ps1
+++ b/COLLECT_RESULTS.ps1
@@ -7,14 +7,14 @@ $Root = $PSScriptRoot
 $Build = Join-Path $Root "build"
 $PackageId = "BP-094"
 $BlockId = "BP-090-094"
-$DeliveryRevision = "OPT-001CR3"
-$DeliveryParent = "OPT-001CR2"
+$DeliveryRevision = "OPT-001CR3R1"
+$DeliveryParent = "OPT-001CR3"
 $Timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
 # original_reference_binary_in_result_archive=$false
 # historical external staging marker retained for contract verification: bp090-094-original-reference
 # quake_game_data_in_result_archive=$false
-$Staging = Join-Path $Build (".opt001cr3-results-{0}" -f $Timestamp)
-$Archive = Join-Path $Build ("MiniQuake_OPT-001CR3_RESULTS_{0}.zip" -f $Timestamp)
+$Staging = Join-Path $Build (".opt001cr3r1-results-{0}" -f $Timestamp)
+$Archive = Join-Path $Build ("MiniQuake_OPT-001CR3R1_RESULTS_{0}.zip" -f $Timestamp)
 
 if (-not (Test-Path -LiteralPath $Build -PathType Container)) { throw "build directory does not exist: $Build" }
 if (Test-Path -LiteralPath $Staging) { Remove-Item -Recurse -Force -LiteralPath $Staging }
@@ -78,6 +78,15 @@ Get-ChildItem -LiteralPath $Build -Recurse -File | ForEach-Object {
 }
 
 $RootFiles = @(
+  "TEST_OPT-001CR3R1.ps1",
+  "CHANGELOG_OPT-001CR3R1.md",
+  "docs\OPT-001CR3R1_TESTING.md",
+  "docs\OPT-001CR3R1_CHANGELOG.md",
+  "docs\OPT-001CR3R1_RESULT_ANALYSIS.md",
+  "docs\OPT-001CR3R1_HOTFIX_REPORT.json",
+  "audit\opt001cr3r1_compiler_safe_inline.json",
+  "tools\run_process_live.py",
+  "patches\OPT-001CR3R1.diff",
   "TEST_OPT-001CR3.ps1",
   "CHANGELOG_OPT-001CR3.md",
   "docs\OPT-001CR3_TESTING.md",
diff --git a/TEST_OPT-001CR3R1.ps1 b/TEST_OPT-001CR3R1.ps1
new file mode 100644
index 0000000..6551508
--- /dev/null
+++ b/TEST_OPT-001CR3R1.ps1
@@ -0,0 +1,422 @@
+﻿[CmdletBinding()]
+param(
+  [Parameter(Mandatory = $true)]
+  [string]$Compiler,
+  [Parameter(Mandatory = $true)]
+  [string]$StdLib,
+  [Parameter(Mandatory = $true)]
+  [string]$QuakeBase,
+  [string]$Game = "id1",
+  [int]$MatrixFrames = 64,
+  [int]$WarmupFrames = 300,
+  [int]$BenchmarkFrames = 3000,
+  [int]$HandleWarmupFrames = 1200,
+  [int]$HandleWindowFrames = 5000,
+  [int]$HandleWindows = 3,
+  [int]$HandleConfirmationWindows = 1,
+  [int]$E1M2VisibleFrames = 1000,
+  [int]$E1M2HeadlessFrames = 10000,
+  [int]$TransitionFrames = 64,
+  [int]$ListenPort = 0,
+  [switch]$SkipBuild,
+  [switch]$SkipTemporaryFirewallRules,
+  [switch]$ContinueIndependentTests
+)
+
+$ErrorActionPreference = "Stop"
+Set-StrictMode -Version Latest
+$env:PYTHONUNBUFFERED = "1"
+
+$Root = $PSScriptRoot
+$Build = Join-Path $Root "build"
+$GameExe = Join-Path $Build "MiniQuake.exe"
+$ContractExe = Join-Path $Build "MiniQuakeOPT001CAllocationTests.exe"
+$HotpathExe = Join-Path $Build "MiniQuakeOPT001CR3HotpathTests.exe"
+$DeliveryRevision = "OPT-001CR3R1"
+$DeliveryParent = "OPT-001CR3"
+$SummaryPath = Join-Path $Build "opt001cr3r1-test-summary.json"
+$LiveProcessRunner = Join-Path $Root "tools\run_process_live.py"
+$EffectiveHandleWindows = $HandleWindows + $HandleConfirmationWindows
+$Steps = @()
+$Failures = 0
+$FirewallRules = @()
+
+
+function Get-FreeUdpPort {
+  $Client = New-Object System.Net.Sockets.UdpClient
+  try {
+    $Client.Client.Bind((New-Object System.Net.IPEndPoint([System.Net.IPAddress]::Loopback, 0)))
+    return [int]([System.Net.IPEndPoint]$Client.Client.LocalEndPoint).Port
+  }
+  finally {
+    $Client.Dispose()
+  }
+}
+
+function Test-IsAdministrator {
+  $Identity = [Security.Principal.WindowsIdentity]::GetCurrent()
+  $Principal = New-Object Security.Principal.WindowsPrincipal($Identity)
+  return $Principal.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
+}
+
+function Add-Step([string]$Name, [string]$Status, [int]$ExitCode, [string]$Log) {
+  $script:Steps += [pscustomobject]@{ name=$Name; status=$Status; exit_code=$ExitCode; log=$Log }
+  if ($Status -eq "FAIL") { $script:Failures += 1 }
+  $Colour = "Gray"
+  if ($Status -eq "PASS") { $Colour = "Green" }
+  if ($Status -eq "FAIL") { $Colour = "Red" }
+  if ($Status -eq "SKIPPED" -or $Status -eq "DIAGNOSTIC") { $Colour = "Yellow" }
+  Write-Host ("[{0}] {1}" -f $Status, $Name) -ForegroundColor $Colour
+}
+
+function ConvertTo-PowerShellSingleQuotedLiteral([string]$Value) {
+  if ($null -eq $Value) { return "''" }
+  return "'" + $Value.Replace("'", "''") + "'"
+}
+
+function Show-LogTail([string]$LogPath, [int]$Lines = 80) {
+  if (-not (Test-Path -LiteralPath $LogPath -PathType Leaf)) {
+    Write-Host ("[MiniQuake/OPT-001CR3R1] log was not created: {0}" -f $LogPath) -ForegroundColor Red
+    return
+  }
+  Write-Host ("[MiniQuake/OPT-001CR3R1] last {0} log lines:" -f $Lines) -ForegroundColor Yellow
+  Get-Content -LiteralPath $LogPath -Tail $Lines | ForEach-Object {
+    Write-Host $_ -ForegroundColor DarkYellow
+  }
+}
+
+function Invoke-ExternalProcessLive(
+  [string]$Name,
+  [string]$Executable,
+  [string[]]$Arguments,
+  [string]$LogName,
+  [switch]$AllowFailure
+) {
+  $LogPath = Join-Path $Build $LogName
+  Write-Host ("[MiniQuake/OPT-001CR3R1] starting {0}" -f $Name) -ForegroundColor Cyan
+  Write-Host ("  executable={0}" -f $Executable)
+  Write-Host ("  arguments={0}" -f ($Arguments -join " "))
+  Write-Host "  output_mode=python_subprocess_live"
+  Write-Host ("  log={0}" -f $LogPath)
+
+  $RunnerArguments = @("-u", $LiveProcessRunner, "--log", $LogPath, "--cwd", $Root, "--", $Executable)
+  $RunnerArguments += $Arguments
+  & $PythonExe @RunnerArguments
+  $Code = [int]$LASTEXITCODE
+  if ($Code -eq 0) {
+    Add-Step $Name "PASS" $Code $LogName
+  } else if ($AllowFailure) {
+    Add-Step $Name "DIAGNOSTIC" $Code $LogName
+  } else {
+    Add-Step $Name "FAIL" $Code $LogName
+    Show-LogTail $LogPath
+    if (-not $ContinueIndependentTests) {
+      throw ("{0} failed with exit code {1}" -f $Name, $Code)
+    }
+  }
+  return $Code
+}
+
+function Invoke-LivePowerShellScript(
+  [string]$Name,
+  [string]$ScriptPath,
+  [string[]]$Arguments,
+  [string]$LogName
+) {
+  $ScriptLiteral = ConvertTo-PowerShellSingleQuotedLiteral $ScriptPath
+  $ArgumentLiterals = @()
+  foreach ($Argument in $Arguments) {
+    $ArgumentLiterals += ConvertTo-PowerShellSingleQuotedLiteral $Argument
+  }
+  $ArgumentExpression = "@(" + ($ArgumentLiterals -join ", ") + ")"
+  $ChildCommand = @"
+`$ErrorActionPreference = 'Stop'
+`$InformationPreference = 'Continue'
+try {
+  `$BuildScript = $ScriptLiteral
+  `$BuildArguments = $ArgumentExpression
+  & `$BuildScript @BuildArguments
+  `$Code = 0
+  if (`$null -ne `$LASTEXITCODE) { `$Code = [int]`$LASTEXITCODE }
+  exit `$Code
+}
+catch {
+  [Console]::Error.WriteLine(`$_.Exception.ToString())
+  exit 1
+}
+"@
+  $EncodedCommand = [Convert]::ToBase64String([Text.Encoding]::Unicode.GetBytes($ChildCommand))
+  return Invoke-ExternalProcessLive $Name $PowerShellExe @(
+    "-NoProfile", "-ExecutionPolicy", "Bypass", "-EncodedCommand", $EncodedCommand
+  ) $LogName -AllowFailure
+}
+
+function Invoke-Live(
+  [string]$Name,
+  [string]$Executable,
+  [string[]]$Arguments,
+  [string]$LogName,
+  [switch]$AllowFailure
+) {
+  return Invoke-ExternalProcessLive $Name $Executable $Arguments $LogName -AllowFailure:$AllowFailure
+}
+
+function Install-LoopbackFirewallRules {
+  if ($SkipTemporaryFirewallRules) {
+    Write-Host "[MiniQuake/OPT-001CR3R1] temporary loopback firewall rules explicitly skipped" -ForegroundColor Yellow
+    return
+  }
+  if (-not (Test-IsAdministrator)) {
+    throw "OPT-001CR3R1 listen-server measurement requires elevated PowerShell for unattended temporary loopback firewall rules. Start PowerShell as Administrator or pass -SkipTemporaryFirewallRules."
+  }
+  foreach ($Command in @("New-NetFirewallRule", "Remove-NetFirewallRule")) {
+    if ($null -eq (Get-Command $Command -ErrorAction SilentlyContinue)) {
+      throw "Windows Defender Firewall cmdlet unavailable: $Command"
+    }
+  }
+  $Prefix = "MiniQuake-OPT001C-$PID"
+  foreach ($Direction in @("Inbound", "Outbound")) {
+    $Name = "$Prefix-$Direction"
+    New-NetFirewallRule `
+      -Name $Name `
+      -DisplayName $Name `
+      -Direction $Direction `
+      -Action Allow `
+      -Program $GameExe `
+      -Protocol UDP `
+      -LocalAddress 127.0.0.1 `
+      -RemoteAddress 127.0.0.1 `
+      -Profile Any | Out-Null
+    $script:FirewallRules += $Name
+  }
+  Write-Host "[MiniQuake/OPT-001CR3R1] temporary loopback firewall rules installed: 2" -ForegroundColor Green
+}
+
+function Remove-LoopbackFirewallRules {
+  foreach ($Name in $script:FirewallRules) {
+    Remove-NetFirewallRule -Name $Name -Confirm:$false -ErrorAction SilentlyContinue
+  }
+  if ($script:FirewallRules.Count -gt 0) {
+    Write-Host "[MiniQuake/OPT-001CR3R1] temporary loopback firewall rules removed" -ForegroundColor Green
+  }
+}
+
+function Write-Summary([string]$Overall, [string]$HandleClassification, [string]$PerformanceClassification) {
+  [ordered]@{
+    schema = "MiniQuakeOPT001CR3R1TestSummary/1"
+    delivery_revision = $DeliveryRevision
+    delivery_parent = $DeliveryParent
+    overall = $Overall
+    failures = $Failures
+    handle_classification = $HandleClassification
+    performance_classification = $PerformanceClassification
+    matrix_frames = $MatrixFrames
+    warmup_frames = $WarmupFrames
+    benchmark_frames = $BenchmarkFrames
+    handle_warmup_frames = $HandleWarmupFrames
+    handle_window_frames = $HandleWindowFrames
+    handle_windows_requested = $HandleWindows
+    handle_confirmation_windows = $HandleConfirmationWindows
+    handle_windows_effective = $EffectiveHandleWindows
+    e1m2_visible_frames = $E1M2VisibleFrames
+    e1m2_headless_frames = $E1M2HeadlessFrames
+    transition_frames = $TransitionFrames
+    maps = @("e1m1", "e1m2")
+    steps = $Steps
+  } | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $SummaryPath -Encoding UTF8
+}
+
+New-Item -ItemType Directory -Force -Path $Build | Out-Null
+if (-not (Test-Path -LiteralPath $LiveProcessRunner -PathType Leaf)) { throw "live process runner missing: $LiveProcessRunner" }
+$Compiler = [IO.Path]::GetFullPath($Compiler)
+$StdLib = [IO.Path]::GetFullPath($StdLib)
+$QuakeBase = [IO.Path]::GetFullPath($QuakeBase)
+
+if ($HandleWindows -lt 3) { throw "HandleWindows must be at least 3" }
+if ($HandleConfirmationWindows -lt 1) { throw "HandleConfirmationWindows must be at least 1" }
+
+if (-not (Test-Path -LiteralPath $Compiler -PathType Leaf)) { throw "MiniLang compiler not found: $Compiler" }
+if (-not (Test-Path -LiteralPath (Join-Path $StdLib "std\fs.ml") -PathType Leaf)) { throw "MiniLang stdlib root invalid: $StdLib" }
+if (-not (Test-Path -LiteralPath (Join-Path $QuakeBase "$Game\pak0.pak") -PathType Leaf)) { throw "Quake pak0.pak not found below: $QuakeBase\$Game" }
+
+$PythonExe = (Get-Command python -ErrorAction Stop | Select-Object -First 1).Path
+$PowerShellExe = (Get-Process -Id $PID).Path
+if ($ListenPort -le 0) { $ListenPort = Get-FreeUdpPort }
+
+Write-Host "MiniQuake OPT-001CR3R1 acceptance test"
+Write-Host "  parent: $DeliveryParent"
+Write-Host "  root:   $Root"
+Write-Host "  maps:   e1m1, e1m2"
+Write-Host "  benchmark: warmup=$WarmupFrames measure=$BenchmarkFrames"
+Write-Host "  handles: warmup=$HandleWarmupFrames requested=$HandleWindows confirmation=$HandleConfirmationWindows effective=$EffectiveHandleWindows x $HandleWindowFrames port=$ListenPort"
+Write-Host "  inherited correctness e1m2: visible=$E1M2VisibleFrames headless=$E1M2HeadlessFrames transition=$TransitionFrames"
+
+try {
+  $PackageVerifyCode = Invoke-Live "OPT-001CR3R1 package verification" $PythonExe @(
+    (Join-Path $Root "tools\verify.py"), "--root", $Root,
+    "--json", (Join-Path $Build "opt001cr3r1-package-verification.json")
+  ) "opt001cr3r1-package-verification.log"
+  if ($PackageVerifyCode -ne 0) {
+    Write-Summary "FAIL" "UNKNOWN" "UNKNOWN"
+    Write-Host "MiniQuake OPT-001CR3R1 acceptance test: FAIL" -ForegroundColor Red
+    exit 2
+  }
+
+  if (-not $SkipBuild) {
+    $BuildArgs = @(
+      "-Compiler", $Compiler, "-StdLib", $StdLib, "-Configuration", "Release",
+      "-NoRunTests", "-SkipPreflight"
+    )
+    $BuildCode = Invoke-LivePowerShellScript `
+      "OPT-001CR3R1 game and contract build" `
+      (Join-Path $Root "build.ps1") `
+      $BuildArgs `
+      "opt001cr3r1-build.log"
+  } else {
+    if (-not (Test-Path -LiteralPath $GameExe -PathType Leaf)) { throw "-SkipBuild requested but MiniQuake.exe is missing" }
+    if (-not (Test-Path -LiteralPath $ContractExe -PathType Leaf)) { throw "-SkipBuild requested but contract test executable is missing" }
+    Add-Step "OPT-001CR3R1 game and contract build" "SKIPPED" 0 "-SkipBuild"
+  }
+
+  if (-not $SkipBuild -and $BuildCode -ne 0) {
+    Add-Step "OPT-001CR3R1 allocation contract tests" "SKIPPED" 0 "dependent tests skipped because build failed"
+    Add-Step "OPT-001CR3R1 inline/array hotpath tests" "SKIPPED" 0 "dependent tests skipped because build failed"
+    Write-Summary "FAIL" "UNKNOWN" "UNKNOWN"
+    Write-Host "ERROR: build failed; all executable-dependent tests were skipped." -ForegroundColor Red
+    Write-Host "MiniQuake OPT-001CR3R1 acceptance test: FAIL" -ForegroundColor Red
+    exit 2
+  }
+  foreach ($RequiredExe in @($GameExe, $ContractExe, $HotpathExe)) {
+    if (-not (Test-Path -LiteralPath $RequiredExe -PathType Leaf)) {
+      Add-Step "OPT-001CR3R1 required executable" "FAIL" 1 $RequiredExe
+      Write-Summary "FAIL" "UNKNOWN" "UNKNOWN"
+      Write-Host ("ERROR: required executable missing after successful build: {0}" -f $RequiredExe) -ForegroundColor Red
+      Write-Host "MiniQuake OPT-001CR3R1 acceptance test: FAIL" -ForegroundColor Red
+      exit 2
+    }
+  }
+
+  [void](Invoke-Live "OPT-001CR3R1 allocation contract tests" $ContractExe @() "opt001cr3r1-allocation-tests.log")
+  [void](Invoke-Live "OPT-001CR3R1 inline/array hotpath tests" $HotpathExe @() "opt001cr3r1-hotpath-tests.log")
+  Install-LoopbackFirewallRules
+
+  foreach ($Map in @("e1m1", "e1m2")) {
+    $MapPrefix = Join-Path $Build ("opt001cr3r1-{0}" -f $Map)
+    [void](Invoke-Live "$Map BSP parse" $GameExe @(
+      "--opt001a-map-parse", $QuakeBase, $Map, $MapPrefix, "-game", $Game
+    ) ("opt001cr3r1-{0}-map-parse.log" -f $Map))
+    [void](Invoke-Live "$Map runtime smoke" $GameExe @(
+      "--runtime-smoke", $QuakeBase, $Map, [string]$MatrixFrames, "-game", $Game
+    ) ("opt001cr3r1-{0}-runtime-smoke.log" -f $Map))
+    [void](Invoke-Live "$Map render smoke" $GameExe @(
+      "--render-smoke", $QuakeBase, $Map, [string]$MatrixFrames, "-game", $Game
+    ) ("opt001cr3r1-{0}-render-smoke.log" -f $Map))
+
+    $TraceA = Join-Path $Build ("opt001cr3r1-{0}-trace-a" -f $Map)
+    $TraceB = Join-Path $Build ("opt001cr3r1-{0}-trace-b" -f $Map)
+    [void](Invoke-Live "$Map trace A" $GameExe @(
+      "--compat-trace", $QuakeBase, $Map, [string]$MatrixFrames, $TraceA, "-game", $Game
+    ) ("opt001cr3r1-{0}-trace-a.log" -f $Map))
+    [void](Invoke-Live "$Map trace B" $GameExe @(
+      "--compat-trace", $QuakeBase, $Map, [string]$MatrixFrames, $TraceB, "-game", $Game
+    ) ("opt001cr3r1-{0}-trace-b.log" -f $Map))
+    [void](Invoke-Live "$Map trace comparison" $PythonExe @(
+      (Join-Path $Root "tools\compare_traces.py"), ($TraceA + ".mqtrace"), ($TraceB + ".mqtrace"),
+      "--json-output", (Join-Path $Build ("opt001cr3r1-{0}-trace-comparison.json" -f $Map))
+    ) ("opt001cr3r1-{0}-trace-comparison.log" -f $Map))
+
+    foreach ($Mode in @("headless", "render")) {
+      $Prefix = Join-Path $Build ("opt001cr3r1-{0}-{1}" -f $Map, $Mode)
+      [void](Invoke-Live "$Map $Mode frame baseline" $GameExe @(
+        "--opt001a-frame-baseline", $QuakeBase, $Map, $Mode,
+        [string]$WarmupFrames, [string]$BenchmarkFrames, $Prefix, "-game", $Game
+      ) ("opt001cr3r1-{0}-{1}-baseline.log" -f $Map, $Mode))
+    }
+  }
+
+  $VisiblePrefix = Join-Path $Build "opt001cr3r1-e1m2-visible"
+  [void](Invoke-Live "OPT-001CR3R1 e1m2 visible validation" $GameExe @(
+    "--render-evidence", $QuakeBase, "e1m2", [string]$E1M2VisibleFrames, $VisiblePrefix, "-game", $Game
+  ) "opt001cr3r1-e1m2-visible.log")
+
+  [void](Invoke-Live "OPT-001CR3R1 e1m2 headless validation" $GameExe @(
+    "--runtime-smoke", $QuakeBase, "e1m2", [string]$E1M2HeadlessFrames, "-game", $Game
+  ) "opt001cr3r1-e1m2-headless.log")
+
+  $TransitionPrefix = Join-Path $Build "opt001cr3r1-transition"
+  [void](Invoke-Live "OPT-001CR3R1 e1m1-e1m2-e1m1 transition" $GameExe @(
+    "--opt001b-transition", $QuakeBase, [string]$TransitionFrames, $TransitionPrefix, "-game", $Game
+  ) "opt001cr3r1-transition.log")
+
+  $PlateauPrefix = Join-Path $Build "opt001cr3r1-listen-handle-plateau"
+  [void](Invoke-Live "listen-server handle plateau" $GameExe @(
+    "--opt001a-handle-plateau", $QuakeBase, "start",
+    [string]$HandleWarmupFrames, [string]$HandleWindowFrames, [string]$EffectiveHandleWindows,
+    $PlateauPrefix, "-game", $Game, "-port", [string]$ListenPort
+  ) "opt001cr3r1-handle-plateau.log" -AllowFailure)
+
+  [void](Invoke-Live "OPT-001CR3R1 aggregate analysis" $PythonExe @(
+    (Join-Path $Root "tools\analyze_opt001a.py"), "--build", $Build,
+    "--prefix", "opt001cr3r1", "--next-revision", "OPT-001D",
+    "--json", (Join-Path $Build "opt001cr3r1-baseline-summary.json"),
+    "--markdown", (Join-Path $Build "opt001cr3r1-baseline-summary.md")
+  ) "opt001cr3r1-aggregate-analysis.log" -AllowFailure)
+
+  $PerformanceJson = Join-Path $Build "opt001cr3r1-performance-comparison.json"
+  [void](Invoke-Live "OPT-001CR3R1 performance comparison" $PythonExe @(
+    (Join-Path $Root "tools\compare_opt001c_performance.py"),
+    "--baseline", (Join-Path $Root "audit\opt001b_performance_baseline.json"),
+    "--build", $Build,
+    "--prefix", "opt001cr3r1",
+    "--json", $PerformanceJson,
+    "--markdown", (Join-Path $Build "opt001cr3r1-performance-comparison.md")
+  ) "opt001cr3r1-performance-comparison.log")
+
+  [void](Invoke-Live "OPT-001CR3R1 incremental performance comparison" $PythonExe @(
+    (Join-Path $Root "tools\compare_opt001cr3_performance.py"),
+    "--baseline", (Join-Path $Root "audit\opt001cr2_accepted_baseline.json"),
+    "--build", $Build,
+    "--prefix", "opt001cr3r1",
+    "--json", (Join-Path $Build "opt001cr3r1-incremental-performance.json"),
+    "--markdown", (Join-Path $Build "opt001cr3r1-incremental-performance.md")
+  ) "opt001cr3r1-incremental-performance.log")
+
+  $HandleClassification = "UNKNOWN"
+  $PlateauSummary = $PlateauPrefix + "-summary.json"
+  if (Test-Path -LiteralPath $PlateauSummary -PathType Leaf) {
+    $HandleClassification = [string]((Get-Content -Raw -LiteralPath $PlateauSummary | ConvertFrom-Json).classification)
+  }
+  $PerformanceClassification = "UNKNOWN"
+  if (Test-Path -LiteralPath $PerformanceJson -PathType Leaf) {
+    $PerformanceClassification = [string]((Get-Content -Raw -LiteralPath $PerformanceJson | ConvertFrom-Json).classification)
+  }
+  $Overall = "PASS"
+  if ($Failures -gt 0) { $Overall = "FAIL" }
+  if ($Overall -eq "PASS" -and $HandleClassification -eq "LEAK") { $Overall = "LEAK" }
+  if ($Overall -eq "PASS" -and $HandleClassification -eq "INCONCLUSIVE") { $Overall = "INCONCLUSIVE" }
+  if ($Overall -eq "PASS" -and $HandleClassification -eq "RESOURCE_GROWTH") { $Overall = "FAIL" }
+  Write-Summary $Overall $HandleClassification $PerformanceClassification
+
+  Write-Host ""
+  Write-Host "MiniQuake OPT-001CR3R1 summary"
+  Write-Host "  handle_classification=$HandleClassification"
+  Write-Host "  performance_classification=$PerformanceClassification"
+  Write-Host "  failures=$Failures"
+  Write-Host "  summary=$SummaryPath"
+  if ($Overall -eq "PASS") {
+    Write-Host "MiniQuake OPT-001CR3R1 acceptance test: PASS" -ForegroundColor Green
+    exit 0
+  }
+  Write-Host ("MiniQuake OPT-001CR3R1 acceptance test: {0}" -f $Overall) -ForegroundColor Red
+  exit 2
+}
+catch {
+  Add-Step "OPT-001CR3R1 harness" "FAIL" 1 $_.Exception.Message
+  Write-Summary "FAIL" "UNKNOWN" "UNKNOWN"
+  Write-Host ("ERROR: " + $_.Exception.Message) -ForegroundColor Red
+  Write-Host "MiniQuake OPT-001CR3R1 acceptance test: FAIL" -ForegroundColor Red
+  exit 1
+}
+finally {
+  Remove-LoopbackFirewallRules
+}
diff --git a/audit/opt001cr3r1_compiler_safe_inline.json b/audit/opt001cr3r1_compiler_safe_inline.json
new file mode 100644
index 0000000..758e705
--- /dev/null
+++ b/audit/opt001cr3r1_compiler_safe_inline.json
@@ -0,0 +1,96 @@
+{
+  "schema": "MiniQuakeOPT001CR3R1Hotpath/1",
+  "revision": "OPT-001CR3R1",
+  "parent": "OPT-001CR3",
+  "result_archive": "MiniQuake_OPT-001CR3_RESULTS_20260808-234854.zip",
+  "result_sha256": "a57053a39cc4cb26a71cf638c2004374007380f89c7f20d525065bc4c4e78af8",
+  "observed_failure": {
+    "package_verification": "PASS",
+    "build": "FAIL",
+    "build_exit_code": 1,
+    "build_log_present_in_result_archive": false,
+    "follow_on_missing_executable": "MiniQuakeOPT001CAllocationTests.exe"
+  },
+  "compiler_safe_inline_policy": {
+    "required": [
+      {
+        "path": "src/miniquake/mathlib.ml",
+        "function": "DotProduct"
+      },
+      {
+        "path": "src/miniquake/protocol15_freeze.ml",
+        "function": "fingerprintValue"
+      },
+      {
+        "path": "src/miniquake/statusbar.ml",
+        "function": "Sbar_ColorForMap"
+      }
+    ],
+    "removed_risky_wrappers": [
+      {
+        "path": "src/miniquake/native.ml",
+        "functions": [
+          "floatBits",
+          "bitsFloat"
+        ]
+      },
+      {
+        "path": "src/miniquake/client.ml",
+        "functions": [
+          "clientFloat"
+        ]
+      },
+      {
+        "path": "src/miniquake/particles.ml",
+        "functions": [
+          "particleFloat"
+        ]
+      },
+      {
+        "path": "src/miniquake/sound/snd_dma.ml",
+        "functions": [
+          "soundF32"
+        ]
+      },
+      {
+        "path": "src/miniquake/sound/mixer.ml",
+        "functions": [
+          "mixerF32"
+        ]
+      },
+      {
+        "path": "src/miniquake/render/gl_warp.ml",
+        "functions": [
+          "warpFloat"
+        ]
+      },
+      {
+        "path": "src/miniquake/client_render_handoff.ml",
+        "functions": [
+          "renderFloat"
+        ]
+      },
+      {
+        "path": "src/miniquake/render/gl_rmain.ml",
+        "functions": [
+          "F32"
+        ]
+      }
+    ],
+    "budget": 3,
+    "rule": "single pure expression, no imported/native helper call in inline body"
+  },
+  "array_optimizations_preserved": [
+    "exact array(count, fill) allocation",
+    "geometric visible-entity builder",
+    "exact particle command/alive buffers",
+    "fixed-size zero/identity/client/handle arrays",
+    "guarded diagnostic frameTrace",
+    "fixed four-element partial IPv4 arrays"
+  ],
+  "harness": {
+    "live_output": "python_subprocess_live",
+    "build_failure_log_tail": 80,
+    "dependent_tests_after_build_failure": "SKIPPED"
+  }
+}
diff --git a/build.ps1 b/build.ps1
index 8e27e87..4817ba8 100644
--- a/build.ps1
+++ b/build.ps1
@@ -379,6 +379,10 @@ New-Item -ItemType Directory -Force -Path $Output | Out-Null
 # for current BP-044 products by the result collector.
 $PackageBuildArtifacts = @(
   "MiniQuake.exe",
+  "MiniQuakeOPT001AContractTests.exe",
+  "MiniQuakeOPT001BCorrectnessTests.exe",
+  "MiniQuakeOPT001CAllocationTests.exe",
+  "MiniQuakeOPT001CR3HotpathTests.exe",
   "MiniQuakeTests.exe",
   "MiniQuakeMilestoneTests.exe",
   "MiniQuakeCompatTraceTests.exe",
@@ -967,6 +971,10 @@ if ($Listings) {
   $CommonArgs += @("--asm", "--asm-pe", "--asm-data")
 }
 
+$Opt001CR3HotpathExe = Join-Path $Output "MiniQuakeOPT001CR3HotpathTests.exe"
+Write-Host "[MiniQuake] compiling $Opt001CR3HotpathExe"
+Invoke-MiniLangCompile -InputFile (Join-Path $Root "tests\opt001cr3_hotpath_tests.ml") -OutputFile $Opt001CR3HotpathExe -CompilerArguments $CommonArgs -Label "opt001cr3-hotpath-tests"
+
 $GameExe = Join-Path $Output "MiniQuake.exe"
 Write-Host "[MiniQuake] compiling $GameExe"
 Invoke-MiniLangCompile -InputFile (Join-Path $Source "main.ml") -OutputFile $GameExe -CompilerArguments $CommonArgs -Label "game"
@@ -983,10 +991,6 @@ $Opt001CContractExe = Join-Path $Output "MiniQuakeOPT001CAllocationTests.exe"
 Write-Host "[MiniQuake] compiling $Opt001CContractExe"
 Invoke-MiniLangCompile -InputFile (Join-Path $Root "tests\opt001c_contract_tests.ml") -OutputFile $Opt001CContractExe -CompilerArguments $CommonArgs -Label "opt001c-allocation-tests"
 
-$Opt001CR3HotpathExe = Join-Path $Output "MiniQuakeOPT001CR3HotpathTests.exe"
-Write-Host "[MiniQuake] compiling $Opt001CR3HotpathExe"
-Invoke-MiniLangCompile -InputFile (Join-Path $Root "tests\opt001cr3_hotpath_tests.ml") -OutputFile $Opt001CR3HotpathExe -CompilerArguments $CommonArgs -Label "opt001cr3-hotpath-tests"
-
 
 if (-not $NoRunTests) {
   Write-Host "[MiniQuake] checking executable package identity"
diff --git a/docs/OPT-001CR3R1_CHANGELOG.md b/docs/OPT-001CR3R1_CHANGELOG.md
new file mode 100644
index 0000000..f51bfd7
--- /dev/null
+++ b/docs/OPT-001CR3R1_CHANGELOG.md
@@ -0,0 +1,10 @@
+# MiniQuake OPT-001CR3R1 – Changelog
+
+- Dauerhafte Live-Ausgabe über `tools/run_process_live.py` ohne PowerShell-`Tee-Object`-Pipeline.
+- Sofortiges Flushen jeder Kindprozesszeile in Konsole und UTF-8-Log.
+- Die letzten 80 Logzeilen werden bei einem Kindprozessfehler nochmals ausgegeben.
+- EXE-abhängige Tests werden nach einem Buildfehler als `SKIPPED` markiert.
+- Neun riskante Inline-Wrapper mit weiteren Package-/Native-Aufrufen wurden zurückgestellt.
+- Drei reine Skalareinzelfunktionen bleiben selektiv inline.
+- Alle CR3-Array-, Builder- und Diagnoseallokationsoptimierungen bleiben erhalten.
+- Der Hotpath-Compiletest läuft vor dem vollständigen Spielbuild.
diff --git a/docs/OPT-001CR3R1_HOTFIX_REPORT.json b/docs/OPT-001CR3R1_HOTFIX_REPORT.json
new file mode 100644
index 0000000..7ab159c
--- /dev/null
+++ b/docs/OPT-001CR3R1_HOTFIX_REPORT.json
@@ -0,0 +1,14 @@
+{
+  "schema": "MiniQuakeOPT001CR3R1Hotfix/1",
+  "revision": "OPT-001CR3R1",
+  "parent": "OPT-001CR3",
+  "result_archive_sha256": "a57053a39cc4cb26a71cf638c2004374007380f89c7f20d525065bc4c4e78af8",
+  "classification": "build_failure_with_missing_build_diagnostics_and_harness_follow_on",
+  "fixes": [
+    "python subprocess live tee",
+    "build failure dependency barrier",
+    "compiler-safe inline subset",
+    "hotpath compile test before game"
+  ],
+  "engine_semantics": "unchanged except retained performance-only CR3 transformations and reduced inline expansion"
+}
diff --git a/docs/OPT-001CR3R1_RESULT_ANALYSIS.md b/docs/OPT-001CR3R1_RESULT_ANALYSIS.md
new file mode 100644
index 0000000..2680ea3
--- /dev/null
+++ b/docs/OPT-001CR3R1_RESULT_ANALYSIS.md
@@ -0,0 +1,15 @@
+# MiniQuake OPT-001CR3R1 – Auswertung des CR3-Laufs
+
+Das Ergebnisarchiv `MiniQuake_OPT-001CR3_RESULTS_20260808-234854.zip` besitzt den SHA-256-Wert `a57053a39cc4cb26a71cf638c2004374007380f89c7f20d525065bc4c4e78af8`.
+
+Der Paketverifier bestand. Der nachfolgende Build endete mit Exitcode 1. Danach versuchte der alte Runner trotz fehlgeschlagenem Build die nicht erzeugte Datei `MiniQuakeOPT001CAllocationTests.exe` zu starten. Das ist ein Folgefehler des Harnesses.
+
+Das Ergebnisarchiv enthält weder `opt001cr3-build.log` noch `compile-*.log`. Daher unterstützt die Evidenz keine seriöse Behauptung, welcher einzelne Compilerfehler den Build stoppte. R1 zieht deshalb eine konservative Grenze:
+
+- die MiniLang-spezifischen Array- und Builderoptimierungen bleiben erhalten;
+- die neun Inline-Wrapper, deren Bodies weitere Package-/Native-Helfer aufrufen, werden auf normale Funktionen zurückgestellt;
+- nur drei einzeilige, reine, allokationsfreie Funktionen bleiben inline;
+- der Build wird über einen separaten Python-Prozess zeilenweise in Konsole und Log weitergereicht;
+- nach einem Buildfehler werden alle EXE-abhängigen Tests als `SKIPPED` markiert.
+
+Damit liefert ein weiterer Fehler erstmals die tatsächliche Compilerdiagnose und beschädigt die Zusammenfassung nicht durch Folgefehler.
diff --git a/docs/OPT-001CR3R1_TESTING.md b/docs/OPT-001CR3R1_TESTING.md
new file mode 100644
index 0000000..8b9a117
--- /dev/null
+++ b/docs/OPT-001CR3R1_TESTING.md
@@ -0,0 +1,34 @@
+# MiniQuake OPT-001CR3R1 – Windows-Test
+
+Das ZIP flach in einen leeren MiniQuake-Ordner entpacken. Danach PowerShell als Administrator öffnen.
+
+```powershell
+Get-ChildItem -Recurse -File | Unblock-File
+$QuakeBase = "C:\Users\nilsk\Dropbox\Quake"
+$CompilerRoot = "C:\Users\nilsk\Desktop\MiniLang\MiniLangCompilerPy"
+
+.\TEST_OPT-001CR3R1.ps1 `
+  -Compiler "$CompilerRoot\mlc_win64.py" `
+  -StdLib $CompilerRoot `
+  -QuakeBase $QuakeBase `
+  -Game id1 `
+  -MatrixFrames 64 `
+  -WarmupFrames 300 `
+  -BenchmarkFrames 3000 `
+  -HandleWarmupFrames 1200 `
+  -HandleWindowFrames 5000 `
+  -HandleWindows 3 `
+  -HandleConfirmationWindows 1 `
+  -E1M2VisibleFrames 1000 `
+  -E1M2HeadlessFrames 10000 `
+  -TransitionFrames 64 `
+  -ContinueIndependentTests
+```
+
+Der Build muss sofort fortlaufende Zeilen unter `output_mode=python_subprocess_live` anzeigen. Bei einem Buildfehler werden abhängige Tests nicht gestartet; stattdessen erscheinen `SKIPPED` und die letzten Logzeilen.
+
+Nach dem Lauf:
+
+```powershell
+.\COLLECT_RESULTS.ps1
+```
diff --git a/src/miniquake/build_info.ml b/src/miniquake/build_info.ml
index 562888a..6d6ba28 100644
--- a/src/miniquake/build_info.ml
+++ b/src/miniquake/build_info.ml
@@ -60,8 +60,8 @@ const COMPAT_FINAL_STATUS = "compat_109_final_candidate_v1"
 const COMPAT_FINAL_FINGERPRINT = 0xe04a7727
 const PACKAGE_PURPOSE = "Verified original GLQuake provenance, bidirectional binary interop, external visual-reference corpus and final compat_109 candidate"
 
-const OPTIMIZATION_STATUS = "opt001cr3_inline_array_hotpath_candidate_v1"
-const OPTIMIZATION_FINGERPRINT = 0x1c001c05
-const OPTIMIZATION_PARENT = "OPT-001CR2"
-const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3"
-const OPTIMIZATION_DELIVERY_PARENT = "OPT-001CR2"
+const OPTIMIZATION_STATUS = "opt001cr3r1_compiler_safe_inline_candidate_v1"
+const OPTIMIZATION_FINGERPRINT = 0x1c001c06
+const OPTIMIZATION_PARENT = "OPT-001CR3"
+const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3R1"
+const OPTIMIZATION_DELIVERY_PARENT = "OPT-001CR3"
diff --git a/src/miniquake/client.ml b/src/miniquake/client.ml
index 03b426f..270bce2 100644
--- a/src/miniquake/client.ml
+++ b/src/miniquake/client.ml
@@ -41,7 +41,7 @@ clDisconnectRequestedServerShutdown = false
 
 // client_state_t stores interpolation velocities, entity transforms and
 // dlight fields as C floats even though cl.time/mtime are doubles.
-function inline clientFloat(value)
+function clientFloat(value)
   return native.bitsFloat(native.floatBits(value))
 end function
 
diff --git a/src/miniquake/client_render_handoff.ml b/src/miniquake/client_render_handoff.ml
index 847d597..76d10df 100644
--- a/src/miniquake/client_render_handoff.ml
+++ b/src/miniquake/client_render_handoff.ml
@@ -24,7 +24,7 @@ const BEAM_STEP = 30.0
 
 currentTemporary = []
 
-function inline renderFloat(value)
+function renderFloat(value)
   return native.bitsFloat(native.floatBits(value))
 end function
 
diff --git a/src/miniquake/native.ml b/src/miniquake/native.ml
index 127789e..e356274 100644
--- a/src/miniquake/native.ml
+++ b/src/miniquake/native.ml
@@ -249,14 +249,14 @@ function glGetString(name)
   return nativeTextResult(output, glGetStringRaw(name, output, len(output)))
 end function
 
-function inline floatBits(value)
+function floatBits(value)
   // Avoid number -> text -> strtod.  MiniLang exposes the exact tagged word via
   // nativeRawValue(), and the bridge understands both immediate f32 and boxed
   // f64 numeric values.
   return f32FromRaw(nativeRawValue(value))
 end function
 
-function inline bitsFloat(bits)
+function bitsFloat(bits)
   // Every IEEE-754 binary32 value has MiniLang's compact immediate-float form.
   // Return that raw word and let nativeValueFromRaw() restore the real value.
   return nativeValueFromRaw(f32ToRaw(bits))
diff --git a/src/miniquake/particles.ml b/src/miniquake/particles.ml
index 9e1997f..d7f4f51 100644
--- a/src/miniquake/particles.ml
+++ b/src/miniquake/particles.ml
@@ -29,7 +29,7 @@ const MAX_PARTICLES = 2048
 const ABSOLUTE_MIN_PARTICLES = 512
 const NUM_VERTEX_NORMALS = 162
 
-function inline particleFloat(value)
+function particleFloat(value)
   return native.bitsFloat(native.floatBits(value))
 end function
 
diff --git a/src/miniquake/protocol15_freeze.ml b/src/miniquake/protocol15_freeze.ml
index 9f15c8e..b7e6f28 100644
--- a/src/miniquake/protocol15_freeze.ml
+++ b/src/miniquake/protocol15_freeze.ml
@@ -94,7 +94,7 @@ function combineMask(values)
   return result
 end function
 
-function fingerprintValue(current, value)
+function inline fingerprintValue(current, value)
   return ((current ^ (value & 0xffffffff)) * 16777619) & 0xffffffff
 end function
 
diff --git a/src/miniquake/render/gl_rmain.ml b/src/miniquake/render/gl_rmain.ml
index a144b59..6ca600e 100644
--- a/src/miniquake/render/gl_rmain.ml
+++ b/src/miniquake/render/gl_rmain.ml
@@ -177,7 +177,7 @@ function Dot(left, right)
   return left[0] * right[0] + left[1] * right[1] + left[2] * right[2]
 end function
 
-function inline F32(value)
+function F32(value)
   return native.bitsFloat(native.floatBits(value))
 end function
 
diff --git a/src/miniquake/render/gl_warp.ml b/src/miniquake/render/gl_warp.ml
index db19567..de71c9e 100644
--- a/src/miniquake/render/gl_warp.ml
+++ b/src/miniquake/render/gl_warp.ml
@@ -22,7 +22,7 @@ gl_subdivide_size = DEFAULT_SUBDIVIDE_SIZE
 // gl_warp.c stores all polygon, direction, and texture-coordinate values in
 // float fields. Explicitly cross the IEEE-754 binary32 boundary at the same
 // assignments so the MiniLang backend cannot retain additional precision.
-function inline warpFloat(value)
+function warpFloat(value)
   return native.bitsFloat(native.floatBits(value))
 end function
 
diff --git a/src/miniquake/sound/mixer.ml b/src/miniquake/sound/mixer.ml
index 6d2f64e..cf47938 100644
--- a/src/miniquake/sound/mixer.ml
+++ b/src/miniquake/sound/mixer.ml
@@ -296,7 +296,7 @@ function setRandomSeed(seed)
   return randomSeed
 end function
 
-function inline mixerF32(value)
+function mixerF32(value)
   return native.bitsFloat(native.floatBits(value))
 end function
 
diff --git a/src/miniquake/sound/snd_dma.ml b/src/miniquake/sound/snd_dma.ml
index dc40741..ca8280a 100644
--- a/src/miniquake/sound/snd_dma.ml
+++ b/src/miniquake/sound/snd_dma.ml
@@ -287,7 +287,7 @@ function SND_PickChannel(system, entityNumber, entityChannel)
   return target
 end function
 
-function inline soundF32(value)
+function soundF32(value)
   return native.bitsFloat(native.floatBits(value))
 end function
 
diff --git a/src/miniquake/statusbar.ml b/src/miniquake/statusbar.ml
index 99019fb..d21e773 100644
--- a/src/miniquake/statusbar.ml
+++ b/src/miniquake/statusbar.ml
@@ -579,7 +579,7 @@ function Sbar_SortFrags(scores)
   return fragsort
 end function
 
-function Sbar_ColorForMap(mapColor)
+function inline Sbar_ColorForMap(mapColor)
   return mapColor + 8
 end function
 
diff --git a/test.ps1 b/test.ps1
index fb7ed68..0412c3e 100644
--- a/test.ps1
+++ b/test.ps1
@@ -1 +1 @@
-﻿& "$PSScriptRoot\TEST_OPT-001CR3.ps1" @args
+﻿& "$PSScriptRoot\TEST_OPT-001CR3R1.ps1" @args
diff --git a/tests/opt001cr3_hotpath_tests.ml b/tests/opt001cr3_hotpath_tests.ml
index ae39698..15896a2 100644
--- a/tests/opt001cr3_hotpath_tests.ml
+++ b/tests/opt001cr3_hotpath_tests.ml
@@ -22,9 +22,9 @@ function check(condition, label)
 end function
 
 function main(args)
-  check(buildInfo.OPTIMIZATION_STATUS == "opt001cr3_inline_array_hotpath_candidate_v1", "optimization status")
-  check(buildInfo.OPTIMIZATION_FINGERPRINT == 0x1c001c05, "optimization fingerprint")
-  check(buildInfo.OPTIMIZATION_PARENT == "OPT-001CR2", "optimization parent")
+  check(buildInfo.OPTIMIZATION_STATUS == "opt001cr3r1_compiler_safe_inline_candidate_v1", "optimization status")
+  check(buildInfo.OPTIMIZATION_FINGERPRINT == 0x1c001c06, "optimization fingerprint")
+  check(buildInfo.OPTIMIZATION_PARENT == "OPT-001CR3", "optimization parent")
 
   filled = arrays.makeFilledArray(5, 7)
   check(len(filled) == 5, "filled array length")
@@ -45,9 +45,9 @@ function main(args)
   b = t.Vec3(4.0, 5.0, 6.0)
   check(math.DotProduct(a, b) == 32.0, "inline dot product")
 
-  print "MiniQuake OPT-001CR3 hotpath tests passed: " + passed
+  print "MiniQuake OPT-001CR3R1 hotpath tests passed: " + passed
   if failed > 0 then
-    print "MiniQuake OPT-001CR3 hotpath tests failed: " + failed
+    print "MiniQuake OPT-001CR3R1 hotpath tests failed: " + failed
     return 1
   end if
   return 0
diff --git a/tools/check_opt001b.py b/tools/check_opt001b.py
index 45d0995..cb7a9a0 100644
--- a/tools/check_opt001b.py
+++ b/tools/check_opt001b.py
@@ -24,7 +24,11 @@ def main() -> int:
     host=(root/'src/miniquake/host.ml').read_text(encoding='utf-8-sig')
     bi=(root/'src/miniquake/build_info.ml').read_text(encoding='utf-8-sig')
     if args.allow_downstream_package:
-        if 'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3"' in bi:
+        if 'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3R1"' in bi:
+            runner_path = root / 'TEST_OPT-001CR3R1.ps1'
+            downstream_revision = 'OPT-001CR3R1'
+            downstream_parent = 'OPT-001CR3'
+        elif 'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3"' in bi:
             runner_path = root / 'TEST_OPT-001CR3.ps1'
             downstream_revision = 'OPT-001CR3'
             downstream_parent = 'OPT-001CR2'
diff --git a/tools/check_opt001c.py b/tools/check_opt001c.py
index 1526111..8065609 100644
--- a/tools/check_opt001c.py
+++ b/tools/check_opt001c.py
@@ -65,7 +65,10 @@ def main() -> int:
     downstream_revision = ""
     downstream_parent = ""
     if args.allow_downstream_package:
-        if 'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3"' in build_info:
+        if 'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3R1"' in build_info:
+            downstream_revision = "OPT-001CR3R1"
+            downstream_parent = "OPT-001CR3"
+        elif 'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3"' in build_info:
             downstream_revision = "OPT-001CR3"
             downstream_parent = "OPT-001CR2"
         elif 'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR2"' in build_info:
diff --git a/tools/check_opt001cr3.py b/tools/check_opt001cr3.py
index d0f156f..435c595 100644
--- a/tools/check_opt001cr3.py
+++ b/tools/check_opt001cr3.py
@@ -3,50 +3,141 @@ from __future__ import annotations
 import argparse, json, re
 from pathlib import Path
 
+
 def req(text, markers, label, errors):
-    for m in markers:
-        if m not in text: errors.append(f"{label} missing marker: {m}")
+    for marker in markers:
+        if marker not in text:
+            errors.append(f"{label} missing marker: {marker}")
+
+
+def block(text, name):
+    match = re.search(rf"(?ms)^function(?:\s+inline)?\s+{re.escape(name)}\b.*?^end function$", text)
+    return match.group(0) if match else ""
 
-def block(text,name):
-    m=re.search(rf"(?ms)^function(?:\s+inline)?\s+{re.escape(name)}\b.*?^end function$",text)
-    return m.group(0) if m else ""
 
 def main():
-    ap=argparse.ArgumentParser(); ap.add_argument('--root',default='.'); ap.add_argument('--json',default=''); a=ap.parse_args(); root=Path(a.root).resolve(); errors=[]
-    info=(root/'src/miniquake/build_info.ml').read_text(encoding='utf-8-sig')
-    arr=(root/'src/miniquake/array_util.ml').read_text(encoding='utf-8-sig')
-    client=(root/'src/miniquake/client.ml').read_text(encoding='utf-8-sig')
-    particles=(root/'src/miniquake/particles.ml').read_text(encoding='utf-8-sig')
-    diag=(root/'src/miniquake/compat_diagnostics.ml').read_text(encoding='utf-8-sig')
-    host=(root/'src/miniquake/host.ml').read_text(encoding='utf-8-sig')
-    runner=(root/'TEST_OPT-001CR3.ps1').read_text(encoding='utf-8-sig')
-    collector=(root/'COLLECT_RESULTS.ps1').read_text(encoding='utf-8-sig')
-    launcher=(root/'test.ps1').read_text(encoding='utf-8-sig')
-    req(info,['opt001cr3_inline_array_hotpath_candidate_v1','0x1c001c05','OPTIMIZATION_PARENT = "OPT-001CR2"','OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3"'],'build_info',errors)
-    req(arr,['function makeFilledArray','return array(count, value)','function makeEmptyArray'],'array_util',errors)
-    for forbidden in ['chunk = [value]','result = result + chunk','chunk = chunk + chunk']:
-        if forbidden in block(arr,'makeFilledArray'): errors.append('array_util retains concatenation fallback: '+forbidden)
-    req(client,['visibleBuilder = arrayutil.createArrayBuilder(c.MAX_VISEDICTS)','client.visibleEntities = arrayutil.finishArrayBuilder(visibleBuilder)'],'client',errors)
-    if 'client.visibleEntities = client.visibleEntities + [entity]' in block(client,'CL_RelinkEntities'): errors.append('CL_RelinkEntities retains concatenation')
-    req(particles,['commandBuffer = array(activeCount + 7, void)','aliveBuffer = array(activeCount, void)','system.active = arrays.copyArrayPrefix(aliveBuffer, aliveCount)'],'particles',errors)
-    pb=block(particles,'R_DrawParticles')
-    if 'commands = commands + [' in pb or 'alive = alive + [' in pb: errors.append('particle hotpath retains list concatenation')
-    req(diag,['if session.diagnosticContextPath != "" then','session.frameTrace = session.frameTrace + [stage]'],'diagnostics',errors)
-    if 'if session.diagnosticContextPath != "" then session.frameTrace = [] end if' not in host: errors.append('host frameTrace reset not guarded')
-    inline_targets={'src/miniquake/native.ml':['floatBits','bitsFloat'],'src/miniquake/mathlib.ml':['DotProduct'],'src/miniquake/client.ml':['clientFloat'],'src/miniquake/particles.ml':['particleFloat'],'src/miniquake/sound/snd_dma.ml':['soundF32'],'src/miniquake/sound/mixer.ml':['mixerF32'],'src/miniquake/render/gl_warp.ml':['warpFloat'],'src/miniquake/client_render_handoff.ml':['renderFloat'],'src/miniquake/render/gl_rmain.ml':['F32']}
-    inline_count=0
-    for rel,names in inline_targets.items():
-        text=(root/rel).read_text(encoding='utf-8-sig')
+    parser = argparse.ArgumentParser()
+    parser.add_argument('--root', default='.')
+    parser.add_argument('--json', default='')
+    args = parser.parse_args()
+    root = Path(args.root).resolve()
+    errors = []
+
+    info = (root/'src/miniquake/build_info.ml').read_text(encoding='utf-8-sig')
+    arr = (root/'src/miniquake/array_util.ml').read_text(encoding='utf-8-sig')
+    client = (root/'src/miniquake/client.ml').read_text(encoding='utf-8-sig')
+    particles = (root/'src/miniquake/particles.ml').read_text(encoding='utf-8-sig')
+    diag = (root/'src/miniquake/compat_diagnostics.ml').read_text(encoding='utf-8-sig')
+    host = (root/'src/miniquake/host.ml').read_text(encoding='utf-8-sig')
+    runner = (root/'TEST_OPT-001CR3R1.ps1').read_text(encoding='utf-8-sig')
+    collector = (root/'COLLECT_RESULTS.ps1').read_text(encoding='utf-8-sig')
+    launcher = (root/'test.ps1').read_text(encoding='utf-8-sig')
+    build = (root/'build.ps1').read_text(encoding='utf-8-sig')
+    live = (root/'tools/run_process_live.py').read_text(encoding='utf-8-sig')
+
+    req(info, [
+        'opt001cr3r1_compiler_safe_inline_candidate_v1', '0x1c001c06',
+        'OPTIMIZATION_PARENT = "OPT-001CR3"',
+        'OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3R1"',
+        'OPTIMIZATION_DELIVERY_PARENT = "OPT-001CR3"',
+    ], 'build_info', errors)
+    req(arr, ['function makeFilledArray', 'return array(count, value)', 'function makeEmptyArray'], 'array_util', errors)
+    for forbidden in ['chunk = [value]', 'result = result + chunk', 'chunk = chunk + chunk']:
+        if forbidden in block(arr, 'makeFilledArray'):
+            errors.append('array_util retains concatenation fallback: ' + forbidden)
+    req(client, [
+        'visibleBuilder = arrayutil.createArrayBuilder(c.MAX_VISEDICTS)',
+        'client.visibleEntities = arrayutil.finishArrayBuilder(visibleBuilder)',
+    ], 'client', errors)
+    if 'client.visibleEntities = client.visibleEntities + [entity]' in block(client, 'CL_RelinkEntities'):
+        errors.append('CL_RelinkEntities retains concatenation')
+    req(particles, [
+        'commandBuffer = array(activeCount + 7, void)',
+        'aliveBuffer = array(activeCount, void)',
+        'system.active = arrays.copyArrayPrefix(aliveBuffer, aliveCount)',
+    ], 'particles', errors)
+    particle_block = block(particles, 'R_DrawParticles')
+    if 'commands = commands + [' in particle_block or 'alive = alive + [' in particle_block:
+        errors.append('particle hotpath retains list concatenation')
+    req(diag, ['if session.diagnosticContextPath != "" then', 'session.frameTrace = session.frameTrace + [stage]'], 'diagnostics', errors)
+    if 'if session.diagnosticContextPath != "" then session.frameTrace = [] end if' not in host:
+        errors.append('host frameTrace reset not guarded')
+
+    required_inline = {
+        'src/miniquake/mathlib.ml': ['DotProduct'],
+        'src/miniquake/protocol15_freeze.ml': ['fingerprintValue'],
+        'src/miniquake/statusbar.ml': ['Sbar_ColorForMap'],
+    }
+    found = []
+    for rel, names in required_inline.items():
+        text = (root/rel).read_text(encoding='utf-8-sig')
         for name in names:
-            if not re.search(rf'^function inline {re.escape(name)}\s*\(',text,re.M): errors.append(f'{rel} missing inline {name}')
-            else: inline_count+=1
-    req(runner,['$DeliveryRevision = "OPT-001CR3"','output_mode=attached_console_live','MiniQuake OPT-001CR3 acceptance test: PASS','compare_opt001cr3_performance.py'],'runner',errors)
-    req(collector,['$DeliveryRevision = "OPT-001CR3"','MiniQuake_OPT-001CR3_RESULTS_','TEST_OPT-001CR3.ps1'],'collector',errors)
-    req(launcher,['TEST_OPT-001CR3.ps1'],'launcher',errors)
-    report={'schema':'MiniQuakeOPT001CR3Static/1','status':'PASS' if not errors else 'FAIL','errors':errors,'inline_functions':inline_count,'array_hotpaths':['array_util','visible_entities','particles','diagnostic_frame_trace','partial_ipv4']}
-    if a.json: Path(a.json).write_text(json.dumps(report,indent=2)+'\n',encoding='utf-8')
-    print('MiniQuake OPT-001CR3 inline/array hotpath verification: '+report['status'])
-    print('  inline_functions='+str(inline_count)); print('  optimized_hotpaths='+str(len(report['array_hotpaths'])))
-    for e in errors: print('  error: '+e)
+            if not re.search(rf'^function inline {re.escape(name)}\s*\(', text, re.M):
+                errors.append(f'{rel} missing compiler-safe inline {name}')
+            else:
+                found.append([rel, name])
+
+    forbidden_inline = {
+        'src/miniquake/native.ml': ['floatBits', 'bitsFloat'],
+        'src/miniquake/client.ml': ['clientFloat'],
+        'src/miniquake/particles.ml': ['particleFloat'],
+        'src/miniquake/sound/snd_dma.ml': ['soundF32'],
+        'src/miniquake/sound/mixer.ml': ['mixerF32'],
+        'src/miniquake/render/gl_warp.ml': ['warpFloat'],
+        'src/miniquake/client_render_handoff.ml': ['renderFloat'],
+        'src/miniquake/render/gl_rmain.ml': ['F32'],
+    }
+    for rel, names in forbidden_inline.items():
+        text = (root/rel).read_text(encoding='utf-8-sig')
+        for name in names:
+            if re.search(rf'^function inline {re.escape(name)}\s*\(', text, re.M):
+                errors.append(f'{rel} retains risky wrapper inline {name}')
+
+    all_inline = []
+    for path in sorted((root/'src').rglob('*.ml')):
+        text = path.read_text(encoding='utf-8-sig')
+        for match in re.finditer(r'(?m)^\s*function inline\s+([A-Za-z_][A-Za-z0-9_]*)\s*\(', text):
+            all_inline.append([path.relative_to(root).as_posix(), match.group(1)])
+    if sorted(all_inline) != sorted(found):
+        errors.append(f'inline budget mismatch: actual={all_inline!r} expected={found!r}')
+
+    req(runner, [
+        '$DeliveryRevision = "OPT-001CR3R1"', '$DeliveryParent = "OPT-001CR3"',
+        'output_mode=python_subprocess_live', 'tools\\run_process_live.py',
+        'dependent tests skipped because build failed', 'Show-LogTail',
+        'MiniQuake OPT-001CR3R1 acceptance test: PASS',
+        'compare_opt001cr3_performance.py',
+    ], 'runner', errors)
+    if 'Tee-Object' in runner:
+        errors.append('current runner still contains Tee-Object')
+    req(live, ['subprocess.Popen(', 'stderr=subprocess.STDOUT', 'sys.stdout.flush()', 'log.flush()', 'child_exit_code='], 'live process runner', errors)
+    req(collector, ['$DeliveryRevision = "OPT-001CR3R1"', 'MiniQuake_OPT-001CR3R1_RESULTS_', 'TEST_OPT-001CR3R1.ps1'], 'collector', errors)
+    req(launcher, ['TEST_OPT-001CR3R1.ps1'], 'launcher', errors)
+    hot_pos = build.find('tests\\opt001cr3_hotpath_tests.ml')
+    game_pos = build.find('$GameExe = Join-Path $Output "MiniQuake.exe"')
+    if hot_pos < 0 or game_pos < 0 or hot_pos > game_pos:
+        errors.append('build does not compile hotpath smoke before game')
+
+    report = {
+        'schema': 'MiniQuakeOPT001CR3R1Static/1',
+        'status': 'PASS' if not errors else 'FAIL',
+        'errors': errors,
+        'inline_functions': len(found),
+        'inline_set': found,
+        'risky_wrapper_inlines': 0,
+        'live_output': 'python_subprocess_live',
+        'dependent_test_policy': 'skip_after_build_failure',
+        'array_hotpaths': ['array_util', 'visible_entities', 'particles', 'diagnostic_frame_trace', 'partial_ipv4'],
+    }
+    if args.json:
+        Path(args.json).write_text(json.dumps(report, indent=2) + '\n', encoding='utf-8')
+    print('MiniQuake OPT-001CR3R1 compiler-safe inline/live-output verification: ' + report['status'])
+    print('  inline_functions=' + str(len(found)))
+    print('  live_output=python_subprocess_live')
+    for error in errors:
+        print('  error: ' + error)
     return 0 if not errors else 1
-if __name__=='__main__': raise SystemExit(main())
+
+
+if __name__ == '__main__':
+    raise SystemExit(main())
diff --git a/tools/run_process_live.py b/tools/run_process_live.py
new file mode 100755
index 0000000..fd8c755
--- /dev/null
+++ b/tools/run_process_live.py
@@ -0,0 +1,81 @@
+#!/usr/bin/env python3
+"""Run one child process with immediate console and UTF-8 log forwarding."""
+from __future__ import annotations
+
+import argparse
+import locale
+import os
+from pathlib import Path
+import subprocess
+import sys
+
+
+def main() -> int:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--log", required=True)
+    parser.add_argument("--cwd", default="")
+    parser.add_argument("command", nargs=argparse.REMAINDER)
+    args = parser.parse_args()
+    command = list(args.command)
+    if command and command[0] == "--":
+        command = command[1:]
+    if not command:
+        parser.error("missing command after --")
+
+    log_path = Path(args.log)
+    log_path.parent.mkdir(parents=True, exist_ok=True)
+    encoding = locale.getpreferredencoding(False) or "utf-8"
+    env = os.environ.copy()
+    env["PYTHONUNBUFFERED"] = "1"
+
+    with log_path.open("w", encoding="utf-8", newline="", buffering=1) as log:
+        log.write("MiniQuake live child-process log\n")
+        log.write("command=" + repr(command) + "\n")
+        log.flush()
+        try:
+            process = subprocess.Popen(
+                command,
+                cwd=args.cwd or None,
+                env=env,
+                stdout=subprocess.PIPE,
+                stderr=subprocess.STDOUT,
+                text=True,
+                encoding=encoding,
+                errors="replace",
+                bufsize=1,
+            )
+        except Exception as exc:
+            message = f"ERROR: could not start child process: {exc}\n"
+            sys.stderr.write(message)
+            sys.stderr.flush()
+            log.write(message)
+            return 127
+
+        assert process.stdout is not None
+        try:
+            for line in process.stdout:
+                sys.stdout.write(line)
+                sys.stdout.flush()
+                log.write(line)
+                log.flush()
+        except KeyboardInterrupt:
+            process.terminate()
+            try:
+                process.wait(timeout=5)
+            except subprocess.TimeoutExpired:
+                process.kill()
+            raise
+        finally:
+            process.stdout.close()
+
+        code = int(process.wait())
+        trailer = f"\nchild_exit_code={code}\n"
+        sys.stdout.write(trailer)
+        sys.stdout.flush()
+        log.write(trailer)
+        log.flush()
+        return code
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/tools/verify.py b/tools/verify.py
index 112aa19..f55de27 100644
--- a/tools/verify.py
+++ b/tools/verify.py
@@ -1,5 +1,5 @@
 #!/usr/bin/env python3
-"""Self-contained package verifier for MiniQuake OPT-001CR3.
+"""Self-contained package verifier for MiniQuake OPT-001CR3R1.
 
 Historical subsystem semantics are checked by the source-guided component
 checkers invoked from build.ps1.  This verifier owns current delivery
@@ -21,8 +21,8 @@ PACKAGE_ID = "BP-094"
 PARENT_PACKAGE_ID = "BP-093"
 BLOCK_ID = "BP-090-094"
 BLOCK_PARENT_PACKAGE_ID = "BP-085-089R8"
-DELIVERY_REVISION = "OPT-001CR3"
-DELIVERY_PARENT = "OPT-001CR2"
+DELIVERY_REVISION = "OPT-001CR3R1"
+DELIVERY_PARENT = "OPT-001CR3"
 MANIFEST = "SOURCE_MANIFEST.sha256"
 
 EXCLUDED_DIRS = {".git", "build", "__pycache__", ".pytest_cache"}
@@ -36,6 +36,11 @@ FORBIDDEN_NAMES = {
 }
 
 REQUIRED_PATHS = {
+    "TEST_OPT-001CR3R1.ps1", "CHANGELOG_OPT-001CR3R1.md",
+    "docs/OPT-001CR3R1_TESTING.md", "docs/OPT-001CR3R1_CHANGELOG.md",
+    "docs/OPT-001CR3R1_RESULT_ANALYSIS.md", "docs/OPT-001CR3R1_HOTFIX_REPORT.json",
+    "audit/opt001cr3r1_compiler_safe_inline.json", "tools/run_process_live.py",
+    "patches/OPT-001CR3R1.diff",
     "TEST_OPT-001CR3.ps1", "CHANGELOG_OPT-001CR3.md",
     "docs/OPT-001CR3_TESTING.md", "docs/OPT-001CR3_CHANGELOG.md",
     "docs/OPT-001CR3_DELIVERY_REPORT.json", "patches/OPT-001CR3.diff",
@@ -239,17 +244,17 @@ def check_identity(root: Path) -> Check:
     build_info = (root / "src/miniquake/build_info.ml").read_text(encoding="utf-8-sig")
     main = (root / "src/main.ml").read_text(encoding="utf-8-sig")
     build = (root / "build.ps1").read_text(encoding="utf-8-sig")
-    test = (root / "TEST_OPT-001CR3.ps1").read_text(encoding="utf-8-sig")
+    test = (root / "TEST_OPT-001CR3R1.ps1").read_text(encoding="utf-8-sig")
     collector = (root / "COLLECT_RESULTS.ps1").read_text(encoding="utf-8-sig")
     launcher = (root / "test.ps1").read_text(encoding="utf-8-sig")
     errors += marker_errors(build_info, [
         'const PACKAGE_ID = "BP-094"', 'const PARENT_PACKAGE_ID = "BP-093"',
         'const BLOCK_ID = "BP-090-094"', 'const BLOCK_PARENT_PACKAGE_ID = "BP-085-089R8"',
-        'const OPTIMIZATION_STATUS = "opt001cr3_inline_array_hotpath_candidate_v1"',
-        'const OPTIMIZATION_FINGERPRINT = 0x1c001c05',
-        'const OPTIMIZATION_PARENT = "OPT-001CR2"',
-        'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3"',
-        'const OPTIMIZATION_DELIVERY_PARENT = "OPT-001CR2"',
+        'const OPTIMIZATION_STATUS = "opt001cr3r1_compiler_safe_inline_candidate_v1"',
+        'const OPTIMIZATION_FINGERPRINT = 0x1c001c06',
+        'const OPTIMIZATION_PARENT = "OPT-001CR3"',
+        'const OPTIMIZATION_DELIVERY_REVISION = "OPT-001CR3R1"',
+        'const OPTIMIZATION_DELIVERY_PARENT = "OPT-001CR3"',
     ], "build_info")
     errors += marker_errors(main, [
         "--opt001a-map-parse", "--opt001a-frame-baseline", "--opt001a-handle-plateau", "--opt001b-transition",
@@ -260,14 +265,14 @@ def check_identity(root: Path) -> Check:
         "MiniQuakeOPT001CR3HotpathTests.exe", "tests\\opt001cr3_hotpath_tests.ml",
     ], "build")
     errors += marker_errors(test, [
-        '$DeliveryRevision = "OPT-001CR3"', '$DeliveryParent = "OPT-001CR2"',
-        "MiniQuake OPT-001CR3 acceptance test: PASS", "output_mode=attached_console_live",
+        '$DeliveryRevision = "OPT-001CR3R1"', '$DeliveryParent = "OPT-001CR3"',
+        "MiniQuake OPT-001CR3R1 acceptance test: PASS", "output_mode=python_subprocess_live",
         "--opt001a-map-parse", "--opt001a-frame-baseline", "--opt001a-handle-plateau",
     ], "test")
     errors += marker_errors(collector, [
-        '$DeliveryRevision = "OPT-001CR3"', "MiniQuake_OPT-001CR3_RESULTS_",
+        '$DeliveryRevision = "OPT-001CR3R1"', "MiniQuake_OPT-001CR3R1_RESULTS_",
     ], "collector")
-    errors += marker_errors(launcher, ["TEST_OPT-001CR3.ps1"], "test.ps1")
+    errors += marker_errors(launcher, ["TEST_OPT-001CR3R1.ps1"], "test.ps1")
     return Check(
         "package_identity", not errors,
         {"package_id": PACKAGE_ID, "parent_package_id": PARENT_PACKAGE_ID,
@@ -922,43 +927,34 @@ def check_external_contract(root: Path) -> Check:
 def check_live_output(root: Path) -> Check:
     errors: list[str] = []
     historical_test = (root / "TEST_BP-090-094R7.ps1").read_text(encoding="utf-8-sig")
-    current_test = (root / "TEST_OPT-001CR3.ps1").read_text(encoding="utf-8-sig")
+    current_test = (root / "TEST_OPT-001CR3R1.ps1").read_text(encoding="utf-8-sig")
+    live_runner = (root / "tools/run_process_live.py").read_text(encoding="utf-8-sig")
     build = (root / "build.ps1").read_text(encoding="utf-8-sig")
     for label, script_text in (("historical test", historical_test), ("current test", current_test), ("build", build)):
         if "PYTHONUNBUFFERED" not in script_text:
             errors.append(f"{label} script does not force unbuffered Python")
-    for label, script_text in (("historical test", historical_test), ("build", build)):
-        if "Write-Host $Line" not in script_text:
-            errors.append(f"{label} script does not forward lines live")
     if "@(& $Executable @Arguments 2>&1)" in historical_test:
         errors.append("historical acceptance runner reintroduced full-process output buffering")
-
-    current_markers = [
-        "function Invoke-LivePowerShellScript(",
-        "output_mode=attached_console_live",
-        "*>&1 | Tee-Object -FilePath",
-        "-EncodedCommand",
-        "& $PowerShellExe -NoProfile -ExecutionPolicy Bypass -EncodedCommand $EncodedCommand",
+    required = [
+        "tools\\run_process_live.py", "output_mode=python_subprocess_live",
+        "Invoke-ExternalProcessLive", "dependent tests skipped because build failed",
+        "Show-LogTail", "Get-Content -LiteralPath $LogPath -Tail",
     ]
-    for marker in current_markers:
-        if marker not in current_test:
-            errors.append(f"current OPT-001CR2 runner missing live-build marker: {marker}")
-    if '[void](Invoke-Live "OPT-001CR2 game and contract build" $PowerShellExe' in current_test:
-        errors.append("current OPT-001CR2 runner still pipes nested PowerShell through parent Invoke-Live")
+    errors += marker_errors(current_test, required, "current runner")
+    if "Tee-Object" in current_test:
+        errors.append("current runner reintroduced a PowerShell Tee-Object pipeline")
+    errors += marker_errors(live_runner, [
+        "subprocess.Popen(", "stderr=subprocess.STDOUT", "sys.stdout.flush()",
+        "log.flush()", "child_exit_code=",
+    ], "live process runner")
     return Check(
-        "live_output",
-        not errors,
-        {
-            "python_unbuffered": True,
-            "current_build_output_mode": "attached_console_live",
-            "child_side_logging": True,
-        },
+        "live_output", not errors,
+        {"python_unbuffered": True, "current_build_output_mode": "python_subprocess_live", "dependent_test_barrier": True},
         errors,
     )
 
 
 
-
 def check_powershell_interpolation_safety(root: Path) -> Check:
     """Reject the `$Variable:` parser trap inside expandable strings.
 
@@ -1548,14 +1544,14 @@ def main() -> int:
         check_external_checkers(root),
     ]
     passed = all(check.passed for check in checks)
-    print("MiniQuake OPT-001CR3 verification")
+    print("MiniQuake OPT-001CR3R1 verification")
     for check in checks:
         print(f"  [{'PASS' if check.passed else 'FAIL'}] {check.name}")
         for key, value in check.details.items():
             print(f"         {key}={value}")
         for error in check.errors:
             print(f"         error: {error}")
-    print(f"MiniQuake OPT-001CR3 verification: {'PASS' if passed else 'FAIL'}")
+    print(f"MiniQuake OPT-001CR3R1 verification: {'PASS' if passed else 'FAIL'}")
     report = {
         "schema_version": 1,
         "package_id": PACKAGE_ID,
