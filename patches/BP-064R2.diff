diff --git a/BLOCK_LEDGER.json b/BLOCK_LEDGER.json
index 01b80855eaad1399dde173de3f2f7b79068a574f..df48ad74b734fc845633e861a0cdc691809b8523 100644
--- a/BLOCK_LEDGER.json
+++ b/BLOCK_LEDGER.json
@@ -4,7 +4,7 @@
   "block_id": "BP-060-064",
   "parent_package_id": "BP-055-059R2",
   "final_package_id": "BP-064",
-  "delivery_revision": "BP-060-064R1",
+  "delivery_revision": "BP-060-064R2",
   "network_platform_status": "network_platform_109_frozen_v1",
   "network_platform_fingerprint": "0xb3ec7589",
   "steps": [
@@ -13,35 +13,35 @@
       "title": "NET main, qsocket pool, poll queue and active host port",
       "patch": "patches/BP-060.diff",
       "runtime_fixtures": 20,
-      "status": "implemented; Windows acceptance pending"
+      "status": "Windows runtime 20/20 PASS in predecessor run"
     },
     {
       "package_id": "BP-061",
       "title": "Protocol 3 control, discovery and connection classification",
       "patch": "patches/BP-061.diff",
       "runtime_fixtures": 24,
-      "status": "implemented; Windows acceptance pending"
+      "status": "R2 production and fixture correction; Windows revalidation pending"
     },
     {
       "package_id": "BP-062",
       "title": "WinSock address, partial IP and landriver parity",
       "patch": "patches/BP-062.diff",
       "runtime_fixtures": 24,
-      "status": "implemented; Windows acceptance pending"
+      "status": "compiled in predecessor run; runtime execution pending BP-061 repair"
     },
     {
       "package_id": "BP-063",
       "title": "sys_win, QHOST/conproc and WinMain lifecycle",
       "patch": "patches/BP-063.diff",
       "runtime_fixtures": 21,
-      "status": "implemented; Windows acceptance pending"
+      "status": "compiled in predecessor run; runtime execution pending BP-061 repair"
     },
     {
       "package_id": "BP-064",
       "title": "network/platform closure and cross-process UDP evidence",
       "patch": "patches/BP-064.diff",
       "runtime_fixtures": 24,
-      "status": "implemented; Windows acceptance pending"
+      "status": "compiled in predecessor run; runtime execution pending BP-061 repair"
     }
   ],
   "block_runtime_fixtures": 113,
@@ -75,12 +75,58 @@
     "serial_modem_ipx": "legacy transports remain outside the mandatory modern Windows profile",
     "quake_game_data": "external; never bundled"
   },
-  "delivery_parent": "BP-060-064",
+  "delivery_parent": "BP-060-064R1",
   "delivery_hotfix": {
-    "scope": "test/build infrastructure only",
+    "revision": "BP-060-064R2",
+    "parent": "BP-060-064R1",
+    "scope": "Protocol-3 rule enumeration plus runtime fixture; inherits live output",
     "live_output": true,
     "python_unbuffered": true,
-    "production_src_changed": false,
+    "rule_end_wire": "command_only",
+    "unknown_previous": "no_reply",
+    "production_src_changed": true,
     "native_changed": false
+  },
+  "initial_delivery_result": {
+    "delivery_revision": "BP-060-064",
+    "result_archive": "MiniQuake_BP-060-064_RESULTS_20260728-184449.zip",
+    "result_archive_sha256": "bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719",
+    "status": "FAIL during BP-061 runtime fixture",
+    "compiled_targets": "65/65",
+    "bp060": "20/20 PASS",
+    "bp061": "19 fixtures completed; void indexing before fixture 20",
+    "later_runtime": "not reached"
+  },
+  "r1_delivery_hotfix": {
+    "revision": "BP-060-064R1",
+    "parent": "BP-060-064",
+    "scope": "live output and logging infrastructure",
+    "windows_run": "not executed separately",
+    "production_source_changed": false
+  },
+  "r2_delivery_hotfix": {
+    "revision": "BP-060-064R2",
+    "parent": "BP-060-064R1",
+    "scope": "Protocol-3 rule enumeration distinction plus BP-061 fixture",
+    "rule_end_wire": "command_only",
+    "unknown_previous": "no_reply",
+    "live_output": true,
+    "production_source_changed": true,
+    "native_changed": false,
+    "windows_acceptance": "pending"
+  },
+  "predecessor_result": {
+    "archive": "MiniQuake_BP-060-064_RESULTS_20260728-184449.zip",
+    "sha256": "bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719",
+    "status": "FAIL at BP-061 fixture 20 after 65/65 targets compiled",
+    "bp060": "20/20 PASS",
+    "first_failure": "Cannot index void in rule enumeration terminator fixture"
+  },
+  "r2_hotfix": {
+    "live_output_carried_forward": true,
+    "rule_end": "command-only CCREP_RULE_INFO",
+    "unknown_previous": "silent no reply",
+    "runtime_fixtures": 113,
+    "contract_fingerprint": "0xb3ec7589"
   }
 }
diff --git a/CHANGELOG_BP-060-064R2.md b/CHANGELOG_BP-060-064R2.md
new file mode 100644
index 0000000000000000000000000000000000000000..98214d2bca679917f85e0d64273984fb14e2e9f6
--- /dev/null
+++ b/CHANGELOG_BP-060-064R2.md
@@ -0,0 +1,24 @@
+# MiniQuake BP-060–BP-064R2
+
+Delivery parent: `BP-060-064R1`  
+Engine package: `BP-064`
+
+## Changes
+
+- Carry forward the R1 live-output PowerShell runner; child-process output is no longer buffered until completion.
+- Add the explicit `TEST_BP-060-064R2.ps1` entry and R2 log/result names.
+- Fix Protocol-3 rule enumeration:
+  - return an empty name/value pair for the command-only end marker;
+  - silently ignore an unknown previous rule, as `net_dgrm.c` does;
+  - prevent indexing `void` in the BP-061 runtime fixture.
+- Extend the BP-061 C oracle, golden metadata and static checker with end-marker and no-reply cases.
+- Update collector, ledger, manifest and result-analysis metadata for R2.
+
+## Compatibility status
+
+No native ABI changes. The candidate contract remains:
+
+```text
+network_platform_109_frozen_v1
+fingerprint=0xb3ec7589
+```
diff --git a/COLLECT_RESULTS.ps1 b/COLLECT_RESULTS.ps1
index 745c8c46e29f87e35c5ddb3d5c1e7baf9af99851..8acbf80a7bbff72593a5b6f44e9b147ffd5a237a 100755
--- a/COLLECT_RESULTS.ps1
+++ b/COLLECT_RESULTS.ps1
@@ -1,219 +1,227 @@
-[CmdletBinding()]
-param([switch]$IncludeAllCurrentDocs)
-
-$ErrorActionPreference = "Stop"
-Set-StrictMode -Version Latest
-$Root = $PSScriptRoot
-$Build = Join-Path $Root "build"
-$PackageId = "BP-064"
-$BlockId = "BP-060-064"
-$DeliveryRevision = "BP-060-064R1"
-$DeliveryParent = "BP-060-064"
-$Timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
-$Staging = Join-Path $Build (".bp060-064r1-results-{0}" -f $Timestamp)
-$Archive = Join-Path $Build ("MiniQuake_BP-060-064R1_RESULTS_{0}.zip" -f $Timestamp)
-
-if (-not (Test-Path -LiteralPath $Build -PathType Container)) { throw "build directory does not exist: $Build" }
-if (Test-Path -LiteralPath $Staging) { Remove-Item -Recurse -Force -LiteralPath $Staging }
-New-Item -ItemType Directory -Force -Path $Staging | Out-Null
-
-$AllowedBuildExtensions = @(".log", ".json", ".txt", ".csv", ".mqtrace", ".tga")
-$ForbiddenNames = @("pak0.pak", "pak1.pak", "progs.dat", "config.cfg", "autoexec.cfg")
-
-function Copy-SafeFile([string]$Source, [string]$Relative) {
-  if (-not (Test-Path -LiteralPath $Source -PathType Leaf)) { return }
-  $Leaf = [IO.Path]::GetFileName($Source).ToLowerInvariant()
-  if ($ForbiddenNames -contains $Leaf) { throw "refusing to collect Quake game data: $Source" }
-  $Normalized = $Relative.Replace('/', '\')
-  if ($Normalized -match '(?i)(^|\\)id1(\\|$)') { throw "refusing to collect id1 content: $Relative" }
-  $Destination = Join-Path $Staging $Normalized
-  New-Item -ItemType Directory -Force -Path (Split-Path -Parent $Destination) | Out-Null
-  Copy-Item -Force -LiteralPath $Source -Destination $Destination
-}
-
-Get-ChildItem -LiteralPath $Build -Recurse -File | ForEach-Object {
-  if ($_.FullName.StartsWith($Staging, [StringComparison]::OrdinalIgnoreCase)) { return }
-  if ($_.FullName -eq $Archive) { return }
-  if ($AllowedBuildExtensions -contains $_.Extension.ToLowerInvariant()) {
-    $Relative = $_.FullName.Substring($Build.Length).TrimStart('\', '/')
-    Copy-SafeFile $_.FullName (Join-Path "build" $Relative)
-  }
-}
-
-$RootFiles = @(
-  "SOURCE_MANIFEST.sha256",
-  "BLOCK_LEDGER.json",
-  "PORT_LEDGER.json",
-  "PORT_STATUS.md",
-  "README.md",
-  "build.ps1",
-  "COLLECT_RESULTS.ps1",
-  "test.ps1",
-  "TEST_BP-055-059.ps1",
-  "TEST_BP-055-059R1.ps1",
-  "TEST_BP-055-059R2.ps1",
-  "CHANGELOG_BP-055.md",
-  "CHANGELOG_BP-056.md",
-  "CHANGELOG_BP-057.md",
-  "CHANGELOG_BP-058.md",
-  "CHANGELOG_BP-059.md",
-  "CHANGELOG_BP-055-059.md",
-  "CHANGELOG_BP-055-059R1.md",
-  "CHANGELOG_BP-055-059R2.md",
-  "docs\BP-050-054R1_ACCEPTANCE_ANALYSIS.md",
-  "docs\BP-055_AUDIO_MEMORY_AUDIT.md",
-  "docs\BP-056_AUDIO_DMA_AUDIT.md",
-  "docs\BP-057_AUDIO_MIXER_AUDIT.md",
-  "docs\BP-058_AUDIO_WIN_AUDIT.md",
-  "docs\BP-059_AUDIO_CLOSURE_AUDIT.md",
-  "docs\BP-055-059_TESTING.md",
-  "docs\BP-055-059_BLOCK_LEDGER.json",
-  "docs\BP-055-059_RESULT_ANALYSIS.md",
-  "docs\BP-055-059R1_TESTING.md",
-  "docs\BP-055-059R1_BLOCK_LEDGER.json",
-  "docs\BP-055-059R1_HOTFIX_REPORT.json",
-  "docs\BP-055-059R1_RESULT_ANALYSIS.md",
-  "docs\BP-055-059R2_TESTING.md",
-  "docs\BP-055-059R2_BLOCK_LEDGER.json",
-  "docs\BP-055-059R2_HOTFIX_REPORT.json",
-  "patches\BP-055.diff",
-  "patches\BP-056.diff",
-  "patches\BP-057.diff",
-  "patches\BP-058.diff",
-  "patches\BP-059.diff",
-  "patches\BP-059R1.diff",
-  "patches\BP-059R2.diff",
-  "tools\check_render_special_054.py",
-  "tools\check_audio_055.py",
-  "tools\check_audio_056.py",
-  "tools\check_audio_057.py",
-  "tools\check_audio_058.py",
-  "tools\check_audio_059.py",
-  "tools\verify.py",
-  "tools\oracle\audio_memory_oracle.c",
-  "tools\oracle\audio_dma_oracle.c",
-  "tools\oracle\audio_mixer_oracle.c",
-  "tools\oracle\audio_win_oracle.c",
-  "tools\oracle\audio_closure_oracle.c",
-  "audit\audio_memory_golden.json",
-  "audit\audio_dma_golden.json",
-  "audit\audio_mixer_golden.json",
-  "audit\audio_win_golden.json",
-  "audit\audio_closure_golden.json",
-  "tests\audio_memory_parity_tests.ml",
-  "tests\audio_dma_parity_tests.ml",
-  "tests\audio_mixer_parity_tests.ml",
-  "tests\audio_win_parity_tests.ml",
-  "tests\audio_closure_tests.ml",
-  "tests\audio_retail_evidence.ml",
-  # Historical BP-012 source-contract evidence: keep these exact markers.
-  "tests\protocol15_serverdata_tests.ml",
-  "docs\BP-012_PROTOCOL15_SERVERDATA_AUDIT.md",
-  "audit\protocol15_serverdata_golden.json",
-  "TEST_BP-060-064.ps1",
-  "TEST_BP-060-064R1.ps1",
-  "CHANGELOG_BP-060-064R1.md",
-  "docs\BP-060-064R1_TESTING.md",
-  "docs\BP-060-064R1_RESULT_ANALYSIS.md",
-  "docs\BP-060-064R1_HOTFIX_REPORT.json",
-  "docs\BP-060-064R1_BLOCK_LEDGER.json",
-  "patches\BP-064R1.diff",
-  "CHANGELOG_BP-060.md",
-  "CHANGELOG_BP-061.md",
-  "CHANGELOG_BP-062.md",
-  "CHANGELOG_BP-063.md",
-  "CHANGELOG_BP-064.md",
-  "CHANGELOG_BP-060-064.md",
-  "docs\BP-055-059R2_ACCEPTANCE_ANALYSIS.md",
-  "docs\BP-060_NETWORK_MAIN_AUDIT.md",
-  "docs\BP-061_NETWORK_CONTROL_AUDIT.md",
-  "docs\BP-062_WINS_ADDRESS_AUDIT.md",
-  "docs\BP-063_SYSTEM_PLATFORM_AUDIT.md",
-  "docs\BP-064_NETWORK_PLATFORM_CLOSURE_AUDIT.md",
-  "docs\BP-060-064_TESTING.md",
-  "docs\BP-060-064_BLOCK_LEDGER.json",
-  "patches\BP-060.diff",
-  "patches\BP-061.diff",
-  "patches\BP-062.diff",
-  "patches\BP-063.diff",
-  "patches\BP-064.diff",
-  "tools\check_network_060.py",
-  "tools\check_network_061.py",
-  "tools\check_network_062.py",
-  "tools\check_network_063.py",
-  "tools\check_network_064.py",
-  "tools\oracle\network_main_oracle.c",
-  "tools\oracle\network_control_oracle.c",
-  "tools\oracle\network_wins_oracle.c",
-  "tools\oracle\system_platform_oracle.c",
-  "tools\oracle\network_platform_closure_oracle.c",
-  "audit\network_main_golden.json",
-  "audit\network_control_golden.json",
-  "audit\network_wins_golden.json",
-  "audit\system_platform_golden.json",
-  "audit\network_platform_closure_golden.json",
-  "tests\network_main_parity_tests.ml",
-  "tests\network_control_parity_tests.ml",
-  "tests\network_wins_address_tests.ml",
-  "tests\system_platform_lifecycle_tests.ml",
-  "tests\network_platform_closure_tests.ml",
-  "tests\network_platform_evidence.ml",
-  "src\miniquake\network_platform_contract.ml"
-)
-foreach ($Relative in $RootFiles) { Copy-SafeFile (Join-Path $Root $Relative) $Relative }
-
-# Preserve all current block documentation and checker metadata when requested.
-if ($IncludeAllCurrentDocs) {
-  Get-ChildItem -LiteralPath (Join-Path $Root "docs") -File | ForEach-Object { Copy-SafeFile $_.FullName (Join-Path "docs" $_.Name) }
-}
-
-$BinaryNames = @(
-  "MiniQuake.exe",
-  "MiniQuakeProtocol15ServerDataTests.exe",
-  "MiniQuakeAudioMemoryTests.exe",
-  "MiniQuakeAudioDmaTests.exe",
-  "MiniQuakeAudioMixerTests.exe",
-  "MiniQuakeAudioWinTests.exe",
-  "MiniQuakeAudioClosureTests.exe",
-  "MiniQuakeAudioRetailEvidence.exe",
-  "MiniQuakeNetworkMainTests.exe",
-  "MiniQuakeNetworkControlTests.exe",
-  "MiniQuakeNetworkWinsAddressTests.exe",
-  "MiniQuakeSystemPlatformTests.exe",
-  "MiniQuakeNetworkPlatformClosureTests.exe",
-  "MiniQuakeNetworkPlatformEvidence.exe",
-  "miniquake_native.dll",
-  "miniquake_text.dll"
-)
-$BinaryState = @()
-foreach ($Name in $BinaryNames) {
-  $Path = Join-Path $Build $Name
-  $Entry = [ordered]@{ name = $Name; present = (Test-Path -LiteralPath $Path -PathType Leaf); sha256 = ""; bytes = 0 }
-  if ($Entry.present) {
-    $Entry.sha256 = (Get-FileHash -Algorithm SHA256 -LiteralPath $Path).Hash.ToLowerInvariant()
-    $Entry.bytes = (Get-Item -LiteralPath $Path).Length
-  }
-  $BinaryState += $Entry
-}
-$Meta = [ordered]@{
-  schema = "MiniQuakeResultCollection/1"
-  package_id = $PackageId
-  block_id = $BlockId
-  delivery_revision = $DeliveryRevision
-  delivery_parent = $DeliveryParent
-  created_utc = (Get-Date).ToUniversalTime().ToString("o")
-  machine = $env:COMPUTERNAME
-  user = $env:USERNAME
-  source_root = $Root
-  binaries = $BinaryState
-}
-$Meta | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath (Join-Path $Staging "collection.json") -Encoding UTF8
-
-if (Test-Path -LiteralPath $Archive) { Remove-Item -Force -LiteralPath $Archive }
-Compress-Archive -Path (Join-Path $Staging "*") -DestinationPath $Archive -CompressionLevel Optimal
-Remove-Item -Recurse -Force -LiteralPath $Staging
-$Hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $Archive).Hash.ToLowerInvariant()
-Write-Host "MiniQuake $DeliveryRevision result archive"
-Write-Host "  path=$Archive"
-Write-Host "  sha256=$Hash"
-Write-Host "  Quake PAK/model/map/audio data and compiled binaries were not included."
+[CmdletBinding()]
+param([switch]$IncludeAllCurrentDocs)
+
+$ErrorActionPreference = "Stop"
+Set-StrictMode -Version Latest
+$Root = $PSScriptRoot
+$Build = Join-Path $Root "build"
+$PackageId = "BP-064"
+$BlockId = "BP-060-064"
+$DeliveryRevision = "BP-060-064R2"
+$DeliveryParent = "BP-060-064R1"
+$Timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
+$Staging = Join-Path $Build (".bp060-064r2-results-{0}" -f $Timestamp)
+$Archive = Join-Path $Build ("MiniQuake_BP-060-064R2_RESULTS_{0}.zip" -f $Timestamp)
+
+if (-not (Test-Path -LiteralPath $Build -PathType Container)) { throw "build directory does not exist: $Build" }
+if (Test-Path -LiteralPath $Staging) { Remove-Item -Recurse -Force -LiteralPath $Staging }
+New-Item -ItemType Directory -Force -Path $Staging | Out-Null
+
+$AllowedBuildExtensions = @(".log", ".json", ".txt", ".csv", ".mqtrace", ".tga")
+$ForbiddenNames = @("pak0.pak", "pak1.pak", "progs.dat", "config.cfg", "autoexec.cfg")
+
+function Copy-SafeFile([string]$Source, [string]$Relative) {
+  if (-not (Test-Path -LiteralPath $Source -PathType Leaf)) { return }
+  $Leaf = [IO.Path]::GetFileName($Source).ToLowerInvariant()
+  if ($ForbiddenNames -contains $Leaf) { throw "refusing to collect Quake game data: $Source" }
+  $Normalized = $Relative.Replace('/', '\')
+  if ($Normalized -match '(?i)(^|\\)id1(\\|$)') { throw "refusing to collect id1 content: $Relative" }
+  $Destination = Join-Path $Staging $Normalized
+  New-Item -ItemType Directory -Force -Path (Split-Path -Parent $Destination) | Out-Null
+  Copy-Item -Force -LiteralPath $Source -Destination $Destination
+}
+
+Get-ChildItem -LiteralPath $Build -Recurse -File | ForEach-Object {
+  if ($_.FullName.StartsWith($Staging, [StringComparison]::OrdinalIgnoreCase)) { return }
+  if ($_.FullName -eq $Archive) { return }
+  if ($AllowedBuildExtensions -contains $_.Extension.ToLowerInvariant()) {
+    $Relative = $_.FullName.Substring($Build.Length).TrimStart('\', '/')
+    Copy-SafeFile $_.FullName (Join-Path "build" $Relative)
+  }
+}
+
+$RootFiles = @(
+  "SOURCE_MANIFEST.sha256",
+  "BLOCK_LEDGER.json",
+  "PORT_LEDGER.json",
+  "PORT_STATUS.md",
+  "README.md",
+  "build.ps1",
+  "COLLECT_RESULTS.ps1",
+  "test.ps1",
+  "TEST_BP-055-059.ps1",
+  "TEST_BP-055-059R1.ps1",
+  "TEST_BP-055-059R2.ps1",
+  "CHANGELOG_BP-055.md",
+  "CHANGELOG_BP-056.md",
+  "CHANGELOG_BP-057.md",
+  "CHANGELOG_BP-058.md",
+  "CHANGELOG_BP-059.md",
+  "CHANGELOG_BP-055-059.md",
+  "CHANGELOG_BP-055-059R1.md",
+  "CHANGELOG_BP-055-059R2.md",
+  "docs\BP-050-054R1_ACCEPTANCE_ANALYSIS.md",
+  "docs\BP-055_AUDIO_MEMORY_AUDIT.md",
+  "docs\BP-056_AUDIO_DMA_AUDIT.md",
+  "docs\BP-057_AUDIO_MIXER_AUDIT.md",
+  "docs\BP-058_AUDIO_WIN_AUDIT.md",
+  "docs\BP-059_AUDIO_CLOSURE_AUDIT.md",
+  "docs\BP-055-059_TESTING.md",
+  "docs\BP-055-059_BLOCK_LEDGER.json",
+  "docs\BP-055-059_RESULT_ANALYSIS.md",
+  "docs\BP-055-059R1_TESTING.md",
+  "docs\BP-055-059R1_BLOCK_LEDGER.json",
+  "docs\BP-055-059R1_HOTFIX_REPORT.json",
+  "docs\BP-055-059R1_RESULT_ANALYSIS.md",
+  "docs\BP-055-059R2_TESTING.md",
+  "docs\BP-055-059R2_BLOCK_LEDGER.json",
+  "docs\BP-055-059R2_HOTFIX_REPORT.json",
+  "patches\BP-055.diff",
+  "patches\BP-056.diff",
+  "patches\BP-057.diff",
+  "patches\BP-058.diff",
+  "patches\BP-059.diff",
+  "patches\BP-059R1.diff",
+  "patches\BP-059R2.diff",
+  "tools\check_render_special_054.py",
+  "tools\check_audio_055.py",
+  "tools\check_audio_056.py",
+  "tools\check_audio_057.py",
+  "tools\check_audio_058.py",
+  "tools\check_audio_059.py",
+  "tools\verify.py",
+  "tools\oracle\audio_memory_oracle.c",
+  "tools\oracle\audio_dma_oracle.c",
+  "tools\oracle\audio_mixer_oracle.c",
+  "tools\oracle\audio_win_oracle.c",
+  "tools\oracle\audio_closure_oracle.c",
+  "audit\audio_memory_golden.json",
+  "audit\audio_dma_golden.json",
+  "audit\audio_mixer_golden.json",
+  "audit\audio_win_golden.json",
+  "audit\audio_closure_golden.json",
+  "tests\audio_memory_parity_tests.ml",
+  "tests\audio_dma_parity_tests.ml",
+  "tests\audio_mixer_parity_tests.ml",
+  "tests\audio_win_parity_tests.ml",
+  "tests\audio_closure_tests.ml",
+  "tests\audio_retail_evidence.ml",
+  # Historical BP-012 source-contract evidence: keep these exact markers.
+  "tests\protocol15_serverdata_tests.ml",
+  "docs\BP-012_PROTOCOL15_SERVERDATA_AUDIT.md",
+  "audit\protocol15_serverdata_golden.json",
+  "TEST_BP-060-064.ps1",
+  "TEST_BP-060-064R1.ps1",
+  "TEST_BP-060-064R2.ps1",
+  "CHANGELOG_BP-060-064R1.md",
+  "CHANGELOG_BP-060-064R2.md",
+  "docs\BP-060-064R1_TESTING.md",
+  "docs\BP-060-064_RESULT_ANALYSIS.md",
+  "docs\BP-060-064R2_TESTING.md",
+  "docs\BP-060-064R2_RESULT_ANALYSIS.md",
+  "docs\BP-060-064R2_HOTFIX_REPORT.json",
+  "docs\BP-060-064R2_BLOCK_LEDGER.json",
+  "docs\BP-060-064R1_RESULT_ANALYSIS.md",
+  "docs\BP-060-064R1_HOTFIX_REPORT.json",
+  "docs\BP-060-064R1_BLOCK_LEDGER.json",
+  "patches\BP-064R1.diff",
+  "patches\BP-064R2.diff",
+  "CHANGELOG_BP-060.md",
+  "CHANGELOG_BP-061.md",
+  "CHANGELOG_BP-062.md",
+  "CHANGELOG_BP-063.md",
+  "CHANGELOG_BP-064.md",
+  "CHANGELOG_BP-060-064.md",
+  "docs\BP-055-059R2_ACCEPTANCE_ANALYSIS.md",
+  "docs\BP-060_NETWORK_MAIN_AUDIT.md",
+  "docs\BP-061_NETWORK_CONTROL_AUDIT.md",
+  "docs\BP-062_WINS_ADDRESS_AUDIT.md",
+  "docs\BP-063_SYSTEM_PLATFORM_AUDIT.md",
+  "docs\BP-064_NETWORK_PLATFORM_CLOSURE_AUDIT.md",
+  "docs\BP-060-064_TESTING.md",
+  "docs\BP-060-064_BLOCK_LEDGER.json",
+  "patches\BP-060.diff",
+  "patches\BP-061.diff",
+  "patches\BP-062.diff",
+  "patches\BP-063.diff",
+  "patches\BP-064.diff",
+  "tools\check_network_060.py",
+  "tools\check_network_061.py",
+  "tools\check_network_062.py",
+  "tools\check_network_063.py",
+  "tools\check_network_064.py",
+  "tools\oracle\network_main_oracle.c",
+  "tools\oracle\network_control_oracle.c",
+  "tools\oracle\network_wins_oracle.c",
+  "tools\oracle\system_platform_oracle.c",
+  "tools\oracle\network_platform_closure_oracle.c",
+  "audit\network_main_golden.json",
+  "audit\network_control_golden.json",
+  "audit\network_wins_golden.json",
+  "audit\system_platform_golden.json",
+  "audit\network_platform_closure_golden.json",
+  "tests\network_main_parity_tests.ml",
+  "tests\network_control_parity_tests.ml",
+  "tests\network_wins_address_tests.ml",
+  "tests\system_platform_lifecycle_tests.ml",
+  "tests\network_platform_closure_tests.ml",
+  "tests\network_platform_evidence.ml",
+  "src\miniquake\network_platform_contract.ml"
+)
+foreach ($Relative in $RootFiles) { Copy-SafeFile (Join-Path $Root $Relative) $Relative }
+
+# Preserve all current block documentation and checker metadata when requested.
+if ($IncludeAllCurrentDocs) {
+  Get-ChildItem -LiteralPath (Join-Path $Root "docs") -File | ForEach-Object { Copy-SafeFile $_.FullName (Join-Path "docs" $_.Name) }
+}
+
+$BinaryNames = @(
+  "MiniQuake.exe",
+  "MiniQuakeProtocol15ServerDataTests.exe",
+  "MiniQuakeAudioMemoryTests.exe",
+  "MiniQuakeAudioDmaTests.exe",
+  "MiniQuakeAudioMixerTests.exe",
+  "MiniQuakeAudioWinTests.exe",
+  "MiniQuakeAudioClosureTests.exe",
+  "MiniQuakeAudioRetailEvidence.exe",
+  "MiniQuakeNetworkMainTests.exe",
+  "MiniQuakeNetworkControlTests.exe",
+  "MiniQuakeNetworkWinsAddressTests.exe",
+  "MiniQuakeSystemPlatformTests.exe",
+  "MiniQuakeNetworkPlatformClosureTests.exe",
+  "MiniQuakeNetworkPlatformEvidence.exe",
+  "miniquake_native.dll",
+  "miniquake_text.dll"
+)
+$BinaryState = @()
+foreach ($Name in $BinaryNames) {
+  $Path = Join-Path $Build $Name
+  $Entry = [ordered]@{ name = $Name; present = (Test-Path -LiteralPath $Path -PathType Leaf); sha256 = ""; bytes = 0 }
+  if ($Entry.present) {
+    $Entry.sha256 = (Get-FileHash -Algorithm SHA256 -LiteralPath $Path).Hash.ToLowerInvariant()
+    $Entry.bytes = (Get-Item -LiteralPath $Path).Length
+  }
+  $BinaryState += $Entry
+}
+$Meta = [ordered]@{
+  schema = "MiniQuakeResultCollection/1"
+  package_id = $PackageId
+  block_id = $BlockId
+  delivery_revision = $DeliveryRevision
+  delivery_parent = $DeliveryParent
+  created_utc = (Get-Date).ToUniversalTime().ToString("o")
+  machine = $env:COMPUTERNAME
+  user = $env:USERNAME
+  source_root = $Root
+  binaries = $BinaryState
+}
+$Meta | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath (Join-Path $Staging "collection.json") -Encoding UTF8
+
+if (Test-Path -LiteralPath $Archive) { Remove-Item -Force -LiteralPath $Archive }
+Compress-Archive -Path (Join-Path $Staging "*") -DestinationPath $Archive -CompressionLevel Optimal
+Remove-Item -Recurse -Force -LiteralPath $Staging
+$Hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $Archive).Hash.ToLowerInvariant()
+Write-Host "MiniQuake $DeliveryRevision result archive"
+Write-Host "  path=$Archive"
+Write-Host "  sha256=$Hash"
+Write-Host "  Quake PAK/model/map/audio data and compiled binaries were not included."
diff --git a/PORT_LEDGER.json b/PORT_LEDGER.json
index 2cfd7c6a99638eb2543a818e85d87c8747b20c83..d4280810b7d179859958ef71edc93694ea0fa824 100644
--- a/PORT_LEDGER.json
+++ b/PORT_LEDGER.json
@@ -2,8 +2,8 @@
   "schema_version": 1,
   "project": "MiniQuake",
   "current_package": "BP-064",
-  "current_delivery_revision": "BP-060-064R1",
-  "delivery_parent": "BP-060-064",
+  "current_delivery_revision": "BP-060-064R2",
+  "delivery_parent": "BP-060-064R1",
   "parent_package": "BP-063",
   "compatibility_target": {
     "reference": "WinQuake/GLQuake 1.09",
@@ -63,7 +63,7 @@
     "block_parent": "BP-055-059R2",
     "date": "2026-07-28",
     "title": "WinQuake network, WinSock and Windows platform compatibility closure",
-    "status": "implemented; live-output delivery hotfix; Windows acceptance pending",
+    "status": "R2 rule-enumeration/runtime hotfix; Windows acceptance pending",
     "protocol_status": "protocol15_frozen_v1",
     "quakec_status": "quakec_109_frozen_v1",
     "world_physics_status": "world_physics_109_frozen_v1",
@@ -1377,11 +1377,49 @@
     "udp_loopback": "PASS"
   },
   "delivery_hotfix": {
-    "revision": "BP-060-064R1",
-    "parent": "BP-060-064",
-    "classification": "test infrastructure only",
+    "revision": "BP-060-064R2",
+    "parent": "BP-060-064R1",
+    "classification": "Protocol-3 control semantics plus runtime fixture; inherits R1 live-output infrastructure",
     "foreground_output_streamed": true,
-    "engine_source_changed": false,
+    "rule_end_internal": [
+      "",
+      ""
+    ],
+    "rule_end_wire": "command_only",
+    "unknown_previous": "no_reply",
+    "engine_source_changed": true,
     "native_changed": false
+  },
+  "bp060064_initial_result": {
+    "result_archive": "MiniQuake_BP-060-064_RESULTS_20260728-184449.zip",
+    "result_archive_sha256": "bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719",
+    "status": "PARTIAL PASS",
+    "compiled_targets": "65/65",
+    "inherited_groups": "PASS through BP-059",
+    "bp060": "20/20 PASS",
+    "bp061": "19 fixtures passed; fixture 20 crashed before assertion",
+    "first_failure": "Cannot index void at network_control_parity_tests.ml:63",
+    "later_gates": "not reached"
+  },
+  "bp060064r1_hotfix": {
+    "delivery_revision": "BP-060-064R1",
+    "parent_delivery": "BP-060-064",
+    "scope": "unbuffered live build/test output",
+    "windows_execution": "not run separately",
+    "production_source_changed": false
+  },
+  "bp060064r2_hotfix": {
+    "delivery_revision": "BP-060-064R2",
+    "delivery_parent": "BP-060-064R1",
+    "live_output": true,
+    "production_source_changed": true,
+    "native_source_changed": false,
+    "rule_enumeration": {
+      "end_of_list": "empty name/value pair -> command-only reply",
+      "unknown_previous": "no reply"
+    },
+    "contract_status": "network_platform_109_frozen_v1",
+    "contract_fingerprint": "0xb3ec7589",
+    "windows_acceptance": "pending"
   }
 }
diff --git a/PORT_STATUS.md b/PORT_STATUS.md
index e0bda111cdf95b3c123b7e9181eb6ba6f0c92f36..366a29f6b306633ebfa4c3303768b10e712977a4 100644
--- a/PORT_STATUS.md
+++ b/PORT_STATUS.md
@@ -4,8 +4,8 @@
 
 ```text
 engine package: BP-064
-delivery: BP-060-064R1
-delivery parent: BP-060-064
+delivery: BP-060-064R2
+delivery parent: BP-060-064R1
 block parent: BP-055-059R2
 profile: compat_109
 ```
@@ -28,8 +28,11 @@ render_special_109_frozen_v1
 audio_109_frozen_v1
 ```
 
-The R1 delivery changes only the PowerShell build/test infrastructure: foreground
-child-process output is streamed live and logged with a per-line flush.
+R2 inherits the unbuffered live-output runner from R1 and corrects the
+Protocol-3 server-rule walk: a regular end-of-list becomes a command-only reply,
+while an unknown predecessor name is ignored as in `net_dgrm.c`. The predecessor
+Windows run compiled 65/65 targets, passed every inherited group and BP-060
+20/20, then stopped in BP-061 before fixture 20.
 
 ## Candidate in this delivery
 
diff --git a/README.md b/README.md
index f7ff4fd166777dbe8e8e38a1c8471014d9550fcb..af0744a498b8be0055e348cc8a9d324b8557e0e9 100644
--- a/README.md
+++ b/README.md
@@ -1,4 +1,4 @@
-# MiniQuake – BP-060–BP-064R1
+# MiniQuake – BP-060–BP-064R2
 
 > Current delivery: network, connectionless control, WinSock and dedicated
 > Windows-platform closure. The accepted parent is BP-055–BP-059R2.
@@ -8,8 +8,11 @@ network_platform_109_frozen_v1
 fingerprint=0xb3ec7589
 ```
 
-Run the Windows acceptance with `TEST_BP-060-064R1.ps1`; see
-`docs/BP-060-064R1_TESTING.md`. All foreground build and test output is streamed live while it is written to the logs. The package never includes Quake game data.
+Run the Windows acceptance with `TEST_BP-060-064R2.ps1`; see
+`docs/BP-060-064R2_TESTING.md`. All foreground build and test output is streamed
+live while it is written to the logs. R2 additionally distinguishes the
+original Protocol-3 rule-list terminator from an unknown predecessor name. The
+package never includes Quake game data.
 
 ---
 
@@ -504,14 +507,17 @@ Warnung aus.
 
 ## Evidenzstand und nächste Pakete
 
-BP-045-BP-049R3 ist die bestätigte Elternbasis. BP-050-BP-054R1 erweitert die
-GLQuake-Kompatibilität um Mirrors, Z-Trick/Clear-Sonderpfade, Envmap,
-Timerefresh und einen Drei-Szenen-Evidenzkorpus. Der optionale Originalvergleich
-verwendet eine SSIM-Untergrenze von 0,95, ohne Referenzbilder einzubetten.
-
-Nach erfolgreicher R1-Windows-Abnahme folgt BP-055-BP-059. Der
-maschinenlesbare Stand steht in `PORT_LEDGER.json` und `BLOCK_LEDGER.json`;
-der aktuelle Testablauf in `docs/BP-050-054R1_TESTING.md`.
+BP-055–BP-059R2 ist die bestätigte Elternbasis. BP-060–BP-064R2 schließt die
+klassische NET-/WinSock-/Windows-Plattformschicht. Der erste Windows-Lauf
+kompilierte 65/65 Programme, bestätigte alle geerbten Gruppen und BP-060 mit
+20/20 Fixtures und stoppte anschließend an der jetzt korrigierten
+Protocol-3-Regelenumeration.
+
+R2 unterscheidet originalgetreu zwischen einem command-only Endpaket und dem
+stillen No-Reply-Fall für einen unbekannten Vorgänger. Der Live-Output-Runner
+aus R1 ist vollständig enthalten. Der maschinenlesbare Stand steht in
+`PORT_LEDGER.json` und `BLOCK_LEDGER.json`; der aktuelle Testablauf in
+`docs/BP-060-064R2_TESTING.md`.
 
 ## Lizenz
 
diff --git a/TEST_BP-060-064.ps1 b/TEST_BP-060-064.ps1
index afd812a602a7d9b81b5bc2279ca9054bb409a5bb..104173069e8c08e1a3db81b7bfc9b2c1953a9832 100755
--- a/TEST_BP-060-064.ps1
+++ b/TEST_BP-060-064.ps1
@@ -1,268 +1,4 @@
-[CmdletBinding()]
-param(
-  [string]$Compiler = "",
-  [Alias("StdLibPath", "ImportRoot")]
-  [string]$StdLib = "",
-  [string]$Python = "",
-  [string]$QuakeBase = "",
-  [string]$Game = "id1",
-  [string]$Map = "start",
-  [ValidateRange(1, 1000000)]
-  [int]$Frames = 300,
-  [ValidateRange(1, 1000000)]
-  [int]$TraceFrames = 128,
-  [ValidateRange(1, 1000000)]
-  [int]$RenderEvidenceFrame = 128,
-  [ValidateSet("Release", "Debug")]
-  [string]$Configuration = "Release",
-  [switch]$SkipBuild,
-  [switch]$SkipGameValidation,
-  [switch]$SkipTraceValidation,
-  [switch]$SkipRenderEvidence,
-  [switch]$SkipAudioEvidence,
-  [switch]$SkipNetworkEvidence,
-  [switch]$NetworkTests,
-  [switch]$RebuildNative,
-  [switch]$Listings,
-  [switch]$ContinueIndependentTests = $true,
-  [switch]$BisectOnFailure
-)
-
-$ErrorActionPreference = "Stop"
-Set-StrictMode -Version Latest
-
-# Force Python tools and the Python MiniLang compiler to flush every line.
-# The PowerShell wrappers below also forward every received line immediately.
-$env:PYTHONUNBUFFERED = "1"
-
-$PackageId = "BP-064"
-$ParentPackageId = "BP-063"
-$BlockId = "BP-060-064"
-$DeliveryRevision = "BP-060-064"
-$BlockParentPackageId = "BP-055-059R2"
-$Root = $PSScriptRoot
-$Build = Join-Path $Root "build"
-$BuildScript = Join-Path $Root "build.ps1"
-$GameExe = Join-Path $Build "MiniQuake.exe"
-$EvidenceExe = Join-Path $Build "MiniQuakeNetworkPlatformEvidence.exe"
-$TraceComparator = Join-Path $Root "tools\compare_traces.py"
-$RenderComparator = Join-Path $Root "tools\compare_render_evidence.py"
-$Timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
-$TranscriptPath = Join-Path $Build ("bp060-064-test-{0}.log" -f $Timestamp)
-$SummaryPath = Join-Path $Build "bp060-064-test-summary.json"
-$Steps = New-Object System.Collections.Generic.List[object]
-
-function Resolve-Tool([string]$Value, [string]$Label) {
-  if (-not [string]::IsNullOrWhiteSpace($Value) -and (Test-Path -LiteralPath $Value -PathType Leaf)) {
-    return [IO.Path]::GetFullPath($Value)
-  }
-  if (-not [string]::IsNullOrWhiteSpace($Value)) {
-    $Command = Get-Command $Value -ErrorAction SilentlyContinue | Select-Object -First 1
-    if ($null -ne $Command) { return $Command.Path }
-  }
-  throw "$Label not found: $Value"
-}
-
-function Add-Step([string]$Name, [string]$Status, [int]$ExitCode, [string]$Detail) {
-  $Steps.Add([ordered]@{ name = $Name; status = $Status; exit_code = $ExitCode; detail = $Detail })
-  $Color = if ($Status -eq "PASS") { "Green" } elseif ($Status -eq "SKIPPED") { "Yellow" } else { "Red" }
-  Write-Host ("[{0}] {1} - {2}" -f $Status, $Name, $Detail) -ForegroundColor $Color
-}
-
-function Failure-Marker([string]$Text) {
-  if ([string]::IsNullOrWhiteSpace($Text)) { return "" }
-  foreach ($Line in ($Text -split "`r?`n")) {
-    if ($Line -match '^\s*FAIL:\s*' -or $Line -match 'tests failed:\s*[1-9]' -or $Line -match 'evidence:\s*FAIL') { return $Line.Trim() }
-  }
-  return ""
-}
-
-function Invoke-LiveProcess(
-  [string]$Executable,
-  [string[]]$Arguments,
-  [string]$LogPath
-) {
-  $Lines = [System.Collections.Generic.List[string]]::new()
-  $Encoding = [System.Text.UTF8Encoding]::new($false)
-  $Writer = [System.IO.StreamWriter]::new($LogPath, $false, $Encoding)
-  $Code = 1
-  try {
-    & $Executable @Arguments 2>&1 |
-      ForEach-Object {
-        $Line = [string]$_
-        $Lines.Add($Line)
-        $Writer.WriteLine($Line)
-        $Writer.Flush()
-        Write-Host $Line
-      }
-    $Code = [int]$LASTEXITCODE
-  } finally {
-    $Writer.Flush()
-    $Writer.Dispose()
-  }
-  $Text = [string]::Join("`n", $Lines.ToArray())
-  return [pscustomobject]@{
-    exit_code = $Code
-    text = $Text
-  }
-}
-
-function Run-Logged([string]$Name, [string]$Executable, [string[]]$Arguments, [string]$LogName, [string]$RequiredMarker = "") {
-  $LogPath = Join-Path $Build $LogName
-  Write-Host ("[MiniQuake] starting {0}" -f $Name) -ForegroundColor Cyan
-  $Result = Invoke-LiveProcess -Executable $Executable -Arguments $Arguments -LogPath $LogPath
-  $Code = [int]$Result.exit_code
-  $Text = [string]$Result.text
-  $Marker = Failure-Marker $Text
-  if ($Code -eq 0 -and -not [string]::IsNullOrWhiteSpace($Marker)) { $Code = 1 }
-  if ($Code -eq 0 -and -not [string]::IsNullOrWhiteSpace($RequiredMarker) -and $Text -notmatch [regex]::Escape($RequiredMarker)) { $Code = 1; $Marker = "missing marker: $RequiredMarker" }
-  if ($Code -ne 0) { Add-Step $Name "FAIL" $Code ("log=" + $LogName + " " + $Marker); throw "$Name failed with exit code $Code" }
-  Add-Step $Name "PASS" 0 ("log=" + $LogName)
-  return $Text
-}
-
-function Run-NetworkEvidencePair([string]$Suffix, [int]$Port) {
-  $ServerOut = Join-Path $Build ("bp060-064-network-server-{0}.log" -f $Suffix)
-  $ServerErr = Join-Path $Build ("bp060-064-network-server-{0}.err.log" -f $Suffix)
-  $ClientLog = Join-Path $Build ("bp060-064-network-client-{0}.log" -f $Suffix)
-  Remove-Item -Force -ErrorAction SilentlyContinue $ServerOut, $ServerErr, $ClientLog
-  Write-Host ("[MiniQuake] starting network evidence server {0} on UDP port {1}" -f $Suffix, $Port) -ForegroundColor Cyan
-  $Server = Start-Process -FilePath $EvidenceExe -ArgumentList @("server", [string]$Port) -PassThru -NoNewWindow -RedirectStandardOutput $ServerOut -RedirectStandardError $ServerErr
-  Start-Sleep -Milliseconds 300
-  Write-Host ("[MiniQuake] starting network evidence client {0}" -f $Suffix) -ForegroundColor Cyan
-  $ClientResult = Invoke-LiveProcess -Executable $EvidenceExe -Arguments @("client", [string]$Port) -LogPath $ClientLog
-  $ClientCode = [int]$ClientResult.exit_code
-  $ClientText = [string]$ClientResult.text
-  if (-not $Server.WaitForExit(7000)) { Stop-Process -Id $Server.Id -Force -ErrorAction SilentlyContinue; throw "network evidence server $Suffix timed out" }
-  $ServerCode = $Server.ExitCode
-  $ServerText = ""
-  if (Test-Path $ServerOut) { $ServerText = Get-Content -Raw -LiteralPath $ServerOut }
-  if (Test-Path $ServerErr) { $ServerText += Get-Content -Raw -LiteralPath $ServerErr }
-  if (-not [string]::IsNullOrWhiteSpace($ServerText)) {
-    Write-Host ("[MiniQuake] network evidence server {0} output" -f $Suffix) -ForegroundColor Cyan
-    foreach ($ServerLine in ($ServerText -split "`r?`n")) {
-      if (-not [string]::IsNullOrEmpty($ServerLine)) { Write-Host $ServerLine }
-    }
-  }
-  if ($ServerCode -ne 0 -or $ClientCode -ne 0) { throw "network evidence pair $Suffix failed: server=$ServerCode client=$ClientCode" }
-  if ($ServerText -notmatch 'network platform evidence server: PASS') { throw "network evidence server $Suffix did not report PASS" }
-  if ($ClientText -notmatch 'network platform evidence: PASS') { throw "network evidence client $Suffix did not report PASS" }
-  return $ClientLog
-}
-
-New-Item -ItemType Directory -Force -Path $Build | Out-Null
-Start-Transcript -Path $TranscriptPath -Force | Out-Null
-$Failure = ""
-try {
-  Write-Host "MiniQuake BP-060-064 acceptance test"
-  Write-Host "  parent: $BlockParentPackageId"
-  Write-Host "  root:   $Root"
-  if ($BisectOnFailure) { Write-Host "  logical bisect markers enabled" }
-
-  if ([string]::IsNullOrWhiteSpace($Python)) { $Python = "python" }
-  $PythonExe = Resolve-Tool $Python "Python"
-
-  if (-not $SkipBuild) {
-    $PowerShellExe = (Get-Process -Id $PID).Path
-    $Arguments = @("-NoProfile", "-ExecutionPolicy", "Bypass", "-File", $BuildScript)
-    if (-not [string]::IsNullOrWhiteSpace($Compiler)) { $Arguments += @("-Compiler", $Compiler) }
-    if (-not [string]::IsNullOrWhiteSpace($StdLib)) { $Arguments += @("-StdLib", $StdLib) }
-    $Arguments += @("-Python", $PythonExe, "-Configuration", $Configuration)
-    if ($NetworkTests) { $Arguments += "-NetworkTests" }
-    if ($RebuildNative) { $Arguments += "-RebuildNative" }
-    if ($Listings) { $Arguments += "-Listings" }
-    [void](Run-Logged "single cumulative build and unit-test suite" $PowerShellExe $Arguments "bp060-064-build-child.log" "MiniQuake BP-064 network/platform closure tests passed: 24")
-  } else {
-    Add-Step "single cumulative build and unit-test suite" "SKIPPED" 0 "-SkipBuild"
-  }
-
-  foreach ($Path in @($GameExe, $EvidenceExe)) { if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) { throw "required executable missing: $Path" } }
-
-  $Version = Run-Logged "package identity" $GameExe @("--version") "bp060-064-version.txt" "Network/platform status: network_platform_109_frozen_v1"
-  if ($Version -notmatch 'Package: BP-064' -or $Version -notmatch 'Block: BP-060-064') { throw "compiled package identity mismatch" }
-
-  $CanUseGame = -not $SkipGameValidation -and -not [string]::IsNullOrWhiteSpace($QuakeBase)
-  if ($CanUseGame) {
-    if (-not (Test-Path -LiteralPath (Join-Path $QuakeBase "$Game\pak0.pak") -PathType Leaf)) { throw "Quake data not found: $QuakeBase\$Game\pak0.pak" }
-    [void](Run-Logged "installed Quake data validation" $GameExe @("--validate-game", $QuakeBase, $Map, "-game", $Game) "bp060-064-game-validation.log" "Validation result: PASS")
-    [void](Run-Logged "headless runtime validation" $GameExe @("--validate-runtime", $QuakeBase, $Map, [string]$Frames, "-game", $Game) "bp060-064-runtime-validation.log")
-  } else {
-    Add-Step "installed Quake data validation" "SKIPPED" 0 "no usable -QuakeBase or skipped"
-    Add-Step "headless runtime validation" "SKIPPED" 0 "no usable -QuakeBase or skipped"
-  }
-
-  if (-not $SkipAudioEvidence -and $CanUseGame) {
-    $AudioExe = Join-Path $Build "MiniQuakeAudioRetailEvidence.exe"
-    $A = Join-Path $Build "bp060-064-audio-a.log"; $B = Join-Path $Build "bp060-064-audio-b.log"
-    $TextA = Run-Logged "retail audio evidence A" $AudioExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($A)) "MiniQuake BP-059 retail audio evidence: PASS"
-    $TextB = Run-Logged "retail audio evidence B" $AudioExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($B)) "MiniQuake BP-059 retail audio evidence: PASS"
-    $HashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $A).Hash.ToLowerInvariant(); $HashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $B).Hash.ToLowerInvariant()
-    if ($HashA -ne $HashB) { throw "retail audio evidence differs" }
-    Add-Step "byte-identical retail audio evidence" "PASS" 0 ("sha256=" + $HashA)
-  } else { Add-Step "retail audio evidence" "SKIPPED" 0 "skipped or no Quake data" }
-
-  if (-not $SkipTraceValidation -and $CanUseGame) {
-    $TraceDir = Join-Path $Build "bp060-064-traces"; Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $TraceDir; New-Item -ItemType Directory -Force -Path $TraceDir | Out-Null
-    $PrefixA = Join-Path $TraceDir "run-a"; $PrefixB = Join-Path $TraceDir "run-b"
-    [void](Run-Logged "compatibility trace A" $GameExe @("--compat-trace", $QuakeBase, $Map, [string]$TraceFrames, $PrefixA, "-game", $Game) "bp060-064-trace-a.log")
-    [void](Run-Logged "compatibility trace B" $GameExe @("--compat-trace", $QuakeBase, $Map, [string]$TraceFrames, $PrefixB, "-game", $Game) "bp060-064-trace-b.log")
-    [void](Run-Logged "byte-identical trace comparison" $PythonExe @($TraceComparator, ("{0}.mqtrace" -f $PrefixA), ("{0}.mqtrace" -f $PrefixB), "--json-output", (Join-Path $Build "bp060-064-trace-comparison.json")) "bp060-064-trace-comparison.log")
-  } else { Add-Step "deterministic compatibility traces" "SKIPPED" 0 "skipped or no Quake data" }
-
-  if (-not $SkipRenderEvidence -and $CanUseGame) {
-    $EvidenceDir = Join-Path $Build "bp060-064-render"; Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $EvidenceDir; New-Item -ItemType Directory -Force -Path $EvidenceDir | Out-Null
-    $EA = Join-Path $EvidenceDir "a"; $EB = Join-Path $EvidenceDir "b"
-    [void](Run-Logged "render evidence A" $GameExe @("--render-evidence", $QuakeBase, $Map, [string]$RenderEvidenceFrame, $EA, "-game", $Game) "bp060-064-render-a.log")
-    [void](Run-Logged "render evidence B" $GameExe @("--render-evidence", $QuakeBase, $Map, [string]$RenderEvidenceFrame, $EB, "-game", $Game) "bp060-064-render-b.log")
-    [void](Run-Logged "byte-identical render evidence" $PythonExe @($RenderComparator, ("{0}.tga" -f $EA), ("{0}.tga" -f $EB), "--require-exact", "--json-output", (Join-Path $Build "bp060-064-render-comparison.json")) "bp060-064-render-comparison.log")
-  } else { Add-Step "deterministic framebuffer evidence" "SKIPPED" 0 "skipped or no Quake data" }
-
-  if (-not $SkipNetworkEvidence) {
-    $PortA = Get-Random -Minimum 30000 -Maximum 45000
-    $PortB = Get-Random -Minimum 45001 -Maximum 60000
-    $ClientA = Run-NetworkEvidencePair "a" $PortA
-    $ClientB = Run-NetworkEvidencePair "b" $PortB
-    $HashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $ClientA).Hash.ToLowerInvariant(); $HashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $ClientB).Hash.ToLowerInvariant()
-    if ($HashA -ne $HashB) { throw "network evidence client reports differ: A=$HashA B=$HashB" }
-    Add-Step "two independent UDP control handshakes" "PASS" 0 ("sha256=" + $HashA)
-  } else { Add-Step "two independent UDP control handshakes" "SKIPPED" 0 "-SkipNetworkEvidence" }
-
-  if ($NetworkTests) { [void](Run-Logged "Winsock UDP loopback smoke" $GameExe @("--udp-smoke", "2000") "bp060-064-udp-smoke.log" "result=PASS") }
-  else { Add-Step "Winsock UDP loopback smoke" "SKIPPED" 0 "-NetworkTests not supplied" }
-
-  $Failed = @($Steps | Where-Object { $_.status -eq "FAIL" })
-  if ($Failed.Count -gt 0) { throw "independent test groups failed: $($Failed.Count)" }
-  Add-Step "MiniQuake BP-060-064 acceptance" "PASS" 0 "all required gates passed"
-} catch {
-  $Failure = $_.Exception.Message
-  Write-Host ("ERROR: " + $Failure) -ForegroundColor Red
-} finally {
-  $Status = if ([string]::IsNullOrWhiteSpace($Failure)) { "PASS" } else { "FAIL" }
-  $Summary = [ordered]@{
-    schema_version = 1
-    package_id = $PackageId
-    parent_package_id = $ParentPackageId
-    block_id = $BlockId
-    block_parent_package_id = $BlockParentPackageId
-    delivery_revision = $DeliveryRevision
-    network_platform_status = "network_platform_109_frozen_v1"
-    network_platform_fingerprint = "0xb3ec7589"
-    status = $Status
-    failure = $Failure
-    created_utc = (Get-Date).ToUniversalTime().ToString("o")
-    quake_base_supplied = -not [string]::IsNullOrWhiteSpace($QuakeBase)
-    steps = $Steps
-  }
-  $Summary | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $SummaryPath -Encoding UTF8
-  Stop-Transcript | Out-Null
-}
-
-if (-not [string]::IsNullOrWhiteSpace($Failure)) {
-  Write-Host "MiniQuake BP-060-064 acceptance test: FAIL" -ForegroundColor Red
-  Write-Host "Run .\COLLECT_RESULTS.ps1 and upload the generated ZIP."
-  exit 1
-}
-Write-Host "MiniQuake BP-060-064 acceptance test: PASS" -ForegroundColor Green
-Write-Host "Result summary: $SummaryPath"
-Write-Host "For feedback, run .\COLLECT_RESULTS.ps1 and upload the generated ZIP."
-exit 0
+﻿$Target = Join-Path $PSScriptRoot "TEST_BP-060-064R2.ps1"
+Write-Warning "BP-060-064 is superseded by BP-060-064R2; forwarding to TEST_BP-060-064R2.ps1."
+& $Target @args
+exit $LASTEXITCODE
diff --git a/TEST_BP-060-064R1.ps1 b/TEST_BP-060-064R1.ps1
index f7d0eddf2b5aef945c2716a5a572142eeed1ef98..0dfb470f192aab2424370c3a44af9e40972f6bfa 100755
--- a/TEST_BP-060-064R1.ps1
+++ b/TEST_BP-060-064R1.ps1
@@ -1,268 +1,4 @@
-[CmdletBinding()]
-param(
-  [string]$Compiler = "",
-  [Alias("StdLibPath", "ImportRoot")]
-  [string]$StdLib = "",
-  [string]$Python = "",
-  [string]$QuakeBase = "",
-  [string]$Game = "id1",
-  [string]$Map = "start",
-  [ValidateRange(1, 1000000)]
-  [int]$Frames = 300,
-  [ValidateRange(1, 1000000)]
-  [int]$TraceFrames = 128,
-  [ValidateRange(1, 1000000)]
-  [int]$RenderEvidenceFrame = 128,
-  [ValidateSet("Release", "Debug")]
-  [string]$Configuration = "Release",
-  [switch]$SkipBuild,
-  [switch]$SkipGameValidation,
-  [switch]$SkipTraceValidation,
-  [switch]$SkipRenderEvidence,
-  [switch]$SkipAudioEvidence,
-  [switch]$SkipNetworkEvidence,
-  [switch]$NetworkTests,
-  [switch]$RebuildNative,
-  [switch]$Listings,
-  [switch]$ContinueIndependentTests = $true,
-  [switch]$BisectOnFailure
-)
-
-$ErrorActionPreference = "Stop"
-Set-StrictMode -Version Latest
-
-# Force Python tools and the Python MiniLang compiler to flush every line.
-# The PowerShell wrappers below also forward every received line immediately.
-$env:PYTHONUNBUFFERED = "1"
-
-$PackageId = "BP-064"
-$ParentPackageId = "BP-063"
-$BlockId = "BP-060-064"
-$DeliveryRevision = "BP-060-064R1"
-$BlockParentPackageId = "BP-055-059R2"
-$Root = $PSScriptRoot
-$Build = Join-Path $Root "build"
-$BuildScript = Join-Path $Root "build.ps1"
-$GameExe = Join-Path $Build "MiniQuake.exe"
-$EvidenceExe = Join-Path $Build "MiniQuakeNetworkPlatformEvidence.exe"
-$TraceComparator = Join-Path $Root "tools\compare_traces.py"
-$RenderComparator = Join-Path $Root "tools\compare_render_evidence.py"
-$Timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
-$TranscriptPath = Join-Path $Build ("bp060-064r1-test-{0}.log" -f $Timestamp)
-$SummaryPath = Join-Path $Build "bp060-064r1-test-summary.json"
-$Steps = New-Object System.Collections.Generic.List[object]
-
-function Resolve-Tool([string]$Value, [string]$Label) {
-  if (-not [string]::IsNullOrWhiteSpace($Value) -and (Test-Path -LiteralPath $Value -PathType Leaf)) {
-    return [IO.Path]::GetFullPath($Value)
-  }
-  if (-not [string]::IsNullOrWhiteSpace($Value)) {
-    $Command = Get-Command $Value -ErrorAction SilentlyContinue | Select-Object -First 1
-    if ($null -ne $Command) { return $Command.Path }
-  }
-  throw "$Label not found: $Value"
-}
-
-function Add-Step([string]$Name, [string]$Status, [int]$ExitCode, [string]$Detail) {
-  $Steps.Add([ordered]@{ name = $Name; status = $Status; exit_code = $ExitCode; detail = $Detail })
-  $Color = if ($Status -eq "PASS") { "Green" } elseif ($Status -eq "SKIPPED") { "Yellow" } else { "Red" }
-  Write-Host ("[{0}] {1} - {2}" -f $Status, $Name, $Detail) -ForegroundColor $Color
-}
-
-function Failure-Marker([string]$Text) {
-  if ([string]::IsNullOrWhiteSpace($Text)) { return "" }
-  foreach ($Line in ($Text -split "`r?`n")) {
-    if ($Line -match '^\s*FAIL:\s*' -or $Line -match 'tests failed:\s*[1-9]' -or $Line -match 'evidence:\s*FAIL') { return $Line.Trim() }
-  }
-  return ""
-}
-
-function Invoke-LiveProcess(
-  [string]$Executable,
-  [string[]]$Arguments,
-  [string]$LogPath
-) {
-  $Lines = [System.Collections.Generic.List[string]]::new()
-  $Encoding = [System.Text.UTF8Encoding]::new($false)
-  $Writer = [System.IO.StreamWriter]::new($LogPath, $false, $Encoding)
-  $Code = 1
-  try {
-    & $Executable @Arguments 2>&1 |
-      ForEach-Object {
-        $Line = [string]$_
-        $Lines.Add($Line)
-        $Writer.WriteLine($Line)
-        $Writer.Flush()
-        Write-Host $Line
-      }
-    $Code = [int]$LASTEXITCODE
-  } finally {
-    $Writer.Flush()
-    $Writer.Dispose()
-  }
-  $Text = [string]::Join("`n", $Lines.ToArray())
-  return [pscustomobject]@{
-    exit_code = $Code
-    text = $Text
-  }
-}
-
-function Run-Logged([string]$Name, [string]$Executable, [string[]]$Arguments, [string]$LogName, [string]$RequiredMarker = "") {
-  $LogPath = Join-Path $Build $LogName
-  Write-Host ("[MiniQuake] starting {0}" -f $Name) -ForegroundColor Cyan
-  $Result = Invoke-LiveProcess -Executable $Executable -Arguments $Arguments -LogPath $LogPath
-  $Code = [int]$Result.exit_code
-  $Text = [string]$Result.text
-  $Marker = Failure-Marker $Text
-  if ($Code -eq 0 -and -not [string]::IsNullOrWhiteSpace($Marker)) { $Code = 1 }
-  if ($Code -eq 0 -and -not [string]::IsNullOrWhiteSpace($RequiredMarker) -and $Text -notmatch [regex]::Escape($RequiredMarker)) { $Code = 1; $Marker = "missing marker: $RequiredMarker" }
-  if ($Code -ne 0) { Add-Step $Name "FAIL" $Code ("log=" + $LogName + " " + $Marker); throw "$Name failed with exit code $Code" }
-  Add-Step $Name "PASS" 0 ("log=" + $LogName)
-  return $Text
-}
-
-function Run-NetworkEvidencePair([string]$Suffix, [int]$Port) {
-  $ServerOut = Join-Path $Build ("bp060-064r1-network-server-{0}.log" -f $Suffix)
-  $ServerErr = Join-Path $Build ("bp060-064r1-network-server-{0}.err.log" -f $Suffix)
-  $ClientLog = Join-Path $Build ("bp060-064r1-network-client-{0}.log" -f $Suffix)
-  Remove-Item -Force -ErrorAction SilentlyContinue $ServerOut, $ServerErr, $ClientLog
-  Write-Host ("[MiniQuake] starting network evidence server {0} on UDP port {1}" -f $Suffix, $Port) -ForegroundColor Cyan
-  $Server = Start-Process -FilePath $EvidenceExe -ArgumentList @("server", [string]$Port) -PassThru -NoNewWindow -RedirectStandardOutput $ServerOut -RedirectStandardError $ServerErr
-  Start-Sleep -Milliseconds 300
-  Write-Host ("[MiniQuake] starting network evidence client {0}" -f $Suffix) -ForegroundColor Cyan
-  $ClientResult = Invoke-LiveProcess -Executable $EvidenceExe -Arguments @("client", [string]$Port) -LogPath $ClientLog
-  $ClientCode = [int]$ClientResult.exit_code
-  $ClientText = [string]$ClientResult.text
-  if (-not $Server.WaitForExit(7000)) { Stop-Process -Id $Server.Id -Force -ErrorAction SilentlyContinue; throw "network evidence server $Suffix timed out" }
-  $ServerCode = $Server.ExitCode
-  $ServerText = ""
-  if (Test-Path $ServerOut) { $ServerText = Get-Content -Raw -LiteralPath $ServerOut }
-  if (Test-Path $ServerErr) { $ServerText += Get-Content -Raw -LiteralPath $ServerErr }
-  if (-not [string]::IsNullOrWhiteSpace($ServerText)) {
-    Write-Host ("[MiniQuake] network evidence server {0} output" -f $Suffix) -ForegroundColor Cyan
-    foreach ($ServerLine in ($ServerText -split "`r?`n")) {
-      if (-not [string]::IsNullOrEmpty($ServerLine)) { Write-Host $ServerLine }
-    }
-  }
-  if ($ServerCode -ne 0 -or $ClientCode -ne 0) { throw "network evidence pair $Suffix failed: server=$ServerCode client=$ClientCode" }
-  if ($ServerText -notmatch 'network platform evidence server: PASS') { throw "network evidence server $Suffix did not report PASS" }
-  if ($ClientText -notmatch 'network platform evidence: PASS') { throw "network evidence client $Suffix did not report PASS" }
-  return $ClientLog
-}
-
-New-Item -ItemType Directory -Force -Path $Build | Out-Null
-Start-Transcript -Path $TranscriptPath -Force | Out-Null
-$Failure = ""
-try {
-  Write-Host "MiniQuake BP-060-064R1 acceptance test"
-  Write-Host "  parent: $BlockParentPackageId"
-  Write-Host "  root:   $Root"
-  if ($BisectOnFailure) { Write-Host "  logical bisect markers enabled" }
-
-  if ([string]::IsNullOrWhiteSpace($Python)) { $Python = "python" }
-  $PythonExe = Resolve-Tool $Python "Python"
-
-  if (-not $SkipBuild) {
-    $PowerShellExe = (Get-Process -Id $PID).Path
-    $Arguments = @("-NoProfile", "-ExecutionPolicy", "Bypass", "-File", $BuildScript)
-    if (-not [string]::IsNullOrWhiteSpace($Compiler)) { $Arguments += @("-Compiler", $Compiler) }
-    if (-not [string]::IsNullOrWhiteSpace($StdLib)) { $Arguments += @("-StdLib", $StdLib) }
-    $Arguments += @("-Python", $PythonExe, "-Configuration", $Configuration)
-    if ($NetworkTests) { $Arguments += "-NetworkTests" }
-    if ($RebuildNative) { $Arguments += "-RebuildNative" }
-    if ($Listings) { $Arguments += "-Listings" }
-    [void](Run-Logged "single cumulative build and unit-test suite" $PowerShellExe $Arguments "bp060-064r1-build-child.log" "MiniQuake BP-064 network/platform closure tests passed: 24")
-  } else {
-    Add-Step "single cumulative build and unit-test suite" "SKIPPED" 0 "-SkipBuild"
-  }
-
-  foreach ($Path in @($GameExe, $EvidenceExe)) { if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) { throw "required executable missing: $Path" } }
-
-  $Version = Run-Logged "package identity" $GameExe @("--version") "bp060-064r1-version.txt" "Network/platform status: network_platform_109_frozen_v1"
-  if ($Version -notmatch 'Package: BP-064' -or $Version -notmatch 'Block: BP-060-064') { throw "compiled package identity mismatch" }
-
-  $CanUseGame = -not $SkipGameValidation -and -not [string]::IsNullOrWhiteSpace($QuakeBase)
-  if ($CanUseGame) {
-    if (-not (Test-Path -LiteralPath (Join-Path $QuakeBase "$Game\pak0.pak") -PathType Leaf)) { throw "Quake data not found: $QuakeBase\$Game\pak0.pak" }
-    [void](Run-Logged "installed Quake data validation" $GameExe @("--validate-game", $QuakeBase, $Map, "-game", $Game) "bp060-064r1-game-validation.log" "Validation result: PASS")
-    [void](Run-Logged "headless runtime validation" $GameExe @("--validate-runtime", $QuakeBase, $Map, [string]$Frames, "-game", $Game) "bp060-064r1-runtime-validation.log")
-  } else {
-    Add-Step "installed Quake data validation" "SKIPPED" 0 "no usable -QuakeBase or skipped"
-    Add-Step "headless runtime validation" "SKIPPED" 0 "no usable -QuakeBase or skipped"
-  }
-
-  if (-not $SkipAudioEvidence -and $CanUseGame) {
-    $AudioExe = Join-Path $Build "MiniQuakeAudioRetailEvidence.exe"
-    $A = Join-Path $Build "bp060-064r1-audio-a.log"; $B = Join-Path $Build "bp060-064r1-audio-b.log"
-    $TextA = Run-Logged "retail audio evidence A" $AudioExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($A)) "MiniQuake BP-059 retail audio evidence: PASS"
-    $TextB = Run-Logged "retail audio evidence B" $AudioExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($B)) "MiniQuake BP-059 retail audio evidence: PASS"
-    $HashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $A).Hash.ToLowerInvariant(); $HashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $B).Hash.ToLowerInvariant()
-    if ($HashA -ne $HashB) { throw "retail audio evidence differs" }
-    Add-Step "byte-identical retail audio evidence" "PASS" 0 ("sha256=" + $HashA)
-  } else { Add-Step "retail audio evidence" "SKIPPED" 0 "skipped or no Quake data" }
-
-  if (-not $SkipTraceValidation -and $CanUseGame) {
-    $TraceDir = Join-Path $Build "bp060-064r1-traces"; Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $TraceDir; New-Item -ItemType Directory -Force -Path $TraceDir | Out-Null
-    $PrefixA = Join-Path $TraceDir "run-a"; $PrefixB = Join-Path $TraceDir "run-b"
-    [void](Run-Logged "compatibility trace A" $GameExe @("--compat-trace", $QuakeBase, $Map, [string]$TraceFrames, $PrefixA, "-game", $Game) "bp060-064r1-trace-a.log")
-    [void](Run-Logged "compatibility trace B" $GameExe @("--compat-trace", $QuakeBase, $Map, [string]$TraceFrames, $PrefixB, "-game", $Game) "bp060-064r1-trace-b.log")
-    [void](Run-Logged "byte-identical trace comparison" $PythonExe @($TraceComparator, ("{0}.mqtrace" -f $PrefixA), ("{0}.mqtrace" -f $PrefixB), "--json-output", (Join-Path $Build "bp060-064r1-trace-comparison.json")) "bp060-064r1-trace-comparison.log")
-  } else { Add-Step "deterministic compatibility traces" "SKIPPED" 0 "skipped or no Quake data" }
-
-  if (-not $SkipRenderEvidence -and $CanUseGame) {
-    $EvidenceDir = Join-Path $Build "bp060-064r1-render"; Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $EvidenceDir; New-Item -ItemType Directory -Force -Path $EvidenceDir | Out-Null
-    $EA = Join-Path $EvidenceDir "a"; $EB = Join-Path $EvidenceDir "b"
-    [void](Run-Logged "render evidence A" $GameExe @("--render-evidence", $QuakeBase, $Map, [string]$RenderEvidenceFrame, $EA, "-game", $Game) "bp060-064r1-render-a.log")
-    [void](Run-Logged "render evidence B" $GameExe @("--render-evidence", $QuakeBase, $Map, [string]$RenderEvidenceFrame, $EB, "-game", $Game) "bp060-064r1-render-b.log")
-    [void](Run-Logged "byte-identical render evidence" $PythonExe @($RenderComparator, ("{0}.tga" -f $EA), ("{0}.tga" -f $EB), "--require-exact", "--json-output", (Join-Path $Build "bp060-064r1-render-comparison.json")) "bp060-064r1-render-comparison.log")
-  } else { Add-Step "deterministic framebuffer evidence" "SKIPPED" 0 "skipped or no Quake data" }
-
-  if (-not $SkipNetworkEvidence) {
-    $PortA = Get-Random -Minimum 30000 -Maximum 45000
-    $PortB = Get-Random -Minimum 45001 -Maximum 60000
-    $ClientA = Run-NetworkEvidencePair "a" $PortA
-    $ClientB = Run-NetworkEvidencePair "b" $PortB
-    $HashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $ClientA).Hash.ToLowerInvariant(); $HashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $ClientB).Hash.ToLowerInvariant()
-    if ($HashA -ne $HashB) { throw "network evidence client reports differ: A=$HashA B=$HashB" }
-    Add-Step "two independent UDP control handshakes" "PASS" 0 ("sha256=" + $HashA)
-  } else { Add-Step "two independent UDP control handshakes" "SKIPPED" 0 "-SkipNetworkEvidence" }
-
-  if ($NetworkTests) { [void](Run-Logged "Winsock UDP loopback smoke" $GameExe @("--udp-smoke", "2000") "bp060-064r1-udp-smoke.log" "result=PASS") }
-  else { Add-Step "Winsock UDP loopback smoke" "SKIPPED" 0 "-NetworkTests not supplied" }
-
-  $Failed = @($Steps | Where-Object { $_.status -eq "FAIL" })
-  if ($Failed.Count -gt 0) { throw "independent test groups failed: $($Failed.Count)" }
-  Add-Step "MiniQuake BP-060-064R1 acceptance" "PASS" 0 "all required gates passed"
-} catch {
-  $Failure = $_.Exception.Message
-  Write-Host ("ERROR: " + $Failure) -ForegroundColor Red
-} finally {
-  $Status = if ([string]::IsNullOrWhiteSpace($Failure)) { "PASS" } else { "FAIL" }
-  $Summary = [ordered]@{
-    schema_version = 1
-    package_id = $PackageId
-    parent_package_id = $ParentPackageId
-    block_id = $BlockId
-    block_parent_package_id = $BlockParentPackageId
-    delivery_revision = $DeliveryRevision
-    network_platform_status = "network_platform_109_frozen_v1"
-    network_platform_fingerprint = "0xb3ec7589"
-    status = $Status
-    failure = $Failure
-    created_utc = (Get-Date).ToUniversalTime().ToString("o")
-    quake_base_supplied = -not [string]::IsNullOrWhiteSpace($QuakeBase)
-    steps = $Steps
-  }
-  $Summary | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $SummaryPath -Encoding UTF8
-  Stop-Transcript | Out-Null
-}
-
-if (-not [string]::IsNullOrWhiteSpace($Failure)) {
-  Write-Host "MiniQuake BP-060-064R1 acceptance test: FAIL" -ForegroundColor Red
-  Write-Host "Run .\COLLECT_RESULTS.ps1 and upload the generated ZIP."
-  exit 1
-}
-Write-Host "MiniQuake BP-060-064R1 acceptance test: PASS" -ForegroundColor Green
-Write-Host "Result summary: $SummaryPath"
-Write-Host "For feedback, run .\COLLECT_RESULTS.ps1 and upload the generated ZIP."
-exit 0
+﻿$Target = Join-Path $PSScriptRoot "TEST_BP-060-064R2.ps1"
+Write-Warning "BP-060-064R1 is superseded by BP-060-064R2; forwarding to TEST_BP-060-064R2.ps1."
+& $Target @args
+exit $LASTEXITCODE
diff --git a/TEST_BP-060-064R2.ps1 b/TEST_BP-060-064R2.ps1
new file mode 100644
index 0000000000000000000000000000000000000000..5318aaaef63d8d2c9726d3a70faea32cc548fd12
--- /dev/null
+++ b/TEST_BP-060-064R2.ps1
@@ -0,0 +1,268 @@
+﻿[CmdletBinding()]
+param(
+  [string]$Compiler = "",
+  [Alias("StdLibPath", "ImportRoot")]
+  [string]$StdLib = "",
+  [string]$Python = "",
+  [string]$QuakeBase = "",
+  [string]$Game = "id1",
+  [string]$Map = "start",
+  [ValidateRange(1, 1000000)]
+  [int]$Frames = 300,
+  [ValidateRange(1, 1000000)]
+  [int]$TraceFrames = 128,
+  [ValidateRange(1, 1000000)]
+  [int]$RenderEvidenceFrame = 128,
+  [ValidateSet("Release", "Debug")]
+  [string]$Configuration = "Release",
+  [switch]$SkipBuild,
+  [switch]$SkipGameValidation,
+  [switch]$SkipTraceValidation,
+  [switch]$SkipRenderEvidence,
+  [switch]$SkipAudioEvidence,
+  [switch]$SkipNetworkEvidence,
+  [switch]$NetworkTests,
+  [switch]$RebuildNative,
+  [switch]$Listings,
+  [switch]$ContinueIndependentTests = $true,
+  [switch]$BisectOnFailure
+)
+
+$ErrorActionPreference = "Stop"
+Set-StrictMode -Version Latest
+
+# Force Python tools and the Python MiniLang compiler to flush every line.
+# The PowerShell wrappers below also forward every received line immediately.
+$env:PYTHONUNBUFFERED = "1"
+
+$PackageId = "BP-064"
+$ParentPackageId = "BP-063"
+$BlockId = "BP-060-064"
+$DeliveryRevision = "BP-060-064R2"
+$BlockParentPackageId = "BP-055-059R2"
+$Root = $PSScriptRoot
+$Build = Join-Path $Root "build"
+$BuildScript = Join-Path $Root "build.ps1"
+$GameExe = Join-Path $Build "MiniQuake.exe"
+$EvidenceExe = Join-Path $Build "MiniQuakeNetworkPlatformEvidence.exe"
+$TraceComparator = Join-Path $Root "tools\compare_traces.py"
+$RenderComparator = Join-Path $Root "tools\compare_render_evidence.py"
+$Timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
+$TranscriptPath = Join-Path $Build ("bp060-064r2-test-{0}.log" -f $Timestamp)
+$SummaryPath = Join-Path $Build "bp060-064r2-test-summary.json"
+$Steps = New-Object System.Collections.Generic.List[object]
+
+function Resolve-Tool([string]$Value, [string]$Label) {
+  if (-not [string]::IsNullOrWhiteSpace($Value) -and (Test-Path -LiteralPath $Value -PathType Leaf)) {
+    return [IO.Path]::GetFullPath($Value)
+  }
+  if (-not [string]::IsNullOrWhiteSpace($Value)) {
+    $Command = Get-Command $Value -ErrorAction SilentlyContinue | Select-Object -First 1
+    if ($null -ne $Command) { return $Command.Path }
+  }
+  throw "$Label not found: $Value"
+}
+
+function Add-Step([string]$Name, [string]$Status, [int]$ExitCode, [string]$Detail) {
+  $Steps.Add([ordered]@{ name = $Name; status = $Status; exit_code = $ExitCode; detail = $Detail })
+  $Color = if ($Status -eq "PASS") { "Green" } elseif ($Status -eq "SKIPPED") { "Yellow" } else { "Red" }
+  Write-Host ("[{0}] {1} - {2}" -f $Status, $Name, $Detail) -ForegroundColor $Color
+}
+
+function Failure-Marker([string]$Text) {
+  if ([string]::IsNullOrWhiteSpace($Text)) { return "" }
+  foreach ($Line in ($Text -split "`r?`n")) {
+    if ($Line -match '^\s*FAIL:\s*' -or $Line -match 'tests failed:\s*[1-9]' -or $Line -match 'evidence:\s*FAIL') { return $Line.Trim() }
+  }
+  return ""
+}
+
+function Invoke-LiveProcess(
+  [string]$Executable,
+  [string[]]$Arguments,
+  [string]$LogPath
+) {
+  $Lines = [System.Collections.Generic.List[string]]::new()
+  $Encoding = [System.Text.UTF8Encoding]::new($false)
+  $Writer = [System.IO.StreamWriter]::new($LogPath, $false, $Encoding)
+  $Code = 1
+  try {
+    & $Executable @Arguments 2>&1 |
+      ForEach-Object {
+        $Line = [string]$_
+        $Lines.Add($Line)
+        $Writer.WriteLine($Line)
+        $Writer.Flush()
+        Write-Host $Line
+      }
+    $Code = [int]$LASTEXITCODE
+  } finally {
+    $Writer.Flush()
+    $Writer.Dispose()
+  }
+  $Text = [string]::Join("`n", $Lines.ToArray())
+  return [pscustomobject]@{
+    exit_code = $Code
+    text = $Text
+  }
+}
+
+function Run-Logged([string]$Name, [string]$Executable, [string[]]$Arguments, [string]$LogName, [string]$RequiredMarker = "") {
+  $LogPath = Join-Path $Build $LogName
+  Write-Host ("[MiniQuake] starting {0}" -f $Name) -ForegroundColor Cyan
+  $Result = Invoke-LiveProcess -Executable $Executable -Arguments $Arguments -LogPath $LogPath
+  $Code = [int]$Result.exit_code
+  $Text = [string]$Result.text
+  $Marker = Failure-Marker $Text
+  if ($Code -eq 0 -and -not [string]::IsNullOrWhiteSpace($Marker)) { $Code = 1 }
+  if ($Code -eq 0 -and -not [string]::IsNullOrWhiteSpace($RequiredMarker) -and $Text -notmatch [regex]::Escape($RequiredMarker)) { $Code = 1; $Marker = "missing marker: $RequiredMarker" }
+  if ($Code -ne 0) { Add-Step $Name "FAIL" $Code ("log=" + $LogName + " " + $Marker); throw "$Name failed with exit code $Code" }
+  Add-Step $Name "PASS" 0 ("log=" + $LogName)
+  return $Text
+}
+
+function Run-NetworkEvidencePair([string]$Suffix, [int]$Port) {
+  $ServerOut = Join-Path $Build ("bp060-064r2-network-server-{0}.log" -f $Suffix)
+  $ServerErr = Join-Path $Build ("bp060-064r2-network-server-{0}.err.log" -f $Suffix)
+  $ClientLog = Join-Path $Build ("bp060-064r2-network-client-{0}.log" -f $Suffix)
+  Remove-Item -Force -ErrorAction SilentlyContinue $ServerOut, $ServerErr, $ClientLog
+  Write-Host ("[MiniQuake] starting network evidence server {0} on UDP port {1}" -f $Suffix, $Port) -ForegroundColor Cyan
+  $Server = Start-Process -FilePath $EvidenceExe -ArgumentList @("server", [string]$Port) -PassThru -NoNewWindow -RedirectStandardOutput $ServerOut -RedirectStandardError $ServerErr
+  Start-Sleep -Milliseconds 300
+  Write-Host ("[MiniQuake] starting network evidence client {0}" -f $Suffix) -ForegroundColor Cyan
+  $ClientResult = Invoke-LiveProcess -Executable $EvidenceExe -Arguments @("client", [string]$Port) -LogPath $ClientLog
+  $ClientCode = [int]$ClientResult.exit_code
+  $ClientText = [string]$ClientResult.text
+  if (-not $Server.WaitForExit(7000)) { Stop-Process -Id $Server.Id -Force -ErrorAction SilentlyContinue; throw "network evidence server $Suffix timed out" }
+  $ServerCode = $Server.ExitCode
+  $ServerText = ""
+  if (Test-Path $ServerOut) { $ServerText = Get-Content -Raw -LiteralPath $ServerOut }
+  if (Test-Path $ServerErr) { $ServerText += Get-Content -Raw -LiteralPath $ServerErr }
+  if (-not [string]::IsNullOrWhiteSpace($ServerText)) {
+    Write-Host ("[MiniQuake] network evidence server {0} output" -f $Suffix) -ForegroundColor Cyan
+    foreach ($ServerLine in ($ServerText -split "`r?`n")) {
+      if (-not [string]::IsNullOrEmpty($ServerLine)) { Write-Host $ServerLine }
+    }
+  }
+  if ($ServerCode -ne 0 -or $ClientCode -ne 0) { throw "network evidence pair $Suffix failed: server=$ServerCode client=$ClientCode" }
+  if ($ServerText -notmatch 'network platform evidence server: PASS') { throw "network evidence server $Suffix did not report PASS" }
+  if ($ClientText -notmatch 'network platform evidence: PASS') { throw "network evidence client $Suffix did not report PASS" }
+  return $ClientLog
+}
+
+New-Item -ItemType Directory -Force -Path $Build | Out-Null
+Start-Transcript -Path $TranscriptPath -Force | Out-Null
+$Failure = ""
+try {
+  Write-Host "MiniQuake BP-060-064R2 acceptance test"
+  Write-Host "  parent: $BlockParentPackageId"
+  Write-Host "  root:   $Root"
+  if ($BisectOnFailure) { Write-Host "  logical bisect markers enabled" }
+
+  if ([string]::IsNullOrWhiteSpace($Python)) { $Python = "python" }
+  $PythonExe = Resolve-Tool $Python "Python"
+
+  if (-not $SkipBuild) {
+    $PowerShellExe = (Get-Process -Id $PID).Path
+    $Arguments = @("-NoProfile", "-ExecutionPolicy", "Bypass", "-File", $BuildScript)
+    if (-not [string]::IsNullOrWhiteSpace($Compiler)) { $Arguments += @("-Compiler", $Compiler) }
+    if (-not [string]::IsNullOrWhiteSpace($StdLib)) { $Arguments += @("-StdLib", $StdLib) }
+    $Arguments += @("-Python", $PythonExe, "-Configuration", $Configuration)
+    if ($NetworkTests) { $Arguments += "-NetworkTests" }
+    if ($RebuildNative) { $Arguments += "-RebuildNative" }
+    if ($Listings) { $Arguments += "-Listings" }
+    [void](Run-Logged "single cumulative build and unit-test suite" $PowerShellExe $Arguments "bp060-064r2-build-child.log" "MiniQuake BP-064 network/platform closure tests passed: 24")
+  } else {
+    Add-Step "single cumulative build and unit-test suite" "SKIPPED" 0 "-SkipBuild"
+  }
+
+  foreach ($Path in @($GameExe, $EvidenceExe)) { if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) { throw "required executable missing: $Path" } }
+
+  $Version = Run-Logged "package identity" $GameExe @("--version") "bp060-064r2-version.txt" "Network/platform status: network_platform_109_frozen_v1"
+  if ($Version -notmatch 'Package: BP-064' -or $Version -notmatch 'Block: BP-060-064') { throw "compiled package identity mismatch" }
+
+  $CanUseGame = -not $SkipGameValidation -and -not [string]::IsNullOrWhiteSpace($QuakeBase)
+  if ($CanUseGame) {
+    if (-not (Test-Path -LiteralPath (Join-Path $QuakeBase "$Game\pak0.pak") -PathType Leaf)) { throw "Quake data not found: $QuakeBase\$Game\pak0.pak" }
+    [void](Run-Logged "installed Quake data validation" $GameExe @("--validate-game", $QuakeBase, $Map, "-game", $Game) "bp060-064r2-game-validation.log" "Validation result: PASS")
+    [void](Run-Logged "headless runtime validation" $GameExe @("--validate-runtime", $QuakeBase, $Map, [string]$Frames, "-game", $Game) "bp060-064r2-runtime-validation.log")
+  } else {
+    Add-Step "installed Quake data validation" "SKIPPED" 0 "no usable -QuakeBase or skipped"
+    Add-Step "headless runtime validation" "SKIPPED" 0 "no usable -QuakeBase or skipped"
+  }
+
+  if (-not $SkipAudioEvidence -and $CanUseGame) {
+    $AudioExe = Join-Path $Build "MiniQuakeAudioRetailEvidence.exe"
+    $A = Join-Path $Build "bp060-064r2-audio-a.log"; $B = Join-Path $Build "bp060-064r2-audio-b.log"
+    $TextA = Run-Logged "retail audio evidence A" $AudioExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($A)) "MiniQuake BP-059 retail audio evidence: PASS"
+    $TextB = Run-Logged "retail audio evidence B" $AudioExe @($QuakeBase, $Game) ([IO.Path]::GetFileName($B)) "MiniQuake BP-059 retail audio evidence: PASS"
+    $HashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $A).Hash.ToLowerInvariant(); $HashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $B).Hash.ToLowerInvariant()
+    if ($HashA -ne $HashB) { throw "retail audio evidence differs" }
+    Add-Step "byte-identical retail audio evidence" "PASS" 0 ("sha256=" + $HashA)
+  } else { Add-Step "retail audio evidence" "SKIPPED" 0 "skipped or no Quake data" }
+
+  if (-not $SkipTraceValidation -and $CanUseGame) {
+    $TraceDir = Join-Path $Build "bp060-064r2-traces"; Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $TraceDir; New-Item -ItemType Directory -Force -Path $TraceDir | Out-Null
+    $PrefixA = Join-Path $TraceDir "run-a"; $PrefixB = Join-Path $TraceDir "run-b"
+    [void](Run-Logged "compatibility trace A" $GameExe @("--compat-trace", $QuakeBase, $Map, [string]$TraceFrames, $PrefixA, "-game", $Game) "bp060-064r2-trace-a.log")
+    [void](Run-Logged "compatibility trace B" $GameExe @("--compat-trace", $QuakeBase, $Map, [string]$TraceFrames, $PrefixB, "-game", $Game) "bp060-064r2-trace-b.log")
+    [void](Run-Logged "byte-identical trace comparison" $PythonExe @($TraceComparator, ("{0}.mqtrace" -f $PrefixA), ("{0}.mqtrace" -f $PrefixB), "--json-output", (Join-Path $Build "bp060-064r2-trace-comparison.json")) "bp060-064r2-trace-comparison.log")
+  } else { Add-Step "deterministic compatibility traces" "SKIPPED" 0 "skipped or no Quake data" }
+
+  if (-not $SkipRenderEvidence -and $CanUseGame) {
+    $EvidenceDir = Join-Path $Build "bp060-064r2-render"; Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $EvidenceDir; New-Item -ItemType Directory -Force -Path $EvidenceDir | Out-Null
+    $EA = Join-Path $EvidenceDir "a"; $EB = Join-Path $EvidenceDir "b"
+    [void](Run-Logged "render evidence A" $GameExe @("--render-evidence", $QuakeBase, $Map, [string]$RenderEvidenceFrame, $EA, "-game", $Game) "bp060-064r2-render-a.log")
+    [void](Run-Logged "render evidence B" $GameExe @("--render-evidence", $QuakeBase, $Map, [string]$RenderEvidenceFrame, $EB, "-game", $Game) "bp060-064r2-render-b.log")
+    [void](Run-Logged "byte-identical render evidence" $PythonExe @($RenderComparator, ("{0}.tga" -f $EA), ("{0}.tga" -f $EB), "--require-exact", "--json-output", (Join-Path $Build "bp060-064r2-render-comparison.json")) "bp060-064r2-render-comparison.log")
+  } else { Add-Step "deterministic framebuffer evidence" "SKIPPED" 0 "skipped or no Quake data" }
+
+  if (-not $SkipNetworkEvidence) {
+    $PortA = Get-Random -Minimum 30000 -Maximum 45000
+    $PortB = Get-Random -Minimum 45001 -Maximum 60000
+    $ClientA = Run-NetworkEvidencePair "a" $PortA
+    $ClientB = Run-NetworkEvidencePair "b" $PortB
+    $HashA = (Get-FileHash -Algorithm SHA256 -LiteralPath $ClientA).Hash.ToLowerInvariant(); $HashB = (Get-FileHash -Algorithm SHA256 -LiteralPath $ClientB).Hash.ToLowerInvariant()
+    if ($HashA -ne $HashB) { throw "network evidence client reports differ: A=$HashA B=$HashB" }
+    Add-Step "two independent UDP control handshakes" "PASS" 0 ("sha256=" + $HashA)
+  } else { Add-Step "two independent UDP control handshakes" "SKIPPED" 0 "-SkipNetworkEvidence" }
+
+  if ($NetworkTests) { [void](Run-Logged "Winsock UDP loopback smoke" $GameExe @("--udp-smoke", "2000") "bp060-064r2-udp-smoke.log" "result=PASS") }
+  else { Add-Step "Winsock UDP loopback smoke" "SKIPPED" 0 "-NetworkTests not supplied" }
+
+  $Failed = @($Steps | Where-Object { $_.status -eq "FAIL" })
+  if ($Failed.Count -gt 0) { throw "independent test groups failed: $($Failed.Count)" }
+  Add-Step "MiniQuake BP-060-064R2 acceptance" "PASS" 0 "all required gates passed"
+} catch {
+  $Failure = $_.Exception.Message
+  Write-Host ("ERROR: " + $Failure) -ForegroundColor Red
+} finally {
+  $Status = if ([string]::IsNullOrWhiteSpace($Failure)) { "PASS" } else { "FAIL" }
+  $Summary = [ordered]@{
+    schema_version = 1
+    package_id = $PackageId
+    parent_package_id = $ParentPackageId
+    block_id = $BlockId
+    block_parent_package_id = $BlockParentPackageId
+    delivery_revision = $DeliveryRevision
+    network_platform_status = "network_platform_109_frozen_v1"
+    network_platform_fingerprint = "0xb3ec7589"
+    status = $Status
+    failure = $Failure
+    created_utc = (Get-Date).ToUniversalTime().ToString("o")
+    quake_base_supplied = -not [string]::IsNullOrWhiteSpace($QuakeBase)
+    steps = $Steps
+  }
+  $Summary | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $SummaryPath -Encoding UTF8
+  Stop-Transcript | Out-Null
+}
+
+if (-not [string]::IsNullOrWhiteSpace($Failure)) {
+  Write-Host "MiniQuake BP-060-064R2 acceptance test: FAIL" -ForegroundColor Red
+  Write-Host "Run .\COLLECT_RESULTS.ps1 and upload the generated ZIP."
+  exit 1
+}
+Write-Host "MiniQuake BP-060-064R2 acceptance test: PASS" -ForegroundColor Green
+Write-Host "Result summary: $SummaryPath"
+Write-Host "For feedback, run .\COLLECT_RESULTS.ps1 and upload the generated ZIP."
+exit 0
diff --git a/audit/network_control_golden.json b/audit/network_control_golden.json
index e96000a4a9d6f4282cd697d0f326734e268a674c..d4714542d1cb3c75bee5d747eff5e37ac20e4f98 100644
--- a/audit/network_control_golden.json
+++ b/audit/network_control_golden.json
@@ -4,7 +4,33 @@
   "protocol_version": 3,
   "game_name": "QUAKE",
   "host_cache_size": 8,
-  "request_opcodes": [1, 2, 3, 4],
-  "reply_opcodes": [129, 130, 131, 132, 133],
-  "fixtures": 24
+  "request_opcodes": [
+    1,
+    2,
+    3,
+    4
+  ],
+  "reply_opcodes": [
+    129,
+    130,
+    131,
+    132,
+    133
+  ],
+  "fixtures": 24,
+  "rule_enumeration": {
+    "first": [
+      "deathmatch",
+      "1"
+    ],
+    "next": [
+      "fraglimit",
+      "20"
+    ],
+    "end": [
+      "",
+      ""
+    ],
+    "unknown_previous": "no_reply"
+  }
 }
diff --git a/docs/BP-060-064R2_BLOCK_LEDGER.json b/docs/BP-060-064R2_BLOCK_LEDGER.json
new file mode 100644
index 0000000000000000000000000000000000000000..df48ad74b734fc845633e861a0cdc691809b8523
--- /dev/null
+++ b/docs/BP-060-064R2_BLOCK_LEDGER.json
@@ -0,0 +1,132 @@
+{
+  "schema_version": 1,
+  "project": "MiniQuake",
+  "block_id": "BP-060-064",
+  "parent_package_id": "BP-055-059R2",
+  "final_package_id": "BP-064",
+  "delivery_revision": "BP-060-064R2",
+  "network_platform_status": "network_platform_109_frozen_v1",
+  "network_platform_fingerprint": "0xb3ec7589",
+  "steps": [
+    {
+      "package_id": "BP-060",
+      "title": "NET main, qsocket pool, poll queue and active host port",
+      "patch": "patches/BP-060.diff",
+      "runtime_fixtures": 20,
+      "status": "Windows runtime 20/20 PASS in predecessor run"
+    },
+    {
+      "package_id": "BP-061",
+      "title": "Protocol 3 control, discovery and connection classification",
+      "patch": "patches/BP-061.diff",
+      "runtime_fixtures": 24,
+      "status": "R2 production and fixture correction; Windows revalidation pending"
+    },
+    {
+      "package_id": "BP-062",
+      "title": "WinSock address, partial IP and landriver parity",
+      "patch": "patches/BP-062.diff",
+      "runtime_fixtures": 24,
+      "status": "compiled in predecessor run; runtime execution pending BP-061 repair"
+    },
+    {
+      "package_id": "BP-063",
+      "title": "sys_win, QHOST/conproc and WinMain lifecycle",
+      "patch": "patches/BP-063.diff",
+      "runtime_fixtures": 21,
+      "status": "compiled in predecessor run; runtime execution pending BP-061 repair"
+    },
+    {
+      "package_id": "BP-064",
+      "title": "network/platform closure and cross-process UDP evidence",
+      "patch": "patches/BP-064.diff",
+      "runtime_fixtures": 24,
+      "status": "compiled in predecessor run; runtime execution pending BP-061 repair"
+    }
+  ],
+  "block_runtime_fixtures": 113,
+  "accepted_parent": {
+    "delivery_revision": "BP-055-059R2",
+    "status": "PASS",
+    "result_archive": "MiniQuake_BP-055-059R2_RESULTS_20260728-155430.zip",
+    "result_archive_sha256": "7634070813d28b57ffdd0c18064e493187c4a0fc47259e55450aa7a75d3b32fd",
+    "required_steps": "143/143 PASS",
+    "optional_steps": "1 Original GLQuake corpus comparison skipped",
+    "trace_sha256": "f8aef48166ea9ffc3bfd864802238feec1b1e8a01dad27f3e55e3b47b20d99c1",
+    "trace_rolling_hash": "d905b042",
+    "retail_audio_evidence_sha256": "9988060eb6c3bb7692338b97325d50c97c233473298194a39196795ec88d4337",
+    "udp_loopback": "PASS"
+  },
+  "acceptance": {
+    "single_cumulative_build": true,
+    "installed_game_validation": true,
+    "headless_frames": 300,
+    "trace_frames": 128,
+    "render_evidence": true,
+    "retail_audio_evidence": true,
+    "network_evidence_process_pairs": 2,
+    "network_evidence_exact_identity": true,
+    "udp_loopback": true
+  },
+  "scope_boundary": {
+    "network_protocol": "Protocol 15 gameplay and Protocol 3 connectionless control",
+    "landriver": "WinSock UDP",
+    "platform": "observable sys_win/conproc behavior with x64-safe native bridge",
+    "serial_modem_ipx": "legacy transports remain outside the mandatory modern Windows profile",
+    "quake_game_data": "external; never bundled"
+  },
+  "delivery_parent": "BP-060-064R1",
+  "delivery_hotfix": {
+    "revision": "BP-060-064R2",
+    "parent": "BP-060-064R1",
+    "scope": "Protocol-3 rule enumeration plus runtime fixture; inherits live output",
+    "live_output": true,
+    "python_unbuffered": true,
+    "rule_end_wire": "command_only",
+    "unknown_previous": "no_reply",
+    "production_src_changed": true,
+    "native_changed": false
+  },
+  "initial_delivery_result": {
+    "delivery_revision": "BP-060-064",
+    "result_archive": "MiniQuake_BP-060-064_RESULTS_20260728-184449.zip",
+    "result_archive_sha256": "bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719",
+    "status": "FAIL during BP-061 runtime fixture",
+    "compiled_targets": "65/65",
+    "bp060": "20/20 PASS",
+    "bp061": "19 fixtures completed; void indexing before fixture 20",
+    "later_runtime": "not reached"
+  },
+  "r1_delivery_hotfix": {
+    "revision": "BP-060-064R1",
+    "parent": "BP-060-064",
+    "scope": "live output and logging infrastructure",
+    "windows_run": "not executed separately",
+    "production_source_changed": false
+  },
+  "r2_delivery_hotfix": {
+    "revision": "BP-060-064R2",
+    "parent": "BP-060-064R1",
+    "scope": "Protocol-3 rule enumeration distinction plus BP-061 fixture",
+    "rule_end_wire": "command_only",
+    "unknown_previous": "no_reply",
+    "live_output": true,
+    "production_source_changed": true,
+    "native_changed": false,
+    "windows_acceptance": "pending"
+  },
+  "predecessor_result": {
+    "archive": "MiniQuake_BP-060-064_RESULTS_20260728-184449.zip",
+    "sha256": "bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719",
+    "status": "FAIL at BP-061 fixture 20 after 65/65 targets compiled",
+    "bp060": "20/20 PASS",
+    "first_failure": "Cannot index void in rule enumeration terminator fixture"
+  },
+  "r2_hotfix": {
+    "live_output_carried_forward": true,
+    "rule_end": "command-only CCREP_RULE_INFO",
+    "unknown_previous": "silent no reply",
+    "runtime_fixtures": 113,
+    "contract_fingerprint": "0xb3ec7589"
+  }
+}
diff --git a/docs/BP-060-064R2_HOTFIX_REPORT.json b/docs/BP-060-064R2_HOTFIX_REPORT.json
new file mode 100644
index 0000000000000000000000000000000000000000..e1e5125acec2f7f41ac44f9f29fe60c13fe9eef9
--- /dev/null
+++ b/docs/BP-060-064R2_HOTFIX_REPORT.json
@@ -0,0 +1,28 @@
+{
+  "schema_version": 1,
+  "delivery_revision": "BP-060-064R2",
+  "delivery_parent": "BP-060-064R1",
+  "engine_package": "BP-064",
+  "predecessor_result": {
+    "archive": "MiniQuake_BP-060-064_RESULTS_20260728-184449.zip",
+    "sha256": "bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719",
+    "compiled_targets": "65/65",
+    "bp060": "20/20 PASS",
+    "first_failure": "BP-061 rule enumeration terminator: Cannot index void"
+  },
+  "root_cause": {
+    "end_of_list": "was represented as void instead of a command-only reply pair",
+    "unknown_previous": "was indistinguishable from end-of-list and would incorrectly receive a reply"
+  },
+  "correction": {
+    "end_of_list": ["", ""],
+    "unknown_previous": "internal no-reply error sentinel",
+    "listener": "catches sentinel and sends no packet",
+    "runtime_fixture_count": 24,
+    "live_output": true
+  },
+  "production_source_changed": true,
+  "native_source_changed": false,
+  "contract_status": "network_platform_109_frozen_v1",
+  "contract_fingerprint": "0xb3ec7589"
+}
diff --git a/docs/BP-060-064R2_RESULT_ANALYSIS.md b/docs/BP-060-064R2_RESULT_ANALYSIS.md
new file mode 100644
index 0000000000000000000000000000000000000000..417055050a1d56115d1adf46dff48bd8775ccc03
--- /dev/null
+++ b/docs/BP-060-064R2_RESULT_ANALYSIS.md
@@ -0,0 +1,74 @@
+# BP-060–BP-064: Auswertung des ersten Windows-Laufs
+
+Der ursprüngliche, noch gepufferte BP-060–BP-064-Lauf wurde nicht abgebrochen,
+sondern lief bis zur ersten fehlerhaften BP-061-Runtimefixture weiter. R1 wurde
+nicht separat ausgeführt; seine Live-Output-Korrektur ist vollständig in R2
+enthalten.
+
+## Ergebnisgrenze
+
+- allgemeiner Preflight: PASS
+- 65 Windows executables / MiniLang-Ziele: erfolgreich kompiliert
+- sämtliche historischen Testgruppen bis einschließlich BP-059: PASS
+- BP-060 NET-main: 20/20 PASS
+- BP-061 Control: Abbruch nach Fixture 19/24
+- BP-062 bis BP-064 und externe Evidenz: wegen des frühen Abbruchs nicht erreicht
+
+Die konkrete Runtime-Diagnose lautet:
+
+```text
+[19/24] rule enumeration
+Error occured: no=1200 message=Cannot index void
+  at network_control_parity_tests.ml:63 in main
+```
+
+Ergebnisarchiv:
+
+```text
+MiniQuake_BP-060-064_RESULTS_20260728-184449.zip
+SHA-256: bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719
+```
+
+## Originalsemantik
+
+`net_dgrm.c` unterscheidet zwei Fälle bei `CCREQ_RULE_INFO`:
+
+1. Ist der angegebene Vorgängername unbekannt, wird die Anfrage still ignoriert.
+2. Wurde der letzte bekannte Server-Cvar erreicht, wird ein
+   `CCREP_RULE_INFO`-Paket ohne nachfolgende Strings gesendet.
+
+MiniQuake hatte beide Fälle intern als `void` repräsentiert. Der Serveradapter
+übersetzte jedes `void` in ein Terminatorpaket. Die Testfixture erwartete dagegen
+für das reguläre Ende ein leeres Feldpaar und indexierte deshalb den tatsächlich
+gelieferten `void`-Wert.
+
+## R2-Korrektur
+
+`nextServerRule` verwendet nun eine eindeutige Darstellung:
+
+```text
+gefundenes Element  -> [name, value]
+reguläres Listenende -> ["", ""]
+unbekannter Vorgänger -> interner error/no-reply-Sentinel
+```
+
+Der Listener wertet `nextServerRule` mit `try(...)` aus und sendet nur dann
+eine Antwort, wenn kein interner Fehler-Sentinel vorliegt. Das leere Feldpaar
+wird über `replyRuleInfo("", "")` zum originalen command-only
+Terminatorpaket. Ein unbekannter Vorgänger bleibt ohne Antwort.
+
+Die Runtimefixture prüft jetzt alle drei Ebenen zusammenhängend:
+
+- normale Enumeration,
+- internes leeres Endpaar und command-only Wirepaket,
+- stilles Ignorieren eines unbekannten Vorgängers.
+
+Der BP-061-Checker, das Golden-Dokument und das unabhängige C-Oracle binden diese
+Unterscheidung nun ausdrücklich. Die Fixturezahl bleibt 24; der Block bleibt bei
+113 neuen Runtimefixtures.
+
+## Live-Ausgabe
+
+Die nicht gepufferte Ausgabe aus R1 ist in R2 enthalten. Compiler-, Preflight-
+und Testausgaben werden zeilenweise sofort auf der Konsole angezeigt und
+gleichzeitig mit Flush in die Logdateien geschrieben.
diff --git a/docs/BP-060-064R2_TESTING.md b/docs/BP-060-064R2_TESTING.md
new file mode 100644
index 0000000000000000000000000000000000000000..6c2c04767b9e5401f3df091b50d7460a7b1604cd
--- /dev/null
+++ b/docs/BP-060-064R2_TESTING.md
@@ -0,0 +1,68 @@
+# BP-060–BP-064R2 Windows acceptance
+
+R2 combines the live-output runner with the BP-061 rule-enumeration correction.
+
+## Run
+
+Use a clean extracted directory and a Quake base directory containing `id1\pak0.pak`:
+
+```powershell
+$QuakeBase = "C:\path\to\Quake"
+Test-Path "$QuakeBase\id1\pak0.pak"
+
+.\TEST_BP-060-064R2.ps1 `
+  -Compiler C:\Users\nilsk\Desktop\MiniLangCompilerPy\mlc_win64.py `
+  -StdLib C:\Users\nilsk\Desktop\MiniLangCompilerPy `
+  -QuakeBase $QuakeBase `
+  -Game id1 `
+  -Map start `
+  -Frames 300 `
+  -TraceFrames 128 `
+  -NetworkTests `
+  -ContinueIndependentTests `
+  -BisectOnFailure
+```
+
+Compiler and runtime output is streamed live and written to `build\bp060-064r2-*.log`.
+
+## Repaired gate
+
+The BP-061 group must continue through:
+
+```text
+[19/24] rule enumeration
+[20/24] rule enumeration terminator
+```
+
+and end with:
+
+```text
+MiniQuake BP-061 network control tests passed: 24
+```
+
+The remaining new groups must report:
+
+```text
+MiniQuake BP-062 WinSock address tests passed: 24
+MiniQuake BP-063 system/platform tests passed: 21
+MiniQuake BP-064 network/platform closure tests passed: 24
+```
+
+The complete target is:
+
+```text
+MiniQuake BP-064 network platform evidence server: PASS
+MiniQuake BP-064 network platform evidence: PASS
+byte-identical trace comparison: PASS
+MiniQuake UDP loopback smoke
+  result=PASS
+MiniQuake BP-060-064R2 acceptance test: PASS
+```
+
+## Collect results
+
+```powershell
+.\COLLECT_RESULTS.ps1
+```
+
+The collector creates `build\MiniQuake_BP-060-064R2_RESULTS_*.zip` and excludes Quake data and compiled binaries.
diff --git a/docs/BP-060-064_RESULT_ANALYSIS.md b/docs/BP-060-064_RESULT_ANALYSIS.md
new file mode 100644
index 0000000000000000000000000000000000000000..cc0ba023c4f47d473e73fca5c7ae8d2a17923c5d
--- /dev/null
+++ b/docs/BP-060-064_RESULT_ANALYSIS.md
@@ -0,0 +1,45 @@
+# BP-060–BP-064 initial Windows result analysis
+
+Result archive: `MiniQuake_BP-060-064_RESULTS_20260728-184449.zip`  
+SHA-256: `bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719`
+
+## Result boundary
+
+The original buffered acceptance runner continued in the background and produced a complete diagnostic archive.
+
+- static source/package verification: PASS;
+- all 65 Windows executables compiled successfully;
+- all inherited runtime groups through BP-059 passed;
+- BP-060 network-main tests: 20/20 PASS;
+- BP-061 passed fixture 19 and stopped before fixture 20;
+- BP-062 through BP-064, installed-game validation, traces, render/audio evidence and UDP evidence were not reached.
+
+The exact diagnostic was:
+
+```text
+[19/24] rule enumeration
+Error occured: no=1200 message=Cannot index void
+  at network_control_parity_tests.ml:63 in main
+```
+
+## Root cause
+
+`nextServerRule(rules, "fraglimit")` returned `void` at the end of the list, while the fixture indexed the result as an empty rule/value pair.
+
+A direct comparison with `net_dgrm.c` exposed a second distinction that the old helper could not represent:
+
+1. after the final valid server cvar, Quake emits a command-only `CCREP_RULE_INFO` packet; this is the normal enumeration terminator;
+2. when the requested previous cvar does not exist, Quake returns without sending any packet.
+
+The old MiniQuake helper returned `void` for both outcomes. The fixture crashed on the first case, and the production listener would have incorrectly replied in the second case.
+
+## R2 correction
+
+R2 represents the two outcomes separately:
+
+- end of list: `["", ""]`, serialized as a command-only `CCREP_RULE_INFO` packet;
+- unknown previous rule: an internal no-reply error sentinel, caught by the listener and silently ignored.
+
+The BP-061 fixture now verifies both semantics and parses the generated command-only terminator.
+
+R2 also carries forward the live-output runner from the unexecuted R1 delivery. Compiler and test output is displayed immediately and flushed to logs.
diff --git a/docs/BP-061_NETWORK_CONTROL_AUDIT.md b/docs/BP-061_NETWORK_CONTROL_AUDIT.md
index 4061981a261101884429c00d0ec695de25d67da2..71cc13a3ccd34064b2d3977382603a572d4c96bb 100644
--- a/docs/BP-061_NETWORK_CONTROL_AUDIT.md
+++ b/docs/BP-061_NETWORK_CONTROL_AUDIT.md
@@ -4,3 +4,26 @@ BP-061 binds Protocol 3 connectionless requests and replies, the eight-entry
 host cache, duplicate suppression, wildcard advertised addresses, host-name
 conflict handling, server-rule enumeration, connect duplicate/replacement
 classification and IPv4 ban masks.
+
+## R2 rule-enumeration parity
+
+The original `net_dgrm.c` distinguishes the final two states of
+`CCREQ_RULE_INFO`:
+
+- When the requested previous cvar does not exist, the server returns without
+  sending a packet.
+- When the requested previous cvar is the final server cvar, the server emits a
+  command-only `CCREP_RULE_INFO` packet. No name/value strings follow.
+
+MiniQuake represents these states separately:
+
+```text
+next rule          -> [name, value]
+end of enumeration -> ["", ""] -> command-only reply
+unknown previous   -> internal error sentinel -> no reply
+```
+
+The listener catches the internal sentinel with `try(...)`. It is not exposed
+on the network and does not change the Protocol-3 wire format. Runtime fixture
+20 parses the command-only terminator; the C oracle and golden metadata also
+bind the silent unknown-previous path.
diff --git a/src/miniquake/net_loop.ml b/src/miniquake/net_loop.ml
index 08a54f0d57cf41a6428c8d0b2abf5107014f6e52..1ab4088d48e6beec6f1d954b96a39898bea4d173 100644
--- a/src/miniquake/net_loop.ml
+++ b/src/miniquake/net_loop.ml
@@ -405,10 +405,15 @@ function nextServerRule(rules, previous)
       if rules[index][0] == previous then found = index; break end if
       index = index + 1
     end while
-    if found < 0 then return void end if
+    // net_dgrm.c silently ignores a rule query whose previous cvar does not
+    // exist. Use an error value as an internal no-reply sentinel so the
+    // listener can distinguish it from the normal end-of-list marker.
+    if found < 0 then return error(3437, "unknown previous server rule") end if
     start = found + 1
   end if
-  if start >= len(rules) then return void end if
+  // After the final server cvar, Quake replies with CCREP_RULE_INFO and no
+  // strings. Represent that command-only reply as an empty name/value pair.
+  if start >= len(rules) then return ["", ""] end if
   return rules[start]
 end function
 
@@ -462,11 +467,14 @@ function pumpListener(state)
           processed = processed + 1
         end if
       else if parsed[0] == control.CCREQ_RULE_INFO then
-        rule = nextServerRule(state.serverRules, parsed[1][0])
-        response = void
-        if rule is void then response = control.replyRuleInfo("", "") else response = control.replyRuleInfo(rule[0], rule[1]) end if
-        udp.send(state.listener, received[1], received[2], response)
-        processed = processed + 1
+        rule = try(nextServerRule(state.serverRules, parsed[1][0]))
+        // An unknown previous cvar gets no reply. A valid end-of-list result
+        // is ["", ""] and emits the command-only enumeration terminator.
+        if rule is not error then
+          response = control.replyRuleInfo(rule[0], rule[1])
+          udp.send(state.listener, received[1], received[2], response)
+          processed = processed + 1
+        end if
       else if parsed[0] == control.CCREQ_CONNECT then
         if parsed[1][0] != control.GAME_NAME then
           // net_dgrm.c silently ignores requests for another game.
diff --git a/test.ps1 b/test.ps1
index 76df8cb955126bc996d91361529b9b1fe06f762b..a0a6e1d2c4ab08a1034a57cd7133588d90d6598f 100755
--- a/test.ps1
+++ b/test.ps1
@@ -1,2 +1,2 @@
-& "$PSScriptRoot\TEST_BP-060-064R1.ps1" @args
-exit $LASTEXITCODE
+﻿& "$PSScriptRoot\TEST_BP-060-064R2.ps1" @args
+exit $LASTEXITCODE
diff --git a/tests/network_control_parity_tests.ml b/tests/network_control_parity_tests.ml
index 290a9aa16e9440f3062637f126a417e174704256..31cedfe23f762bd2eb355d6d04b5770cf854811d 100644
--- a/tests/network_control_parity_tests.ml
+++ b/tests/network_control_parity_tests.ml
@@ -58,9 +58,11 @@ function main(args)
   bp061Check(netloop.listenerAddress("10.0.0.5", 26000) == "10.0.0.5:26000", "listener address text")
 
   rules = [["deathmatch", "1"], ["fraglimit", "20"]]
-  bp061Check(netloop.nextServerRule(rules, "")[0] == "deathmatch" and netloop.nextServerRule(rules, "deathmatch")[0] == "fraglimit", "rule enumeration")
+  unknownRule = try(netloop.nextServerRule(rules, "missing"))
+  bp061Check(netloop.nextServerRule(rules, "")[0] == "deathmatch" and netloop.nextServerRule(rules, "deathmatch")[0] == "fraglimit" and unknownRule is error, "rule enumeration")
   finalRule = netloop.nextServerRule(rules, "fraglimit")
-  bp061Check(finalRule[0] == "" and finalRule[1] == "", "rule enumeration terminator")
+  terminatorPacket = control.parse(control.replyRuleInfo(finalRule[0], finalRule[1]))
+  bp061Check(finalRule[0] == "" and finalRule[1] == "" and terminatorPacket[0] == control.CCREP_RULE_INFO and terminatorPacket[1][0] == "" and terminatorPacket[1][1] == "", "rule enumeration terminator")
 
   remote = netloop.createRemoteSocket(void, "127.0.0.1", 27000)
   remote.connectTime = 99.0
diff --git a/tools/check_network_061.py b/tools/check_network_061.py
index a2e2b98aa45163bed27d5550f562def5865c273a..0c2b5371ded4048936c2e4606df0e2a49085fac2 100755
--- a/tools/check_network_061.py
+++ b/tools/check_network_061.py
@@ -1,18 +1,46 @@
 #!/usr/bin/env python3
-import argparse,json,pathlib,sys
+"""Verify BP-061 Protocol-3 control and rule-enumeration parity."""
+from __future__ import annotations
+import argparse, json, pathlib, sys
 
-def main():
- p=argparse.ArgumentParser();p.add_argument('--root',default='.');p.add_argument('--json',default='');a=p.parse_args();r=pathlib.Path(a.root).resolve();e=[]
- loop=(r/'src/miniquake/net_loop.ml').read_text(encoding='utf-8-sig'); ctl=(r/'src/miniquake/net_control.ml').read_text(encoding='utf-8-sig'); t=(r/'tests/network_control_parity_tests.ml').read_text(encoding='utf-8-sig'); g=json.loads((r/'audit/network_control_golden.json').read_text())
- for m in ['const HOST_CACHE_SIZE = 8','len(result) < HOST_CACHE_SIZE','function uniqueHostName','function connectionRequestAction']:
-  if m not in loop:e.append('net_loop missing '+m)
- for m in ['const NET_PROTOCOL_VERSION = 3','const GAME_NAME = "QUAKE"','function requestConnect','function replyServerInfo']:
-  if m not in ctl:e.append('net_control missing '+m)
- if 'passed: 24' not in t:e.append('fixture marker missing')
- if g.get('fixtures')!=24 or g.get('host_cache_size')!=8:e.append('golden mismatch')
- out={'schema_version':1,'package':'BP-061','status':'PASS' if not e else 'FAIL','errors':e,'fixtures':24,'host_cache_size':8}
- if a.json:pathlib.Path(a.json).write_text(json.dumps(out,indent=2)+'\n')
- print('MiniQuake BP-061 network control verification: '+out['status'])
- for x in e:print('  [FAIL] '+x)
- return 0 if not e else 1
-if __name__=='__main__':sys.exit(main())
+def main() -> int:
+    ap=argparse.ArgumentParser(); ap.add_argument('--root',default='.'); ap.add_argument('--json',default=''); a=ap.parse_args()
+    r=pathlib.Path(a.root).resolve(); errors=[]
+    loop=(r/'src/miniquake/net_loop.ml').read_text(encoding='utf-8-sig')
+    ctl=(r/'src/miniquake/net_control.ml').read_text(encoding='utf-8-sig')
+    test=(r/'tests/network_control_parity_tests.ml').read_text(encoding='utf-8-sig')
+    golden=json.loads((r/'audit/network_control_golden.json').read_text(encoding='utf-8'))
+    oracle=(r/'tools/oracle/network_control_oracle.c').read_text(encoding='utf-8')
+
+    for m in ('const HOST_CACHE_SIZE = 8','len(result) < HOST_CACHE_SIZE','function uniqueHostName','function connectionRequestAction','function nextServerRule'):
+        if m not in loop: errors.append('net_loop missing '+m)
+    for m in (
+        'return error(3437, "unknown previous server rule")',
+        'if start >= len(rules) then return ["", ""] end if',
+        'rule = try(nextServerRule(state.serverRules, parsed[1][0]))',
+        'if rule is not error then',
+        'response = control.replyRuleInfo(rule[0], rule[1])',
+    ):
+        if m not in loop: errors.append('net_loop rule-enumeration contract missing: '+m)
+    for m in ('if found < 0 then return void end if','if start >= len(rules) then return void end if','if rule is void then response = control.replyRuleInfo'):
+        if m in loop: errors.append('legacy ambiguous rule-enumeration path remains: '+m)
+
+    for m in ('const NET_PROTOCOL_VERSION = 3','const GAME_NAME = "QUAKE"','function requestConnect','function replyServerInfo','function replyRuleInfo','if rule != "" then','if msg.remaining(reader) == 0 then fields = ["", ""]'):
+        if m not in ctl: errors.append('net_control missing '+m)
+
+    for m in ('passed: 24','unknownRule = try(netloop.nextServerRule(rules, "missing"))','unknownRule is error','finalRule[0] == "" and finalRule[1] == ""','terminatorPacket = control.parse(control.replyRuleInfo(finalRule[0], finalRule[1]))'):
+        if m not in test: errors.append('network-control runtime fixture missing: '+m)
+
+    expected={'first':['deathmatch','1'],'next':['fraglimit','20'],'end':['',''],'unknown_previous':'no_reply'}
+    if golden.get('fixtures')!=24 or golden.get('host_cache_size')!=8: errors.append('golden count/host-cache mismatch')
+    if golden.get('rule_enumeration')!=expected: errors.append('golden rule-enumeration semantics differ')
+    for m in ('rule_first=%s:%s','rule_next=%s:%s','rule_end=%s','rule_unknown=%s','"terminator"','"no_reply"'):
+        if m not in oracle: errors.append('network-control C oracle missing: '+m)
+
+    out={'schema_version':2,'package':'BP-061','delivery_revision':'BP-060-064R2','status':'PASS' if not errors else 'FAIL','errors':errors,'fixtures':24,'host_cache_size':8,'rule_end':'command_only_reply','unknown_previous':'no_reply'}
+    if a.json: pathlib.Path(a.json).write_text(json.dumps(out,indent=2)+'\n',encoding='utf-8')
+    print('MiniQuake BP-061 network control verification: '+out['status'])
+    if not errors: print('  fixtures=24 host_cache_size=8 rule_end=command_only unknown_previous=no_reply')
+    for e in errors: print('  [FAIL] '+e)
+    return 0 if not errors else 1
+if __name__=='__main__': sys.exit(main())
diff --git a/tools/oracle/network_control_oracle.c b/tools/oracle/network_control_oracle.c
index e87814f7b9059a37b0630c0bfd4f765a7c4c0508..71dccfad18efb1bcca71fa90af7d2e15551a9d75 100644
--- a/tools/oracle/network_control_oracle.c
+++ b/tools/oracle/network_control_oracle.c
@@ -1,10 +1,61 @@
-#include <stdint.h>
 #include <stdio.h>
+#include <string.h>
+
+typedef struct {
+    const char *name;
+    const char *value;
+    int server;
+} rule_t;
+
+/* Return 0 for net_dgrm.c's silent unknown-previous path. Return 1 when a
+ * reply is emitted; empty strings represent the command-only terminator. */
+static int next_rule(const rule_t *rules, int count, const char *previous,
+                     const char **name, const char **value) {
+    int start = 0;
+    int i;
+    if (previous[0] != '\0') {
+        int found = -1;
+        for (i = 0; i < count; ++i) {
+            if (strcmp(rules[i].name, previous) == 0) {
+                found = i;
+                break;
+            }
+        }
+        if (found < 0) return 0;
+        start = found + 1;
+    }
+    for (i = start; i < count; ++i) {
+        if (rules[i].server) {
+            *name = rules[i].name;
+            *value = rules[i].value;
+            return 1;
+        }
+    }
+    *name = "";
+    *value = "";
+    return 1;
+}
+
 int main(void) {
-  printf("protocol=3\n");
-  printf("game=QUAKE\n");
-  printf("host_cache=8\n");
-  printf("request=1,2,3,4\n");
-  printf("reply=129,130,131,132,133\n");
-  return 0;
+    const rule_t rules[] = {
+        {"deathmatch", "1", 1},
+        {"developer", "0", 0},
+        {"fraglimit", "20", 1},
+    };
+    const char *name = NULL;
+    const char *value = NULL;
+
+    printf("protocol=3\n");
+    printf("game=QUAKE\n");
+    printf("host_cache=8\n");
+    printf("request=1,2,3,4\n");
+    printf("reply=129,130,131,132,133\n");
+    if (!next_rule(rules, 3, "", &name, &value)) return 1;
+    printf("rule_first=%s:%s\n", name, value);
+    if (!next_rule(rules, 3, "deathmatch", &name, &value)) return 1;
+    printf("rule_next=%s:%s\n", name, value);
+    if (!next_rule(rules, 3, "fraglimit", &name, &value)) return 1;
+    printf("rule_end=%s\n", name[0] == '\0' && value[0] == '\0' ? "terminator" : "invalid");
+    printf("rule_unknown=%s\n", next_rule(rules, 3, "missing", &name, &value) ? "reply" : "no_reply");
+    return 0;
 }
diff --git a/tools/verify.py b/tools/verify.py
index a1026770b3c9bf14eea869ff52ea73333a97650f..f58ab67d71510124535c146c5f207de18c140c08 100755
--- a/tools/verify.py
+++ b/tools/verify.py
@@ -24,8 +24,8 @@ PACKAGE_ID = "BP-064"
 PARENT_PACKAGE_ID = "BP-063"
 BLOCK_ID = "BP-060-064"
 BLOCK_PARENT_PACKAGE_ID = "BP-055-059R2"
-DELIVERY_REVISION = "BP-060-064R1"
-DELIVERY_PARENT = "BP-060-064"
+DELIVERY_REVISION = "BP-060-064R2"
+DELIVERY_PARENT = "BP-060-064R1"
 PROTOCOL_STATUS = "protocol15_frozen_v1"
 QUAKEC_STATUS = "quakec_109_frozen_v1"
 WORLD_PHYSICS_STATUS = "world_physics_109_frozen_v1"
@@ -550,6 +550,10 @@ REQUIRED_PATHS.update({
     "docs/BP-060-064R1_TESTING.md", "docs/BP-060-064R1_RESULT_ANALYSIS.md",
     "docs/BP-060-064R1_HOTFIX_REPORT.json", "docs/BP-060-064R1_BLOCK_LEDGER.json",
     "patches/BP-064R1.diff",
+    "CHANGELOG_BP-060-064R2.md", "TEST_BP-060-064R2.ps1",
+    "docs/BP-060-064R2_TESTING.md", "docs/BP-060-064R2_RESULT_ANALYSIS.md",
+    "docs/BP-060-064R2_HOTFIX_REPORT.json", "docs/BP-060-064R2_BLOCK_LEDGER.json",
+    "patches/BP-064R2.diff",
     "src/miniquake/network_platform_contract.ml",
     "tests/network_main_parity_tests.ml", "tests/network_control_parity_tests.ml",
     "tests/network_wins_address_tests.ml", "tests/system_platform_lifecycle_tests.ml",
@@ -4894,517 +4898,6 @@ def check_bp055059r2_audio_runtime_hotfix_contract(root: Path) -> CheckResult:
         errors.append(f"cannot validate BP-055-059R2 audio runtime hotfix: {exc}")
     return CheckResult("bp055059r2_audio_runtime_hotfix_contract", not errors, details, errors)
 
-def check_current_delivery_scripts(root: Path) -> CheckResult:
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    expected: dict[str, list[str]] = {
-        "build.ps1": [
-            '$PackageId = "BP-059"', '$ParentPackageId = "BP-058"',
-            '$BlockId = "BP-055-059"', '$BlockParentPackageId = "BP-050-054R1"',
-            '$AudioStatus = "audio_109_frozen_v1"', "--allow-downstream-package",
-            "MiniQuakeAudioMemoryTests.exe", "MiniQuakeAudioRetailEvidence.exe",
-            "check_audio_055.py", "check_audio_059.py",
-        ],
-        "TEST_BP-055-059R1.ps1": [
-            '$PackageId = "BP-059"', '$BlockId = "BP-055-059"',
-            '$DeliveryRevision = "BP-055-059R1"', '$AudioStatus = "audio_109_frozen_v1"',
-            "--allow-downstream-package", "MiniQuakeAudioRetailEvidence.exe",
-            "byte-identical retail audio evidence", "misc/menu1.wav", "ambience/water1.wav",
-            "ContinueIndependentTests", "BisectOnFailure",
-            "MiniQuake $DeliveryRevision acceptance test: PASS",
-        ],
-        "COLLECT_RESULTS.ps1": [
-            "MiniQuake_BP-055-059R1_RESULTS_", '$DeliveryRevision = "BP-055-059R1"',
-            '$DeliveryParent = "BP-055-059"', "audio_closure_golden.json",
-            "MiniQuakeAudioRetailEvidence.exe", "Compress-Archive",
-            "MiniQuakeProtocol15ServerDataTests.exe", "BP-012_PROTOCOL15_SERVERDATA_AUDIT.md",
-            "protocol15_serverdata_golden.json", "BP-059_AUDIO_CLOSURE_AUDIT.md",
-            "BP-055-059R1_HOTFIX_REPORT.json", "patches\\BP-059R1.diff",
-        ],
-        "test.ps1": ["TEST_BP-055-059R1.ps1"],
-    }
-    for relative, markers in expected.items():
-        path = root / relative
-        try:
-            text = path.read_text(encoding="utf-8-sig")
-        except OSError as exc:
-            errors.append(f"cannot read {relative}: {exc}")
-            continue
-        for marker in markers:
-            if marker not in text:
-                errors.append(f"{relative} missing marker: {marker}")
-    details["scripts_checked"] = len(expected)
-    return CheckResult("delivery_scripts", not errors, details, errors)
-
-
-def check_package_identity(root: Path) -> CheckResult:
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    try:
-        text = (root / "src/miniquake/build_info.ml").read_text(encoding="utf-8")
-        expected = {
-            "PACKAGE_ID": PACKAGE_ID, "PARENT_PACKAGE_ID": PARENT_PACKAGE_ID,
-            "BLOCK_ID": BLOCK_ID, "BLOCK_PARENT_PACKAGE_ID": BLOCK_PARENT_PACKAGE_ID,
-            "PROTOCOL_STATUS": PROTOCOL_STATUS, "QUAKEC_STATUS": QUAKEC_STATUS,
-            "WORLD_PHYSICS_STATUS": WORLD_PHYSICS_STATUS,
-            "HOST_LIFECYCLE_STATUS": HOST_LIFECYCLE_STATUS,
-            "CLIENT_RENDER_STATUS": CLIENT_RENDER_STATUS,
-            "WORLD_RENDER_STATUS": WORLD_RENDER_STATUS,
-            "MODEL_UI_RENDER_STATUS": MODEL_UI_RENDER_STATUS,
-            "COMPATIBILITY_PROFILE": "compat_109",
-            "NATIVE_TEXT_ABI": "caller_owned_bytes_v1",
-            "PROTOCOL_TEXT_ABI": "quake_latin1_cstring_v1",
-        }
-        for name, wanted in expected.items():
-            match = re.search(rf'^const\s+{name}\s*=\s*"([^"]+)"\s*$', text, flags=re.M)
-            if not match or match.group(1) != wanted:
-                errors.append(f"build_info.ml {name} differs from {wanted!r}")
-            else: details[name.lower()] = wanted
-        numeric = {
-            "HOST_LIFECYCLE_FINGERPRINT": HOST_LIFECYCLE_FINGERPRINT,
-            "CLIENT_RENDER_FINGERPRINT": CLIENT_RENDER_FINGERPRINT,
-            "WORLD_RENDER_FINGERPRINT": WORLD_RENDER_FINGERPRINT,
-            "MODEL_UI_RENDER_FINGERPRINT": MODEL_UI_RENDER_FINGERPRINT,
-        }
-        for name, wanted in numeric.items():
-            match = re.search(rf'^const\s+{name}\s*=\s*(0x[0-9a-fA-F]+|\d+)\s*$', text, flags=re.M)
-            if not match or int(match.group(1), 0) != wanted:
-                errors.append(f"build_info.ml {name} differs from 0x{wanted:08x}")
-        main = (root / "src/main.ml").read_text(encoding="utf-8")
-        for marker in (
-            "buildInfo.MODEL_UI_RENDER_STATUS",
-            "Model/UI/render fingerprint: 0x0a62f5b1",
-            "--render-evidence", "runRenderEvidenceCommand",
-        ):
-            if marker not in main: errors.append(f"src/main.ml missing identity/command marker: {marker}")
-        host = (root / "src/miniquake/host.ml").read_text(encoding="utf-8")
-        if "compatDiagnostics.u32Hex(result[5])" not in host:
-            errors.append("render-evidence command does not use the imported diagnostic alias")
-        purpose = re.search(r'^const\s+PACKAGE_PURPOSE\s*=\s*"([^"]+)"', text, flags=re.M)
-        purpose_text = purpose.group(1).lower() if purpose else ""
-        if not all(term in purpose_text for term in ("alias", "sprite", "render")):
-            errors.append("PACKAGE_PURPOSE does not describe the current block")
-    except OSError as exc:
-        errors.append(f"cannot read package identity files: {exc}")
-    return CheckResult("package_identity", not errors, details, errors)
-
-
-
-def check_frozen_parent_contracts(root: Path) -> CheckResult:
-    """Validate all accepted parent fingerprints and the BP-045-049R3 result."""
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    try:
-        build = (root / "src/miniquake/build_info.ml").read_text(encoding="utf-8")
-        string_values = {
-            "PROTOCOL_STATUS": PROTOCOL_STATUS,
-            "QUAKEC_STATUS": QUAKEC_STATUS,
-            "WORLD_PHYSICS_STATUS": WORLD_PHYSICS_STATUS,
-            "HOST_LIFECYCLE_STATUS": HOST_LIFECYCLE_STATUS,
-            "CLIENT_RENDER_STATUS": CLIENT_RENDER_STATUS,
-            "WORLD_RENDER_STATUS": WORLD_RENDER_STATUS,
-            "MODEL_UI_RENDER_STATUS": MODEL_UI_RENDER_STATUS,
-        }
-        for key, wanted in string_values.items():
-            match = re.search(rf'^const\s+{key}\s*=\s*"([^"]+)"\s*$', build, flags=re.M)
-            if not match or match.group(1) != wanted:
-                errors.append(f"build_info.ml {key} differs from {wanted!r}")
-        numeric_values = {
-            "HOST_LIFECYCLE_FINGERPRINT": HOST_LIFECYCLE_FINGERPRINT,
-            "CLIENT_RENDER_FINGERPRINT": CLIENT_RENDER_FINGERPRINT,
-            "WORLD_RENDER_FINGERPRINT": WORLD_RENDER_FINGERPRINT,
-            "MODEL_UI_RENDER_FINGERPRINT": MODEL_UI_RENDER_FINGERPRINT,
-        }
-        for key, wanted in numeric_values.items():
-            match = re.search(rf'^const\s+{key}\s*=\s*(0x[0-9a-fA-F]+|\d+)\s*$', build, flags=re.M)
-            if not match or int(match.group(1), 0) != wanted:
-                errors.append(f"build_info.ml {key} differs from 0x{wanted:08x}")
-        analysis = (root / "docs/BP-045-049R3_ACCEPTANCE_ANALYSIS.md").read_text(encoding="utf-8")
-        markers = (
-            "112/112",
-            "65bb521faf300fed61b7d03e15429896a31de27d934ee59459e251fac08bd2bf",
-            "b2589f2712b0aed42f31b1a35ebb4837c211391ba3e8d8888c422242d051af28",
-            "d905b042",
-            "1325e6fd365c1e62fbf36c4fadcbe9110faa94778c4c14b43fa9efe56d35e154",
-            "55f9fc9c", "3ec42fe3", "SSIM: `1.0`",
-            "model_ui_render_109_frozen_v1", "0x0a62f5b1",
-        )
-        for marker in markers:
-            if marker not in analysis:
-                errors.append(f"BP-045-049R3 acceptance analysis is missing {marker}")
-        details.update({
-            "accepted_parent": "BP-045-049R3",
-            "parent_result_sha256": "65bb521faf300fed61b7d03e15429896a31de27d934ee59459e251fac08bd2bf",
-            "parent_trace_sha256": "b2589f2712b0aed42f31b1a35ebb4837c211391ba3e8d8888c422242d051af28",
-            "parent_render_ssim": 1.0,
-        })
-    except OSError as exc:
-        errors.append(f"cannot validate frozen parent contracts: {exc}")
-    return CheckResult("frozen_parent_contracts", not errors, details, errors)
-
-
-def check_render_corpus_comparator(root: Path) -> CheckResult:
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    path = root / "tools/compare_render_corpus.py"
-    try:
-        completed = subprocess.run(
-            [sys.executable, str(path), "--root", str(root), "--self-test"],
-            cwd=root, capture_output=True, text=True, timeout=30, check=False,
-        )
-        details.update({"returncode": completed.returncode, "stdout": completed.stdout.strip()})
-        if completed.returncode != 0:
-            errors.append(completed.stdout.strip() or completed.stderr.strip() or "render corpus comparator self-test failed")
-    except (OSError, subprocess.SubprocessError) as exc:
-        errors.append(f"cannot run render corpus comparator: {exc}")
-    return CheckResult("render_evidence_corpus_comparator", not errors, details, errors)
-
-
-def check_bp050054r1_cvar_fixture_contract(root: Path) -> CheckResult:
-    """Bind the BP-054 Windows fixture failure and its CommandLine adapter fix."""
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    try:
-        test = (root / "tests/render_special_closure_tests.ml").read_text(encoding="utf-8")
-        checker = (root / "tools/check_render_special_054.py").read_text(encoding="utf-8")
-        analysis = (root / "docs/BP-050-054_RESULT_ANALYSIS.md").read_text(encoding="utf-8")
-        changelog = (root / "CHANGELOG_BP-050-054R1.md").read_text(encoding="utf-8")
-        ledger = json.loads((root / "BLOCK_LEDGER.json").read_text(encoding="utf-8"))
-        port_ledger = json.loads((root / "PORT_LEDGER.json").read_text(encoding="utf-8"))
-        hotfix = json.loads((root / "docs/BP-050-054R1_HOTFIX_REPORT.json").read_text(encoding="utf-8"))
-
-        test_markers = (
-            "import miniquake.common as bp054Common",
-            "commandLine = bp054Common.create([])",
-            "bp054Host.createCvars(commandLine, true)",
-            "MiniQuake BP-054 render-special closure tests passed: 24",
-        )
-        for item in test_markers:
-            if item not in test:
-                errors.append(f"BP-054 R1 fixture missing marker: {item}")
-        if 'bp054Host.createCvars("", true)' in test:
-            errors.append("BP-054 R1 fixture still passes a string to host.createCvars")
-        if test.count("bp054Run(") != 25:
-            errors.append("BP-054 R1 fixture count differs from 24")
-
-        checker_markers = (
-            "canonical command-line constructor",
-            "bp054Common.create([])",
-            "host.createCvars",
-            "still passes a string",
-        )
-        for item in checker_markers:
-            if item not in checker:
-                errors.append(f"BP-054 checker missing R1 guard marker: {item}")
-
-        evidence_markers = (
-            "4e2afe7a4266486fd3b60c6ed5d5e5c4995c34fa96234979b3dd11c2733d61e9",
-            "Cannot access member 'commandLine' on non-struct value",
-            "bbfb59d8d0225a9a0401f3beb59a0422a57f898aebc35f239440e8cece742ad0",
-            "129 PASS, one FAIL",
-            "test-adapter defect",
-        )
-        lower_analysis = analysis.lower()
-        for item in evidence_markers:
-            if item.lower() not in lower_analysis:
-                errors.append(f"R1 result analysis missing evidence marker: {item}")
-
-        for item in ("test adapter", "common.create([])", "no production source", "0x2a3d8081"):
-            if item.lower() not in changelog.lower():
-                errors.append(f"R1 changelog missing marker: {item}")
-
-        expected_ledger = {
-            "delivery_revision": DELIVERY_REVISION,
-            "delivery_parent": DELIVERY_PARENT,
-        }
-        for key, wanted in expected_ledger.items():
-            if ledger.get(key) != wanted:
-                errors.append(f"BLOCK_LEDGER.json {key} differs: {ledger.get(key)!r}")
-        predecessor = ledger.get("predecessor_delivery_result", {})
-        if predecessor.get("result_archive_sha256") != "4e2afe7a4266486fd3b60c6ed5d5e5c4995c34fa96234979b3dd11c2733d61e9":
-            errors.append("BLOCK_LEDGER.json predecessor result hash differs")
-        history = ledger.get("delivery_hotfix_history", [])
-        delivery = next((item for item in history if isinstance(item, dict) and item.get("revision") == DELIVERY_REVISION), None)
-        if delivery is None:
-            errors.append("BLOCK_LEDGER.json does not preserve the R1 hotfix")
-            delivery = {}
-        for key, wanted in (
-            ("parent", DELIVERY_PARENT),
-            ("production_source_changed", False),
-            ("native_source_changed", False),
-            ("engine_contracts_changed", False),
-            ("fixture_count_changed", False),
-        ):
-            if delivery.get(key) != wanted:
-                errors.append(f"BLOCK_LEDGER.json R1 {key} differs: {delivery.get(key)!r}")
-
-        if port_ledger.get("current_delivery_revision") != DELIVERY_REVISION:
-            errors.append("PORT_LEDGER.json current delivery revision differs")
-        if port_ledger.get("delivery_parent") != DELIVERY_PARENT:
-            errors.append("PORT_LEDGER.json delivery parent differs")
-        port_hotfix = port_ledger.get("bp050054r1_hotfix", {})
-        if port_hotfix.get("delivery_revision") != DELIVERY_REVISION:
-            errors.append("PORT_LEDGER.json does not record the R1 hotfix")
-        if port_hotfix.get("production_source_changed") is not False:
-            errors.append("PORT_LEDGER.json incorrectly records a production-source change")
-
-        expected_hotfix = {
-            "revision": DELIVERY_REVISION,
-            "parent": DELIVERY_PARENT,
-            "result_archive_sha256": "4e2afe7a4266486fd3b60c6ed5d5e5c4995c34fa96234979b3dd11c2733d61e9",
-            "production_source_changed": False,
-            "native_source_changed": False,
-            "engine_contracts_changed": False,
-            "fixture_count_changed": False,
-        }
-        for key, wanted in expected_hotfix.items():
-            if hotfix.get(key) != wanted:
-                errors.append(f"R1 hotfix report {key} differs: {hotfix.get(key)!r}")
-
-        patch = root / "patches/BP-054R1.diff"
-        if not patch.is_file() or patch.stat().st_size == 0:
-            errors.append("patches/BP-054R1.diff is missing or empty")
-        else:
-            patch_text = patch.read_text(encoding="utf-8", errors="replace")
-            if re.search(r"^[+-]{3} [ab]/src/", patch_text, flags=re.M) or re.search(r"^[+-]{3} [ab]/native/", patch_text, flags=re.M):
-                errors.append("BP-054R1 logical patch changes production or native source")
-            if "tests/render_special_closure_tests.ml" not in patch_text:
-                errors.append("BP-054R1 logical patch does not include the corrected fixture")
-
-        completed = subprocess.run(
-            [sys.executable, str(root / "tools/check_render_special_054.py"), "--root", str(root)],
-            cwd=root, capture_output=True, text=True, timeout=60, check=False,
-        )
-        if completed.returncode != 0:
-            errors.append("BP-054 checker rejects the R1 tree: " + completed.stdout.strip())
-
-        details.update({
-            "delivery_revision": DELIVERY_REVISION,
-            "delivery_parent": DELIVERY_PARENT,
-            "fixture_count": 24,
-            "production_source_changed": False,
-            "native_source_changed": False,
-            "checker_exit_code": completed.returncode,
-        })
-    except (OSError, json.JSONDecodeError, subprocess.SubprocessError) as exc:
-        errors.append(f"cannot validate BP-050-054R1 cvar fixture contract: {exc}")
-    return CheckResult("bp050054r1_cvar_fixture_contract", not errors, details, errors)
-
-
-def check_render_special_block_contract(root: Path) -> CheckResult:
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    try:
-        ledger = json.loads((root / "BLOCK_LEDGER.json").read_text(encoding="utf-8"))
-        expected = {
-            "block_id": BLOCK_ID,
-            "parent_package_id": BLOCK_PARENT_PACKAGE_ID,
-            "final_package_id": PACKAGE_ID,
-            "delivery_revision": DELIVERY_REVISION,
-            "delivery_parent": DELIVERY_PARENT,
-            "render_special_status": RENDER_SPECIAL_STATUS,
-            "render_special_fingerprint": "0x2a3d8081",
-        }
-        for key, wanted in expected.items():
-            if ledger.get(key) != wanted:
-                errors.append(f"BLOCK_LEDGER.json {key} is {ledger.get(key)!r}, expected {wanted!r}")
-        steps = ledger.get("steps", [])
-        ids = [item.get("package_id") for item in steps if isinstance(item, dict)]
-        counts = [item.get("runtime_fixtures") for item in steps if isinstance(item, dict)]
-        if ids != ["BP-050", "BP-051", "BP-052", "BP-053", "BP-054"]:
-            errors.append(f"block step order differs: {ids!r}")
-        if counts != [22, 20, 20, 18, 24] or ledger.get("block_runtime_fixtures") != 104:
-            errors.append(f"block fixture vector differs: {counts!r}, total={ledger.get('block_runtime_fixtures')!r}")
-        parent = ledger.get("accepted_parent", {})
-        parent_expected = {
-            "delivery_revision": "BP-045-049R3",
-            "status": "PASS",
-            "result_archive_sha256": "65bb521faf300fed61b7d03e15429896a31de27d934ee59459e251fac08bd2bf",
-            "trace_sha256": "b2589f2712b0aed42f31b1a35ebb4837c211391ba3e8d8888c422242d051af28",
-            "trace_rolling_hash": "d905b042",
-            "render_tga_sha256": "1325e6fd365c1e62fbf36c4fadcbe9110faa94778c4c14b43fa9efe56d35e154",
-            "render_ssim": 1.0,
-        }
-        for key, wanted in parent_expected.items():
-            if parent.get(key) != wanted:
-                errors.append(f"accepted parent {key} differs: {parent.get(key)!r}")
-        contract = (root / "src/miniquake/render_special_contract.ml").read_text(encoding="utf-8")
-        if RENDER_SPECIAL_STATUS not in contract or "0x2a3d8081" not in contract.lower():
-            errors.append("render-special contract source differs")
-        golden = json.loads((root / "audit/render_special_closure_golden.json").read_text(encoding="utf-8"))
-        if golden.get("fingerprint") != RENDER_SPECIAL_FINGERPRINT:
-            errors.append("render-special golden fingerprint differs")
-        if golden.get("fixtures") != 24 or golden.get("total_block_fixtures") != 104:
-            errors.append("render-special golden fixture counts differ")
-        for package in ids:
-            patch = root / "patches" / f"{package}.diff"
-            if not patch.is_file() or patch.stat().st_size == 0:
-                errors.append(f"logical block patch is missing or empty: patches/{package}.diff")
-        details.update({
-            "steps": ids,
-            "fixture_vector": counts,
-            "runtime_fixtures": ledger.get("block_runtime_fixtures"),
-            "contract_status": RENDER_SPECIAL_STATUS,
-            "contract_fingerprint": "0x2a3d8081",
-            "optional_original_ssim_floor": 0.95,
-        })
-    except (OSError, json.JSONDecodeError, ValueError, TypeError) as exc:
-        errors.append(f"cannot validate render-special block contract: {exc}")
-    return CheckResult("render_special_block_contract", not errors, details, errors)
-
-
-def check_current_delivery_scripts(root: Path) -> CheckResult:
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    expected: dict[str, list[str]] = {
-        "build.ps1": [
-            '$PackageId = "BP-054"', '$ParentPackageId = "BP-053"',
-            '$BlockId = "BP-050-054"', '$BlockParentPackageId = "BP-045-049R3"',
-            '$RenderSpecialStatus = "render_special_109_frozen_v1"',
-            "MiniQuakeMirrorSpecialRenderTests.exe", "MiniQuakeRenderSpecialClosureTests.exe",
-            "check_render_special_050.py", "check_render_special_054.py", "compare_render_corpus.py",
-        ],
-        "TEST_BP-050-054R1.ps1": [
-            '$PackageId = "BP-054"', '$BlockId = "BP-050-054"',
-            '$DeliveryRevision = "BP-050-054R1"',
-            "OriginalRenderReference", "SkipRenderCorpus", "ContinueIndependentTests", "BisectOnFailure",
-            "name = \"start-064\"", "name = \"e1m1-128\"",
-            "compare_render_corpus.py", "MiniQuake $DeliveryRevision acceptance test: PASS",
-            "bp050-054r1-render-special-closure-tests.log",
-        ],
-        "COLLECT_RESULTS.ps1": [
-            "MiniQuake_BP-050-054R1_RESULTS_", ".mqtrace", ".tga",
-            '$DeliveryRevision = "BP-050-054R1"', '$DeliveryParent = "BP-050-054"',
-            "render_special_closure_golden.json", "Compress-Archive",
-            "MiniQuakeProtocol15ServerDataTests.exe", "BP-012_PROTOCOL15_SERVERDATA_AUDIT.md",
-            "protocol15_serverdata_golden.json", "BP-054_RENDER_SPECIAL_CLOSURE_AUDIT.md",
-            "BP-050-054_RESULT_ANALYSIS.md", "BP-050-054R1_TESTING.md",
-            "BP-050-054R1_HOTFIX_REPORT.json", "BP-054R1.diff",
-        ],
-        "test.ps1": ["TEST_BP-050-054R1.ps1"],
-    }
-    for relative, markers in expected.items():
-        path = root / relative
-        try:
-            text = path.read_text(encoding="utf-8-sig")
-        except OSError as exc:
-            errors.append(f"cannot read {relative}: {exc}")
-            continue
-        for marker in markers:
-            if marker not in text:
-                errors.append(f"{relative} missing marker: {marker}")
-    details["scripts_checked"] = len(expected)
-    return CheckResult("delivery_scripts", not errors, details, errors)
-
-def check_package_identity(root: Path) -> CheckResult:
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    try:
-        text = (root / "src/miniquake/build_info.ml").read_text(encoding="utf-8")
-        expected = {
-            "PACKAGE_ID": PACKAGE_ID, "PARENT_PACKAGE_ID": PARENT_PACKAGE_ID,
-            "BLOCK_ID": BLOCK_ID, "BLOCK_PARENT_PACKAGE_ID": BLOCK_PARENT_PACKAGE_ID,
-            "PROTOCOL_STATUS": PROTOCOL_STATUS, "QUAKEC_STATUS": QUAKEC_STATUS,
-            "WORLD_PHYSICS_STATUS": WORLD_PHYSICS_STATUS,
-            "HOST_LIFECYCLE_STATUS": HOST_LIFECYCLE_STATUS,
-            "CLIENT_RENDER_STATUS": CLIENT_RENDER_STATUS,
-            "WORLD_RENDER_STATUS": WORLD_RENDER_STATUS,
-            "MODEL_UI_RENDER_STATUS": MODEL_UI_RENDER_STATUS,
-            "RENDER_SPECIAL_STATUS": RENDER_SPECIAL_STATUS,
-            "COMPATIBILITY_PROFILE": "compat_109",
-            "NATIVE_TEXT_ABI": "caller_owned_bytes_v1",
-            "PROTOCOL_TEXT_ABI": "quake_latin1_cstring_v1",
-        }
-        for name, wanted in expected.items():
-            match = re.search(rf'^const\s+{name}\s*=\s*"([^"]+)"\s*$', text, flags=re.M)
-            if not match or match.group(1) != wanted:
-                errors.append(f"build_info.ml {name} differs from {wanted!r}")
-            else:
-                details[name.lower()] = wanted
-        numeric = {
-            "HOST_LIFECYCLE_FINGERPRINT": HOST_LIFECYCLE_FINGERPRINT,
-            "CLIENT_RENDER_FINGERPRINT": CLIENT_RENDER_FINGERPRINT,
-            "WORLD_RENDER_FINGERPRINT": WORLD_RENDER_FINGERPRINT,
-            "MODEL_UI_RENDER_FINGERPRINT": MODEL_UI_RENDER_FINGERPRINT,
-            "RENDER_SPECIAL_FINGERPRINT": RENDER_SPECIAL_FINGERPRINT,
-        }
-        for name, wanted in numeric.items():
-            match = re.search(rf'^const\s+{name}\s*=\s*(0x[0-9a-fA-F]+|\d+)\s*$', text, flags=re.M)
-            if not match or int(match.group(1), 0) != wanted:
-                errors.append(f"build_info.ml {name} differs from 0x{wanted:08x}")
-        main = (root / "src/main.ml").read_text(encoding="utf-8")
-        for marker in (
-            "buildInfo.RENDER_SPECIAL_STATUS", "Render-special fingerprint: 0x2a3d8081",
-            "--render-evidence", "runRenderEvidenceCommand",
-        ):
-            if marker not in main:
-                errors.append(f"src/main.ml missing identity/command marker: {marker}")
-        purpose = re.search(r'^const\s+PACKAGE_PURPOSE\s*=\s*"([^"]+)"', text, flags=re.M)
-        purpose_text = purpose.group(1).lower() if purpose else ""
-        if not all(term in purpose_text for term in ("mirror", "envmap", "evidence")):
-            errors.append("PACKAGE_PURPOSE does not describe the current block")
-    except OSError as exc:
-        errors.append(f"cannot read package identity files: {exc}")
-    return CheckResult("package_identity", not errors, details, errors)
-
-
-
-def check_frozen_parent_contracts(root: Path) -> CheckResult:
-    """Validate accepted contracts and the green BP-055-059R2 Windows result."""
-    errors: list[str] = []
-    details: dict[str, object] = {}
-    try:
-        build = (root / "src/miniquake/build_info.ml").read_text(encoding="utf-8")
-        string_values = {
-            "PROTOCOL_STATUS": PROTOCOL_STATUS,
-            "QUAKEC_STATUS": QUAKEC_STATUS,
-            "WORLD_PHYSICS_STATUS": WORLD_PHYSICS_STATUS,
-            "HOST_LIFECYCLE_STATUS": HOST_LIFECYCLE_STATUS,
-            "CLIENT_RENDER_STATUS": CLIENT_RENDER_STATUS,
-            "WORLD_RENDER_STATUS": WORLD_RENDER_STATUS,
-            "MODEL_UI_RENDER_STATUS": MODEL_UI_RENDER_STATUS,
-            "RENDER_SPECIAL_STATUS": RENDER_SPECIAL_STATUS,
-            "AUDIO_STATUS": AUDIO_STATUS,
-        }
-        for key, wanted in string_values.items():
-            match = re.search(rf'^const\s+{key}\s*=\s*"([^"]+)"\s*$', build, flags=re.M)
-            if not match or match.group(1) != wanted:
-                errors.append(f"build_info.ml {key} differs from {wanted!r}")
-        numeric_values = {
-            "HOST_LIFECYCLE_FINGERPRINT": HOST_LIFECYCLE_FINGERPRINT,
-            "CLIENT_RENDER_FINGERPRINT": CLIENT_RENDER_FINGERPRINT,
-            "WORLD_RENDER_FINGERPRINT": WORLD_RENDER_FINGERPRINT,
-            "MODEL_UI_RENDER_FINGERPRINT": MODEL_UI_RENDER_FINGERPRINT,
-            "RENDER_SPECIAL_FINGERPRINT": RENDER_SPECIAL_FINGERPRINT,
-            "AUDIO_FINGERPRINT": AUDIO_FINGERPRINT,
-        }
-        for key, wanted in numeric_values.items():
-            match = re.search(rf'^const\s+{key}\s*=\s*(0x[0-9a-fA-F]+|\d+)\s*$', build, flags=re.M)
-            if not match or int(match.group(1), 0) != wanted:
-                errors.append(f"build_info.ml {key} differs from 0x{wanted:08x}")
-        analysis = (root / "docs/BP-055-059R2_ACCEPTANCE_ANALYSIS.md").read_text(encoding="utf-8")
-        markers = (
-            "BP-055–BP-059R2", "PASS", "143 required steps passed",
-            "7634070813d28b57ffdd0c18064e493187c4a0fc47259e55450aa7a75d3b32fd",
-            "f8aef48166ea9ffc3bfd864802238feec1b1e8a01dad27f3e55e3b47b20d99c1",
-            "d905b042",
-            "9988060eb6c3bb7692338b97325d50c97c233473298194a39196795ec88d4337",
-            "audio_109_frozen_v1", "0xdcf7a002",
-        )
-        for item in markers:
-            if item not in analysis:
-                errors.append(f"BP-055-059R2 acceptance analysis is missing {item}")
-        details.update({
-            "accepted_parent": "BP-055-059R2",
-            "parent_result_sha256": "7634070813d28b57ffdd0c18064e493187c4a0fc47259e55450aa7a75d3b32fd",
-            "parent_trace_sha256": "f8aef48166ea9ffc3bfd864802238feec1b1e8a01dad27f3e55e3b47b20d99c1",
-            "audio_status": AUDIO_STATUS,
-        })
-    except OSError as exc:
-        errors.append(f"cannot validate frozen parent contracts: {exc}")
-    return CheckResult("frozen_parent_contracts", not errors, details, errors)
-
-
 def check_network_platform_block_contract(root: Path) -> CheckResult:
     errors: list[str] = []
     details: dict[str, object] = {}
@@ -5473,6 +4966,7 @@ def check_network_platform_block_contract(root: Path) -> CheckResult:
     return CheckResult("network_platform_block_contract", not errors, details, errors)
 
 
+
 def check_current_delivery_scripts(root: Path) -> CheckResult:
     errors: list[str] = []
     details: dict[str, object] = {}
@@ -5485,28 +4979,25 @@ def check_current_delivery_scripts(root: Path) -> CheckResult:
             "MiniQuakeNetworkMainTests.exe", "MiniQuakeNetworkPlatformEvidence.exe",
             "check_network_060.py", "check_network_064.py",
         ],
-        "TEST_BP-060-064.ps1": [
+        "TEST_BP-060-064R2.ps1": [
             '$PackageId = "BP-064"', '$BlockId = "BP-060-064"',
-            '$DeliveryRevision = "BP-060-064"',
-            '$env:PYTHONUNBUFFERED = "1"', "Invoke-LiveProcess",
+            '$DeliveryRevision = "BP-060-064R2"',
+            '$env:PYTHONUNBUFFERED = "1"', "Invoke-LiveProcess", "Writer.Flush()",
             "Run-NetworkEvidencePair", "network evidence client reports differ",
-            "MiniQuake BP-060-064 acceptance test: PASS",
-        ],
-        "TEST_BP-060-064R1.ps1": [
-            '$PackageId = "BP-064"', '$BlockId = "BP-060-064"',
-            '$DeliveryRevision = "BP-060-064R1"',
-            '$env:PYTHONUNBUFFERED = "1"', "Invoke-LiveProcess",
-            "Writer.Flush()", "MiniQuake BP-060-064R1 acceptance test: PASS",
+            "MiniQuake BP-060-064R2 acceptance test: PASS",
         ],
+        "TEST_BP-060-064.ps1": ["TEST_BP-060-064R2.ps1", "superseded"],
+        "TEST_BP-060-064R1.ps1": ["TEST_BP-060-064R2.ps1", "superseded"],
         "COLLECT_RESULTS.ps1": [
-            "MiniQuake_BP-060-064R1_RESULTS_", '$DeliveryRevision = "BP-060-064R1"',
-            '$DeliveryParent = "BP-060-064"', "network_platform_closure_golden.json",
+            "MiniQuake_BP-060-064R2_RESULTS_", '$DeliveryRevision = "BP-060-064R2"',
+            '$DeliveryParent = "BP-060-064R1"', "network_platform_closure_golden.json",
             "MiniQuakeNetworkPlatformEvidence.exe", "patches\\BP-064.diff",
-            "patches\\BP-064R1.diff", "TEST_BP-060-064R1.ps1",
+            "patches\\BP-064R1.diff", "patches\\BP-064R2.diff",
+            "TEST_BP-060-064R2.ps1", "BP-060-064R2_HOTFIX_REPORT.json",
             "MiniQuakeProtocol15ServerDataTests.exe", "BP-012_PROTOCOL15_SERVERDATA_AUDIT.md",
             "protocol15_serverdata_golden.json",
         ],
-        "test.ps1": ["TEST_BP-060-064R1.ps1"],
+        "test.ps1": ["TEST_BP-060-064R2.ps1"],
     }
     for relative, markers in expected.items():
         try:
@@ -5518,21 +5009,14 @@ def check_current_delivery_scripts(root: Path) -> CheckResult:
             if item not in source:
                 errors.append(f"{relative} missing marker: {item}")
     details["scripts_checked"] = len(expected)
+    details["delivery_revision"] = DELIVERY_REVISION
     return CheckResult("delivery_scripts", not errors, details, errors)
 
-
 def check_live_output_contract(root: Path) -> CheckResult:
     errors: list[str] = []
     details: dict[str, object] = {}
     targets = {
-        "TEST_BP-060-064.ps1": (
-            '$env:PYTHONUNBUFFERED = "1"',
-            "function Invoke-LiveProcess",
-            "ForEach-Object",
-            "Writer.Flush()",
-            "Write-Host $Line",
-        ),
-        "TEST_BP-060-064R1.ps1": (
+        "TEST_BP-060-064R2.ps1": (
             '$env:PYTHONUNBUFFERED = "1"',
             "function Invoke-LiveProcess",
             "ForEach-Object",
@@ -5548,30 +5032,98 @@ def check_live_output_contract(root: Path) -> CheckResult:
         ),
     }
     forbidden = {
-        "TEST_BP-060-064.ps1": (r"\$Lines\s*=\s*@\(&",),
-        "TEST_BP-060-064R1.ps1": (r"\$Lines\s*=\s*@\(&",),
+        "TEST_BP-060-064R2.ps1": (r"\$Lines\s*=\s*@\(&",),
         "build.ps1": (r"\$OutputLines\s*=\s*@\(&\s*\$Executable",),
     }
-    for relative, required in targets.items():
+    for relative, markers in targets.items():
         try:
             source = (root / relative).read_text(encoding="utf-8-sig")
         except OSError as exc:
             errors.append(f"cannot read {relative}: {exc}")
             continue
-        for item in required:
+        for item in markers:
             if item not in source:
                 errors.append(f"{relative} is missing live-output marker: {item}")
         for pattern in forbidden.get(relative, ()):
             if re.search(pattern, source):
                 errors.append(f"{relative} still buffers a foreground child process: {pattern}")
     details.update({
-        "scripts_checked": len(targets),
-        "python_unbuffered": True,
-        "log_flush_per_line": True,
-        "foreground_output_streamed": True,
+        "scripts_checked": len(targets), "python_unbuffered": True,
+        "log_flush_per_line": True, "foreground_output_streamed": True,
+        "delivery_revision": DELIVERY_REVISION,
     })
     return CheckResult("live_output_contract", not errors, details, errors)
 
+def check_bp060064r2_rule_enumeration_contract(root: Path) -> CheckResult:
+    errors: list[str] = []
+    details: dict[str, object] = {}
+    expected_hash = "bde52d9bcb2e7052c3dcac5ee62dbe49dd3acd6887cbeabfc54c1f8b4916c719"
+    try:
+        loop = (root / "src/miniquake/net_loop.ml").read_text(encoding="utf-8-sig")
+        test = (root / "tests/network_control_parity_tests.ml").read_text(encoding="utf-8-sig")
+        analysis = (root / "docs/BP-060-064R2_RESULT_ANALYSIS.md").read_text(encoding="utf-8")
+        changelog = (root / "CHANGELOG_BP-060-064R2.md").read_text(encoding="utf-8")
+        hotfix = json.loads((root / "docs/BP-060-064R2_HOTFIX_REPORT.json").read_text(encoding="utf-8"))
+        ledger = json.loads((root / "BLOCK_LEDGER.json").read_text(encoding="utf-8"))
+        port = json.loads((root / "PORT_LEDGER.json").read_text(encoding="utf-8"))
+        for marker in (
+            'return error(3437, "unknown previous server rule")',
+            'if start >= len(rules) then return ["", ""] end if',
+            'rule = try(nextServerRule(state.serverRules, parsed[1][0]))',
+            'if rule is not error then',
+        ):
+            if marker not in loop:
+                errors.append(f"R2 production rule-enumeration marker missing: {marker}")
+        for forbidden in ("if found < 0 then return void end if", "if start >= len(rules) then return void end if"):
+            if forbidden in loop:
+                errors.append(f"R2 production still contains ambiguous void rule result: {forbidden}")
+        for marker in (
+            'unknownRule = try(netloop.nextServerRule(rules, "missing"))', "unknownRule is error",
+            'finalRule[0] == "" and finalRule[1] == ""',
+            "terminatorPacket = control.parse(control.replyRuleInfo(finalRule[0], finalRule[1]))",
+        ):
+            if marker not in test:
+                errors.append(f"R2 runtime fixture marker missing: {marker}")
+        for marker in (expected_hash, "65 Windows executables", "Cannot index void", "command-only"):
+            if marker not in analysis:
+                errors.append(f"R2 result analysis missing marker: {marker}")
+        for marker in ("live-output", "unknown previous rule", "command-only end marker"):
+            if marker not in changelog:
+                errors.append(f"R2 changelog missing marker: {marker}")
+        if hotfix.get("delivery_revision") != DELIVERY_REVISION or hotfix.get("delivery_parent") != DELIVERY_PARENT:
+            errors.append("R2 hotfix report delivery lineage differs")
+        if hotfix.get("predecessor_result", {}).get("sha256") != expected_hash:
+            errors.append("R2 hotfix report predecessor hash differs")
+        if hotfix.get("production_source_changed") is not True or hotfix.get("native_source_changed") is not False:
+            errors.append("R2 hotfix source-change classification differs")
+        if ledger.get("delivery_revision") != DELIVERY_REVISION or ledger.get("delivery_parent") != DELIVERY_PARENT:
+            errors.append("BLOCK_LEDGER.json R2 delivery lineage differs")
+        if ledger.get("predecessor_result", {}).get("sha256") != expected_hash:
+            errors.append("BLOCK_LEDGER.json predecessor result hash differs")
+        if port.get("current_delivery_revision") != DELIVERY_REVISION or port.get("delivery_parent") != DELIVERY_PARENT:
+            errors.append("PORT_LEDGER.json R2 delivery lineage differs")
+        if port.get("bp060064r2_hotfix", {}).get("contract_fingerprint") != "0xb3ec7589":
+            errors.append("PORT_LEDGER.json R2 hotfix fingerprint differs")
+        patch = root / "patches/BP-064R2.diff"
+        if not patch.is_file() or patch.stat().st_size == 0:
+            errors.append("patches/BP-064R2.diff is missing or empty")
+        else:
+            patch_text = patch.read_text(encoding="utf-8", errors="replace")
+            for marker in ("src/miniquake/net_loop.ml", "tests/network_control_parity_tests.ml"):
+                if marker not in patch_text:
+                    errors.append(f"BP-064R2 patch missing {marker}")
+            if re.search(r"^diff --git a/native/", patch_text, flags=re.M):
+                errors.append("BP-064R2 patch unexpectedly changes native source")
+        details.update({
+            "delivery_revision": DELIVERY_REVISION, "delivery_parent": DELIVERY_PARENT,
+            "predecessor_result_sha256": expected_hash, "compiled_targets": "65/65",
+            "bp060_runtime": "20/20 PASS", "rule_end": "command-only reply",
+            "unknown_previous": "no reply", "live_output": True,
+        })
+    except (OSError, json.JSONDecodeError, TypeError) as exc:
+        errors.append(f"cannot validate BP-060-064R2 rule-enumeration hotfix: {exc}")
+    return CheckResult("bp060064r2_rule_enumeration_contract", not errors, details, errors)
+
 def check_package_identity(root: Path) -> CheckResult:
     errors: list[str] = []
     details: dict[str, object] = {}
@@ -5647,6 +5199,7 @@ def run_checks(root: Path, manifest_path: Path) -> VerificationReport:
         check_current_component(root, "check_network_063.py", "bp063_system_platform_contract"),
         check_current_component(root, "check_network_064.py", "bp064_network_platform_closure_contract"),
         check_network_platform_block_contract(root),
+        check_bp060064r2_rule_enumeration_contract(root),
         check_current_delivery_scripts(root),
         check_live_output_contract(root),
         check_package_identity(root),
